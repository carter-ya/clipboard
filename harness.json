{
  "$schema_version": "1.0",
  "document_purpose": "Clipboard 项目的唯一权威文档。任何代理或开发者在提议改动前都必须先读这份文件。若代码与本文档不一致，必须在同一次提交中一并更新本文档——不得让两者静默分叉。",
  "last_updated": "2026-04-24",
  "project": {
    "name": "Clipboard",
    "one_liner": "macOS 的 Alfred 风格剪贴板历史——常驻菜单栏，支持全局快捷键、快速搜索；选中条目会写回系统剪贴板（由用户手动 Cmd+V 粘贴）。",
    "platform": "macOS 13.0+（部署目标）",
    "distribution": "直接分发未签名 .dmg（ad-hoc 签名 CODE_SIGN_IDENTITY=\"-\"）。用户首次安装需 xattr -cr 解除 Gatekeeper 隔离标记；docs/install.sh 提供一键脚本（curl | bash：拉最新 DMG → 校验 SHA-256 → 退出正在运行的 Clipboard → 拷到 /Applications → xattr -cr），随 docs/ 走 GitHub Pages 暴露为 https://carter-ya.github.io/clipboard/install.sh。Sparkle 通过 EdDSA 校验后续自动更新。暂不上 Mac App Store，暂不走 Apple Developer ID + 公证（$99/年预算未动）；如果用户量起来或收到 Gatekeeper 投诉再重新评估。",
    "language_locale": "UI 文案双语 zh-Hans + en（跟随系统语言，可在偏好设置中显式覆盖）；代码、注释、commit 信息一律使用英文。"
  },
  "non_goals": [
    "v1.x 不做 iCloud / 跨设备同步",
    "不移植到 Windows / Linux",
    "不走 Mac App Store 分发（对后台剪贴板监听应用而言 MAS 审核不现实）",
    "不做插件 / 扩展机制",
    "不做移动端伴侣应用",
    "v1.x 不做 AI 功能（摘要 / 翻译 / 改写）"
  ],
  "tech_stack": {
    "language": "Swift 5.9+",
    "ui_framework": "视图用 SwiftUI；NSStatusItem + NSPanel 外壳用 AppKit",
    "storage": "内存数组（权威）+ 磁盘 JSON 快照（Codable，原子写入）。较大的 payload（图片、RTF 二进制块）单独存为 blobs/<sha256>.<ext> 文件，由路径引用。容量默认上限 N=100，固定项（pinned）豁免。敏感条目只驻留内存、不落盘（见 data_model.persistence_strategy）。不引入任何数据库引擎。",
    "global_hotkey": "sindresorhus/KeyboardShortcuts（SPM）",
    "project_generation": "XcodeGen（project.yml 是源头；.xcodeproj 由其生成，不提交仓库）",
    "package_manager": "Swift Package Manager（不用 CocoaPods、不用 Carthage）",
    "task_runner": "just（仓库根目录的 Justfile）",
    "lint_format": "swift-format（Apple 官方）",
    "logging": "os.Logger（统一日志系统）",
    "testing": "XCTest + swift-testing（能简化书写时使用）",
    "ci": "GitHub Actions（在 macOS runner 上 build + test）——延后到 S5 之后再启用"
  },
  "targets": [
    {
      "name": "ClipboardApp",
      "type": "macOS .app",
      "role": "轻量的 UI 外壳与装配层，不包含业务逻辑。",
      "info_plist_keys": {
        "LSUIElement": true,
        "LSMinimumSystemVersion": "13.0"
      },
      "entitlements": {
        "com.apple.security.app-sandbox": false
      }
    },
    {
      "name": "ClipboardCore",
      "type": "Swift Package（library）",
      "role": "所有业务逻辑；不依赖 App 运行即可完整做单元测试。",
      "depends_on": [
        "KeyboardShortcuts"
      ]
    },
    {
      "name": "ClipboardCoreTests",
      "type": "Swift Package 测试 target",
      "role": "面向协议与 Fake 实现的单元测试 + 集成测试。"
    }
  ],
  "concurrency_model": {
    "monitor_queue": "专用 serial DispatchQueue（标签 com.clipboard.monitor）：承载 ~80ms 轮询定时器 + pasteboard 读取 + 廉价尺寸探测 + sourceBundleID 抓取 + ClipFilter 链执行。tick 默认只做一次 changeCount IPC 读；唯有 changeCount 推进时才继续拿 types/data 并跑 ClipFilter 链。只做轻量工作，绝不算 sha256、绝不做磁盘 I/O。",
    "store_queue": "专用 serial DispatchQueue（标签 com.clipboard.store）：承载 sha256 计算、去重、容量驱逐、blob 写入、JSON 原子写入。ClipStore 对外的 async API 内部都调度到此队列。",
    "main_queue": "SwiftUI / AppKit / HotkeyService 事件处理在主线程；ViewModel 通过 Combine / AsyncStream 订阅 ClipStore 的变更并在主线程刷新 UI。",
    "timer_leeway": "DispatchSourceTimer 给 ~20ms leeway，让调度器合并唤醒、降低功耗。",
    "rationale": "三队列分工杜绝『轮询线程因为 sha256 或磁盘写入阻塞而漏拷贝』以及『主线程被同步 I/O 卡住』。"
  },
  "module_protocols": [
    {
      "name": "ClipboardMonitoring",
      "responsibility": "输出原始剪贴板变更事件流。每次 changeCount 变化时在 monitor_queue 上：(1) 用 NSWorkspace 抓一次前台 App 的 bundleID；(2) 构造 ClipContext 并执行 ClipFilter 链；(3) 把未被 .reject 的条目发给下游（.markSensitive 的条目会带上 sensitive=true 标记）。",
      "api": "var changes: AsyncStream<RawClipItem> { get }  ; func start() ; func stop()",
      "impl": "NSPasteboardMonitor 在 monitor_queue 的 DispatchSourceTimer 每 300ms 轮询一次 NSPasteboard.general.changeCount；未变化则直接返回。变化时：(1) 读 NSPasteboardItem.types 做粗分流；(2) 廉价探测尺寸——public.file-url 用 stat() 累加被引文件大小（不读文件内容），对大概率大体积的内联类型（public.tiff/public.png/public.data/public.image）按 maxClipSizeBytes 为上限限长读取，一旦累计超过立即丢弃该类型并把整条 RawClipItem 标记为 tooLarge；(3) 抓前台 App bundleID；(4) 把 RawClipItem + ClipContext 交给 ClipFilter 链，让链路决定 accept / reject / markSensitive；(5) 把决策结果发给下游。Monitor 本身不做 sha256、不写盘、不持久化。",
      "fake": "FakeClipboardMonitor 接受测试或场景中手动调用 .push(item:)。"
    },
    {
      "name": "ClipFilter",
      "responsibility": "判定一个原始剪贴项应被接受（.accept）、拒绝（.reject，绝不进 store）还是标记为敏感（.markSensitive，进 store 但仅驻留内存、UI 遮罩、不落盘）。作为可组合链路在 Monitor 内部、发出事件之前应用。",
      "api": "struct ClipContext { sourceBundleID: String?; changeCount: Int; timestamp: Date } ; enum FilterDecision { case accept, reject(String), markSensitive(String) } ; func evaluate(_ item: RawClipItem, context: ClipContext) -> FilterDecision",
      "impl": "有序链路，首次遇到 .reject 即短路返回；首次遇到 .markSensitive 也短路返回（后续 filter 不再评估，该条目一律敏感）：SizeFilter(maxClipSizeBytes) → SensitivityFilter（识别 org.nspasteboard.ConcealedType）→ BlocklistFilter（按 sourceBundleID 匹配黑名单）。每个 filter 都是独立的小类型，便于单测。",
      "fake": "各 filter 本就是纯函数，测试直接使用真实实现并注入配置即可。"
    },
    {
      "name": "ClipStore",
      "responsibility": "在内存中持有剪贴项（权威数据），快照落盘（sensitive 条目排除），提供搜索、置顶、删除，并执行容量上限。",
      "api": "insert(_:) / search(query:filters:) / pin(id:) / delete(id:) / clearAll() / item(id:) / all() / observe() -> AsyncStream<StoreEvent>",
      "impl": "JSONSnapshotClipStore：内存中是按时间倒序的数组（最新在前），所有写操作在 store_queue 上串行执行。插入时先算 sha256 → 若已存在相同哈希则更新 createdAt、移到队首、保留原 sourceBundleID（first-write-wins）与 pinned 状态、丢弃新来的条目；否则新增。blob 引用按 sha256 计数：新增条目若 payload.blobPath 指向的 sha256 首次出现才真正写文件，去重命中时 refcount+1，驱逐时 refcount-1、归零才 unlink。sensitive 条目进入内存数组，但写盘阶段过滤掉——即不进入 history.json、其大 payload 也不写 blobs/（仅保留内联 inlineData 于内存）。任何非敏感变更都会安排一次防抖（500ms）的原子写入 history.json（先写 .tmp 再 rename）。启动时：解码 history.json；若失败则重命名为 .bak 并以空历史启动，然后发出一个可被 UI 捕获的 store.corrupted 事件；启动末尾跑一次 reconcileBlobs()——扫描 blobs/ 目录，凡不在 in-memory refcount 表里的文件一律删除（孤儿清理）。搜索是对 preview 字段的大小写不敏感子串匹配（preview 已硬截断到 256 字符；长文本中段不可搜是已知限制），再按 kind/pinned/sourceBundleID/sensitive 过滤——N ≤ 几千时线性扫描完全够用。",
      "fake": "InMemoryClipStore（不做磁盘 I/O）用于测试；真实实现也可以指向临时目录运行。"
    },
    {
      "name": "HotkeyService",
      "responsibility": "监听用户配置的全局快捷键并发出事件；暴露『绑定失败 / 冲突』信号给 UI 层。",
      "api": "var events: AsyncStream<Void> { get } ; var bindingStatus: AsyncStream<BindingStatus> { get } ; func bind(_ shortcut: KeyboardShortcuts.Name)",
      "impl": "KeyboardShortcutsHotkeyService 包装 sindresorhus/KeyboardShortcuts；bind 失败时通过 bindingStatus 发出 .conflict(reason) 让 UI 弹引导页。",
      "fake": "FakeHotkeyService 提供 .fire() / .simulateConflict() 手动触发，用于测试。"
    },
    {
      "name": "PasteboardWriter",
      "responsibility": "把选中的 ClipItem 的所有 payload 写回系统剪贴板。不会模拟 Cmd+V——由用户自行粘贴。敏感条目同样可被写回（用户主动选择的行为）。",
      "api": "func write(_ item: ClipItem) throws",
      "impl": "NSPasteboardWriter：清空 NSPasteboard.general，然后按每个 payload 的原始 pasteboardType 声明并写入（inlineData 或从 blobPath 读回的文件内容）。接收方 App 自行挑选它支持的最丰富类型。",
      "fake": "RecordingPasteboardWriter 记录调用以便断言。"
    }
  ],
  "data_model": {
    "storage_layout": {
      "root": "~/Library/Application Support/Clipboard/",
      "files": [
        "history.json — 有序历史，仅含非敏感条目，原子写入（tmp + rename）",
        "history.json.bak — 启动时解码失败会自动生成",
        "blobs/<sha256>.<ext> — 不适合内联的非敏感二进制 payload（图片、rtf、file-url 列表）；敏感条目的大 payload 不落盘",
        "preferences.json — 用户设置（快捷键绑定、保留策略、maxClipSizeBytes、skipSensitive、blocklistEnabled、blockedBundleIDs、外观、语言覆盖等）"
      ]
    },
    "clip_item_schema": {
      "id": "UUID 字符串",
      "createdAt": "ISO-8601 字符串（去重命中时也会被更新）",
      "kind": "text | rtf | image | file | mixed。mixed 指单次剪贴同时包含多种主要 kind（例如同时带 public.rtf 和 public.tiff）；判定规则：按 pasteboardType 优先级（rtf > image > file > text）取主 kind，其余类型并存于 payloads。",
      "preview": "用于列表展示和搜索的短人类可读字符串——**硬截断到 256 字符**（超长文本只搜索截断后的前缀；完整内容仍在 payloads 中）",
      "sha256": "跨所有 payload 计算的内容哈希，用于去重",
      "sizeBytes": "所有 payload 的总字节数",
      "pinned": "bool",
      "sensitive": "bool——SensitivityFilter 或 BlocklistFilter 返回 .markSensitive 时置 true。语义：该条目只驻留内存、不写 history.json 也不写 blobs/；UI 默认以遮罩呈现 preview 和 payloads，可通过『显示敏感内容』操作临时揭示；App 重启后丢失。",
      "sourceBundleID": "拷贝时前台 App 的 bundle identifier（可空）——在 Monitor 抓取 changeCount 变化时同步读取 NSWorkspace.shared.frontmostApplication",
      "payloads": "{ pasteboardType, inlineData?（≤ 16KB 时用 base64 内联；sensitive 条目一律内联，无论大小，以避免落盘）, blobPath?（相对于 blobs/） } 数组——inlineData / blobPath 二者只占其一"
    },
    "inline_threshold_bytes": 16384,
    "retention": {
      "primary": "按条数限制（默认 100，可配置）。固定项不计入上限，也不会因上限被驱逐——即便固定项的条数本身 ≥ cap，也全部保留。",
      "secondary_optional": "按时间上限（默认关闭）、按总大小上限（默认关闭），均可由用户配置。"
    },
    "dedup": "插入时对条目的**规范化主内容**计算 sha256，而非对整组 payload 做整体哈希。原因：同一份内容被不同 App 复制时，附带的 UTI 元数据（如 NSStringPboardType、各家 App 的动态派生类型）会不同，但主内容相同——若整组哈希，稍微不同的 UTI 集合会产出不同的 sha 并导致重复行。规范化规则：按优先级（public.png / public.tiff / public.jpeg > public.rtf > public.utf8-plain-text / public.plain-text > public.html > public.file-url）选取首个命中的主 payload，只对该 payload 的 (pasteboardType + data) 做哈希；若没有任何主类型命中，则退回全 payload 哈希。若已有条目的 sha256 相同，则更新其 createdAt 为当前时间、移到队首、**保留原 sourceBundleID 和 pinned 状态**（first-write-wins），丢弃新来的条目。去重不做时间窗口限制。若原条目是非敏感而新来的是敏感（源 App 切到黑名单时），不升格——以第一次为准。",
    "persistence_strategy": "运行期以内存数组为权威，同时持有 sensitive 条目；磁盘快照仅包含非敏感条目。任何非敏感变更都会安排一次防抖（500ms）的原子快照写入 history.json；敏感变更不触发写盘。优雅关闭时立即 flush（仅非敏感）。崩溃时最多丢失一个防抖窗口的非敏感数据；敏感条目必然丢失（符合『不明文落盘』的安全目标）。启动末尾跑一次 reconcileBlobs() 清理孤儿 blob。",
    "search_semantics": "仅对 preview（硬截断 256 字符）做大小写不敏感子串匹配，再叠加 kind/pinned/sourceBundleID/sensitive 过滤。长文本中段不可搜是 v1 的已知限制；若未来要支持全文搜索需引入索引（v1 不做）。sensitive 条目默认包含在搜索结果中但展示时遮罩。",
    "ui_mask_policy": "sensitive=true 的条目在 HistoryPanel 中：row 显示『●●●●（来自 <App>）』+ 锁图标，时间与 kind 正常显示；选中该行即写入剪贴板（行为一致），但 preview 不明文渲染；预览面板同样以遮罩呈现，提供『临时显示』按钮（仅本次会话生效，不改变持久状态）。",
    "import_semantics": "导入 JSON 采用合并语义——按 sha256 去重后与现有历史合并；缺失 blob 的条目整条跳过并在导入结果里计数；导入不会带入 sensitive 条目（导出时就已排除）。"
  },
  "ui_interactions": {
    "panel_open_resets_selection": "每次 HistoryPanel 打开时（菜单栏点击 / ⌥⌘V / 菜单『Show History』），自动把 selectedID 重置为 filteredItems.first?.id 并把列表滚动到首行——避免上次的陈旧选中状态 **和滚动位置** 影响这次操作。列表滚动通过 ScrollViewReader + ViewModel.scrollEpoch 计数器驱动：每次 resetSelection() 时 epoch +1，View 的 .onChange(scrollEpoch) 回调里调 proxy.scrollTo(firstID, anchor: .top)。",
    "row_click_activates": "点击 HistoryPanel 中的任一行即视为激活——写回剪贴板 + 关闭面板。单击而非双击；双击不再需要。实现：ClipRowView.onTapGesture（count:1）→ onActivate；与 ↵ 共享同一路径（suppressNextCloseCommit 防双写）。",
    "panel_close_commits_selection": "HistoryPanel 的**任何**关闭路径（Esc / 面板外点击 / 再次按 ⌥⌘V / 菜单栏再点图标 / 窗口 X 按钮）都会把**当前选中项**写回系统剪贴板——等同于用户显式按 ↵。副作用：该条目经由 Monitor→Store 的去重链路自动移到队首（dedup first-write-wins 保留原 sourceBundleID）。如果关闭时无选中项（列表空），则普通关闭。显式的 ↵ 路径已提交过则不重复提交（用 suppress 标志避免双写）。",
    "click_outside_closes": "面板可见期间通过 NSEvent.addGlobalMonitorForEvents(.leftMouseDown | .rightMouseDown) 监听鼠标点击。**关键坑**：`.nonactivatingPanel` 样式下本 App 从未 activate，AppKit 把我们自己面板上的标题栏/关闭按钮等 chrome 区域的点击也当作『发给其他 App 的事件』派发给 global monitor——会导致『点一下标题窗口自己关了』的 bug。因此回调里不能无条件 close：必须先拿 NSEvent.mouseLocation（屏幕坐标）与 self.frame 比较，**仅当点击在面板矩形之外才 close**。监听器生命周期依旧与面板可见性绑定：show 时装、close 时拆。",
    "preferences_activation_policy": "打开偏好设置窗口前把 NSApp.activationPolicy 切到 .regular（App 短暂获得 Dock 图标与完整键盘焦点），关闭偏好后切回 .accessory。原因：LSUIElement=true 的菜单栏 App 默认不接受标准键盘输入焦点，KeyboardShortcuts.Recorder 等需要录制键盘的控件在 .accessory 下会失灵。切换时机：PreferencesWindowController.show() 之前切 .regular；windowWillClose 通知到达时切回 .accessory。",
    "no_menu_bar_icon": "**不放菜单栏图标**——LSUIElement=true 的纯后台 App 依赖全局快捷键（⌥⌘V）作为唯一入口。首启引导窗口（OnboardingController.showIfFirstRun）承担『告知用户默认快捷键』的任务。次级操作（Preferences / Pause / Clear / About / Quit）全部从 HistoryPanel 内部的键盘路径或从 Preferences 窗口内部到达——见下。回退路径：`applicationShouldHandleReopen(_:hasVisibleWindows:)` 在用户再次从 Finder / Dock / Spotlight 启动已运行的 App 时打开 Preferences 窗口，避免『装完不见了』的困惑。",
    "single_instance": "**同一时刻只允许一个 Clipboard 进程运行**——无菜单栏图标意味着用户不会发现已经有一份在跑，同时开两份会出现两个 Monitor 并发读 pasteboard / 两份 store 竞争同一份 history.json / blobs 目录的混乱。启动流程：main.swift 在创建 NSApplication 之前用 NSRunningApplication.runningApplications(withBundleIdentifier:) 过滤掉自身 pid，若仍有实例：(1) activate 那个既有实例；(2) 通过 DistributedNotificationCenter 广播 'com.clipboard.app.reopen' 通知；(3) exit(0) 终止当前进程。既有实例的 AppDelegate 在 applicationDidFinishLaunching 里订阅同名通知，收到时等价于执行 applicationShouldHandleReopen——打开 Preferences，给用户一个清晰的『你以为启动的那份其实是唤起旧的』反馈。开发时可通过环境变量 CLIPBOARD_ALLOW_MULTIPLE=1 跳过检查，避免 Xcode Run 与已运行实例冲突。",
    "panel_cmd_comma_opens_prefs": "HistoryPanel 可见时按 ⌘, 关闭面板并打开 Preferences 窗口。实现：panel 里一个隐藏的 .keyboardShortcut(',', modifiers: .command) 按钮触发 onShowPreferences 回调；回调先 panel.suppressNextCloseCommit=true（避免焦点切换误提交选中条目），再调 panel.close()，再让 AppDelegate 走它原有的 openPreferences 路径（会再次尊重 S4 的窗口互斥约束）。",
    "panel_hosts_arrow_keys": "HistoryPanel 可见时，**无论焦点在哪**（搜索框 / 列表 / 按钮），↑↓ 都只做条目选择——不做文本框光标移动。实现：panel 在 show 时装一个 NSEvent.addLocalMonitorForEvents(.keyDown) 的监听，keyCode 125（Down）/ 126（Up）时调 viewModel.selectNext/selectPrevious 并返回 nil 吞掉事件；其他键直接返回事件让它继续走正常响应链。监听器生命周期与 outsideClickMonitor 一致——show 装、close 拆。这是 macOS 13 的折中方案（macOS 14+ 有 .onKeyPress，但我们目标 13）。",
    "activate_bumps_to_top": "用户选中并激活（click / ↵ / close-commit）一条历史时，该条目应**立即**移到队首，不依赖 Monitor→Store 的 poll 循环。AppWiring.activate 除了 PasteboardWriter.write 还需调 ClipStore.bumpToTop(id:)——同步更新 createdAt、移到数组首、发出 .updated 事件、触发防抖写盘。（Monitor 随后对新的 pasteboard 状态也会触发一次 insert，但因 sha256 去重命中，会无副作用地再 bump 一次 createdAt。）",
    "tab_switch_resets_selection": "切换 All / Pinned 标签时，若原 selectedID 已不在 filteredItems 里，键盘 ↑↓ 无法继续工作——必须把 selectedID 重新指向新列表的首行。实现：HistoryPanelView 的 .onChange(of: viewModel.currentTab) 回调里调 viewModel.resetSelection()——也顺便把列表滚到顶端，和面板开启路径一致。",
    "panel_keyboard_shortcuts": "HistoryPanel 可见时的键盘快捷键规范（**活约定**，取代 S9/S25 等历史 slice 中的旧描述 ⌘1–9 快速激活）。(1) ↵ 激活当前选中（写剪贴板 + 关面板）；(2) ↑↓ 行间移动选择（见 panel_hosts_arrow_keys）；(3) Esc 关闭面板；(4) ⌘⌫ 删除当前选中；(5) ⌘P 切换选中 pin；(6) ⌘, 关面板并打开 Preferences（见 panel_cmd_comma_opens_prefs）；(7) ⌘⇧[ / ⌘⇧] 在 HistoryPanelTab.allCases 里循环切换（两端 wrap），遵循 macOS 通用『上/下一 tab』约定——两 tab 下等价 toggle，实现走 viewModel.cycleTab(forward:) 以便日后扩展；(8) ⌘1–⌘6 直达 Kind 过滤，顺序与 KindChipBar 一致：⌘1=All(nil)、⌘2=Text、⌘3=Image、⌘4=File、⌘5=Rich(rtf)、⌘6=Mixed；按同键不 toggle（清空过滤走 ⌘1）。实现统一走 HistoryPanelView.keyboardShortcutButtons 里的隐藏 .keyboardShortcut Button，与 @FocusState 搜索框焦点共存（SwiftUI 在字符写入 TextField 之前拦截这些组合）。注意坑：SwiftUI `.keyboardShortcut` 对 shift+digit 的字符匹配不可靠（按 ⇧1 实际产生 `!`，不会匹配 KeyEquivalent(\"1\")）——本表刻意避开 ⌘⇧+数字组合；若将来必须加，改用 NSEvent.addLocalMonitorForEvents 按 keyCode 识别（风格与 panel_hosts_arrow_keys 一致），别用字形变体（! @ # $ % ^，非 US 键盘会失效）。"
  },
  "filtering": {
    "pipeline_order": [
      "SizeFilter",
      "SensitivityFilter",
      "BlocklistFilter"
    ],
    "applied_at": "在 ClipboardMonitoring 内部、RawClipItem 被发到下游之前应用；链路的决策会附加到条目上传递给 ClipStore（reject 的条目根本不会到 store；markSensitive 的条目到 store 时 sensitive=true）。",
    "filters": {
      "SizeFilter": {
        "config_key": "maxClipSizeBytes",
        "default": 10485760,
        "default_human": "10 MiB (10 * 1024 * 1024)",
        "configurable": true,
        "unit": "bytes",
        "semantics": "单次剪贴的所有 pasteboard 类型总字节数。public.file-url 的大小为被引路径的 stat() 文件大小之和（不读取文件内容）。内联类型（text/rtf/image/data）的大小按限额读取——读到 >= maxClipSizeBytes 即早停并返回 .reject(tooLarge)，不读完整数据。maxClipSizeBytes 设为 0 表示禁用上限。返回值：超限时 .reject('tooLarge'); 否则 .accept。",
        "user_setting_label": "跳过大于 … 的条目",
        "owner_slice": "S1"
      },
      "SensitivityFilter": {
        "config_key": "skipSensitive",
        "default": true,
        "semantics": "若剪贴板声明了 'org.nspasteboard.ConcealedType' 类型（密码管理器社区约定），返回 .markSensitive('concealedType')——条目进入 store 但仅驻留内存、UI 遮罩、不落盘。当 skipSensitive 为 false 时，直接返回 .accept（用户显式选择不做标记）。",
        "owner_slice": "S7"
      },
      "BlocklistFilter": {
        "config_key": "blockedBundleIDs",
        "toggle_key": "blocklistEnabled",
        "toggle_default": true,
        "default": [
          "com.1password.1password",
          "com.1password.1password7",
          "com.agilebits.onepassword7",
          "com.bitwarden.desktop",
          "com.apple.keychainaccess"
        ],
        "semantics": "若 blocklistEnabled 为 false，视为空集直接 .accept（用户显式关闭了来源屏蔽，允许从这些应用拷贝的内容以普通条目进历史并落盘）。否则：若 ClipContext.sourceBundleID 命中黑名单，返回 .markSensitive('blockedSource:<bundleID>')——条目进入 store 但仅驻留内存、UI 遮罩、不落盘。UI 上 blocklistEnabled=false 时屏蔽列表编辑框只读、内容仍展示以便用户随时重新启用。",
        "owner_slice": "S7"
      }
    },
    "logging": "每次 .reject 或 .markSensitive 都会发出结构化事件（monitor.skipped 或 monitor.sensitive）——用户调查『为什么我刚复制的东西没出现 / 为什么显示为遮罩』时可通过 `just logs` 查看。"
  },
  "harness": {
    "justfile_commands": {
      "gen": "xcodegen generate",
      "build": "xcodebuild -scheme ClipboardApp -destination 'platform=macOS' -derivedDataPath build build",
      "run": "just build && open build/Build/Products/Debug/Clipboard.app",
      "test": "swift test --package-path Core",
      "lint": "swift-format lint --strict --recursive Core/Sources Core/Tests App",
      "fmt": "swift-format format --in-place --recursive Core/Sources Core/Tests App",
      "logs": "log stream --level debug --predicate 'subsystem == \"com.clipboard.app\"'",
      "reset": "rm -rf \"$HOME/Library/Application Support/Clipboard\"",
      "scenario": "swift run --package-path Core scenario-runner",
      "clean": "rm -rf build .build *.xcodeproj DerivedData"
    },
    "observability": {
      "log_subsystem": "com.clipboard.app",
      "log_categories": [
        "monitor",
        "store",
        "hotkey",
        "paste",
        "ui",
        "scenario"
      ],
      "structured_events": [
        "monitor.change{changeCount,types,bundleID}",
        "monitor.skipped{reason,bytes?,limit?,types,bundleID}",
        "monitor.sensitive{reason,types,bundleID}",
        "store.insert{id,kind,size,sensitive}",
        "store.evict{id,sha256,blobDeleted}",
        "store.corrupted{path,renamedTo}",
        "store.reconcile{orphansDeleted}",
        "hotkey.fired{elapsedMs}",
        "hotkey.bindingFailed{reason}",
        "paste.write{itemId,types,sizeBytes,sensitive}"
      ],
      "dev_mirror_log_to_file": "~/Library/Logs/Clipboard/dev.log（仅当环境变量 CLIPBOARD_DEV=1 时生效）"
    },
    "conventions": [
      "NSPasteboard 的读只在 ClipboardMonitoring 的实现里发生；NSPasteboard 的写只在 PasteboardWriter 的实现里发生。",
      "不合成键盘事件（不用 CGEvent / AppleScript / Accessibility 自动化）——选中条目只负责写入剪贴板，由用户手动粘贴。",
      "HistoryPanel（.floating 层级）与本应用任何其他窗口互斥——展示其他窗口前必须先 dismiss HistoryPanel，避免置顶冲突。",
      "敏感条目绝不以明文形式落盘——JSON 快照和 blobs/ 均排除 sensitive=true 的条目；导出也排除。",
      "Monitor 只做 probe + bundleID 抓取 + filter 链；sha256 / 持久化 / blob 读写一律交给 store_queue。",
      "队列分工：monitor_queue（轮询与过滤）、store_queue（哈希与持久化）、main_queue（UI）——不得跨队列同步阻塞。",
      "UI 层通过协议依赖 Core，不直接依赖具体实现——装配点在 App/AppWiring.swift。",
      "SwiftUI View 和 AppDelegate 内不放业务逻辑。",
      "Core 的公共 API 只有显式标注时才 @MainActor 安全，其余默认按后台线程对待。",
      "人体工程学场景下错误用 Swift typed throws；其他情况用领域特定的 Error enum。",
      "测试之外禁止 force unwrap；除调试期的程序员错误外禁止 fatalError。",
      "日志只走 os.Logger；提交的代码中不得使用 print()。"
    ]
  },
  "slices": [
    {
      "id": "S0",
      "title": "Harness 脚手架",
      "goal": "项目可编译，菜单栏图标可见，测试通过，Justfile 可用。",
      "deliverables": [
        "`git init` 初始化仓库（默认分支 main），先落 .gitignore 再提交任何源码",
        ".gitignore（忽略 *.xcodeproj、build/、DerivedData/、.build/、.DS_Store）",
        "project.yml（XcodeGen）",
        "Justfile",
        "Core Swift Package 骨架，含空协议",
        "ClipboardApp target：AppDelegate 注册 NSStatusItem 并显示占位图标",
        "CLAUDE.md：列出构建命令与约定概要",
        "一个通过的占位 XCTest",
        "初始 commit 使用 commit_message 提交骨架"
      ],
      "acceptance": [
        "`git status` 在干净工作区显示无未追踪文件（*.xcodeproj、build/、DerivedData/、.build/ 等均被忽略）。",
        "`git log` 至少有一条 commit，即 S0 的初始 commit。",
        "冷启动 `just gen && just build` 成功。",
        "`just run` 后 App 打开，菜单栏有图标（无 Dock 图标）。",
        "`just test` 输出至少一个通过的用例。",
        "`just lint` 对已提交代码零违规。"
      ],
      "commit_message": "S0: harness bootstrap (xcodegen, just, core skeleton, menu bar shell)"
    },
    {
      "id": "S1",
      "title": "剪贴板监听 → 日志",
      "goal": "系统中的每一次拷贝事件都产生一条结构化日志，过滤链骨架就位。",
      "deliverables": [
        "ClipboardMonitoring 协议 + NSPasteboardMonitor 实现（monitor_queue 上 ~80ms DispatchSourceTimer，leeway ~20ms；tick 每次先读 changeCount，命中才进一步读内容）",
        "RawClipItem 值类型，覆盖所有可用的 NSPasteboard.PasteboardType",
        "ClipContext 结构 + FilterDecision 枚举（.accept | .reject(String) | .markSensitive(String)）+ ClipFilter 协议",
        "ClipFilter 链路装配（在 Monitor 内部、发出条目之前），首遇 .reject / .markSensitive 即短路",
        "SizeFilter 实现：file-url 用 stat() 不读文件；内联类型按 maxClipSizeBytes 为上限限长读取，累计超限即早停并返回 .reject('tooLarge')；maxClipSizeBytes=0 禁用",
        "SensitivityFilter 的 no-op 占位实现（始终 .accept）——真正的 ConcealedType 识别放 S7",
        "BlocklistFilter 的 no-op 占位实现（始终 .accept）——真正的黑名单匹配放 S7",
        "sourceBundleID 抓取点：monitor 每次 changeCount 变化时在 monitor_queue 上同步调一次 NSWorkspace.shared.frontmostApplication.bundleIdentifier，放入 ClipContext",
        "App 启动时装配 Monitor 并为每次变更记日志；被拒条目发出 monitor.skipped{reason,bytes?,limit?,types,bundleID}",
        "为测试提供 FakeClipboardMonitor",
        "单元测试覆盖：同 changeCount 不重复发出、多类型抽取、SizeFilter 接受/拒绝边界、超大 file-url 在不读取文件内容的前提下被拒、内联类型读满 maxClipSizeBytes 即早停未读完整数据、maxClipSizeBytes=0 禁用、占位 filter 始终 .accept"
      ],
      "acceptance": [
        "在任意 App 中拷贝文本 / 图片 / 文件 → `just logs` 1 秒内看到 monitor.change 事件，伴随正确的 bundleID。",
        "拷贝 >10 MiB 文件 → `just logs` 看到 monitor.skipped{reason:'tooLarge',...}，且没有对应的 monitor.change。",
        "将 maxClipSizeBytes 设为 0 → 之前被拒的大文件此时能被接受。",
        "单元测试全绿。"
      ],
      "commit_message": "S1: NSPasteboard monitor, filter chain skeleton, size filter"
    },
    {
      "id": "S2",
      "title": "内存 store + JSON 快照持久化",
      "goal": "所有被监听到的非敏感变更都持久化、去重、并执行容量上限——不引入数据库依赖。敏感条目仅驻留内存。",
      "deliverables": [
        "ClipItem / Payload Codable 模型（sensitive、sourceBundleID 字段齐全）",
        "实现 ClipStore 的 JSONSnapshotClipStore（有序内存数组 + 防抖原子写，所有写在 store_queue 上串行）",
        "preview 硬截断到 256 字符",
        "Blob 写入路径：非敏感 payload > inline_threshold_bytes（16KB）走 blobs/<sha256>.<ext>；敏感 payload 一律内联于内存，不写盘",
        "blob 引用计数表（按 sha256 计数）——去重命中 refcount+1；驱逐 refcount-1；归零才 unlink。驱逐事件 store.evict{...,blobDeleted} 记录是否删文件",
        "启动末尾的 reconcileBlobs()：扫描 blobs/ 目录，与 in-memory refcount 表做差集，删除孤儿；发出 store.reconcile{orphansDeleted}",
        "持久化过滤：写 history.json 时排除 sensitive=true 的条目；敏感条目不触发防抖写盘",
        "桥接：ClipboardMonitoring 事件流 → ClipStore.insert（把 filter 的决策转为 sensitive 标志）",
        "插入时执行容量上限（超过上限就驱逐最旧的非 pinned 条目）；pinned 不计入 cap 也不会被驱逐",
        "去重命中时：更新 createdAt、移到队首、保留原 sourceBundleID 与 pinned 状态、不升格 sensitive 标志",
        "启动加载带损坏恢复（解码失败则重命名为 .bak 并以空历史启动；发出 store.corrupted 事件）",
        "优雅关闭时的 flush 钩子（通过 NSApplication willTerminate）",
        "测试：去重把已有条目移到队首而非复制且 sourceBundleID 保留；容量驱逐跳过 pinned；pinned 超过 cap 也全部保留；损坏文件恢复生成 .bak 且发出 store.corrupted；round-trip（写入 → 重载 → 相等）；sensitive 条目不出现在 history.json、其 blob 文件不出现在 blobs/；reconcileBlobs 删除孤儿；blob 引用计数在多条目共享同一 sha256 时正确；搜索按 kind/pinned/sensitive 过滤；preview 被硬截断"
      ],
      "acceptance": [
        "拷贝 5 个不同内容 → history.json 有 5 个条目，按时间倒序。",
        "同一内容拷贝两次 → 只剩 1 条在队首，createdAt 被更新，sourceBundleID 保留首次值。",
        "cap=3 且已有 1 条 pinned 的情况下再拷贝 5 个新内容 → 剩下 1 pinned + 3 最新 = 共 4 条。",
        "杀掉 App 再重启 → 非敏感历史完好无损；敏感历史如预期丢失。",
        "手动破坏 history.json 后重启 → 启动成功，生成 .bak 文件，发出 store.corrupted 事件。",
        "模拟注入一个 sensitive=true 的条目 → 它能被搜索到、可被选中写回剪贴板；但 history.json 里没有它；blobs/ 里没有它的大 payload 文件。",
        "在 blobs/ 手工放一个无人引用的文件，重启 App → 该文件被 reconcileBlobs 删除。"
      ],
      "commit_message": "S2: in-memory store, JSON snapshot, dedup, refcounted blobs, sensitive items stay in memory"
    },
    {
      "id": "S3",
      "title": "最小 HistoryPanel UI",
      "goal": "点击菜单栏图标会打开面板，列出最近条目并支持实时搜索；UI 能对 store 的变更做增量刷新。",
      "deliverables": [
        "HistoryPanel（NSPanel 内的 SwiftUI），含搜索框 + List",
        "观察 ClipStore 的 ViewModel（通过 AsyncStream），对搜索输入做 150ms 防抖",
        "行视图显示 kind 图标、preview（已截断）、相对时间、来源 App",
        "面板打开时发生新拷贝：列表自动增量刷新、当前选中行保留（通过 item id）",
        "缩略图策略预埋：行视图从 payload 生成 64×64 预览位图并通过 LRU 内存缓存（容量 64 项或 16MB 上限，取先到）——S3 先提供文本/图标的轻量实现，S6 接入真实缩略图生成",
        "键盘：↑↓ 移动选择，Esc 关闭"
      ],
      "acceptance": [
        "点击菜单栏图标可切换面板显示 / 隐藏。",
        "面板处于打开状态时再拷贝新内容，列表会在 1 秒内出现新行，已选中行不被重置。",
        "1k 行数据下搜索的感知延迟 <100ms。",
        "Esc 可关闭面板。"
      ],
      "commit_message": "S3: history panel with search, keyboard nav, live refresh, thumbnail cache"
    },
    {
      "id": "S4",
      "title": "全局快捷键 + 非激活面板",
      "goal": "按下已配置的快捷键即可召唤面板，浮现在所有 App、所有 Space、全屏应用之上，且不从前台 App 抢走焦点。",
      "deliverables": [
        "HotkeyService 实现（基于 KeyboardShortcuts）；默认 ⌥⌘V",
        "NSPanel 配置：.nonactivatingPanel 样式，层级使用 NSWindow.Level.floating（必要时可上调到 .popUpMenu），collectionBehavior 包含 .canJoinAllSpaces + .fullScreenAuxiliary + .stationary（面板浮现在所有 App、所有 Space、包括全屏应用之上），hidesOnDeactivate=false，becomesKeyOnlyIfNeeded=true——这样目标 App 始终保持前台状态，面板关闭后用户可直接 Cmd+V",
        "多显示器规则：面板出现在光标所在的那块屏幕（而非主屏）",
        "窗口互斥约束：HistoryPanel 与本应用其他任何窗口（偏好设置、引导页等）互斥；在显示其他窗口之前必须先 dismiss HistoryPanel"
      ],
      "acceptance": [
        "TextEdit 处于前台时，⌥⌘V 显示面板；整个过程中 TextEdit 保持前台（可在 Dock / Mission Control 中验证）。",
        "把 Safari 切到全屏后按 ⌥⌘V → 面板浮现在全屏 Safari 之上（验证 .fullScreenAuxiliary）。",
        "切换到其他 Space 后再按 ⌥⌘V → 面板出现在当前 Space（验证 .canJoinAllSpaces）。",
        "把鼠标移到副屏后按 ⌥⌘V → 面板出现在副屏。",
        "面板可接收键盘输入（搜索框能打字），但不会在 Dock 中激活本 App。",
        "关闭面板后 TextEdit 仍保持焦点，可直接接收手动的 Cmd+V。"
      ],
      "commit_message": "S4: global hotkey + always-on-top non-activating panel"
    },
    {
      "id": "S5",
      "title": "选中条目回写剪贴板",
      "goal": "选中条目会把它的所有 payload 写回系统剪贴板并关闭面板；用户在任意 App 中手动 Cmd+V 粘贴。",
      "deliverables": [
        "PasteboardWriter 实现：清空 NSPasteboard.general，再按每个 payload 的原始 pasteboardType 声明并写入",
        "HistoryPanel 的选中处理：↵ 或点击某行 → PasteboardWriter.write → 关闭面板；不操纵焦点、不合成按键",
        "结构化日志事件 paste.write{itemId,types,sizeBytes,sensitive}",
        "使用 RecordingPasteboardWriter 的集成测试：覆盖所有 payload 类型是否被写入、选中是否关闭面板、sensitive 条目也能被正常写回"
      ],
      "acceptance": [
        "TextEdit 处于前台时，⌥⌘V → 选中一条 → 面板关闭，TextEdit 仍在前台，按 Cmd+V 可粘贴内容。",
        "选中 RTF 条目后，在 Pages 中粘贴为富文本、在 Terminal 中粘贴为纯文本（同一次剪贴板状态，接收方自行挑选）——确认原始所有类型都被写入。",
        "`just logs` 在每次选中时都能看到一条 paste.write 事件。"
      ],
      "commit_message": "S5: restore selected item to system clipboard on selection"
    },
    {
      "id": "S6",
      "title": "富类型：RTF、图片、文件",
      "goal": "非纯文本剪贴内容的往返完整性。",
      "deliverables": [
        "扩展 RawClipItem 抽取：public.rtf、public.tiff/public.png、public.file-url",
        "行视图：图片行显示缩略图（接入 S3 预埋的 LRU 缓存，异步在 store_queue 生成），文件行显示图标 + 文件名",
        "预览面板（面板右侧）按 kind 渲染完整内容",
        "场景脚本：按剧本生成多种拷贝类型并端到端校验",
        "可选：↵ 按住 ⇧ 时以纯文本粘贴（只写 public.utf8-plain-text 到剪贴板）——实现时需与 S9 的快捷键集中规划一致"
      ],
      "acceptance": [
        "从 Safari 复制带样式文本 → 在 Pages 中粘贴保留格式。",
        "从 Finder 复制 PNG → 在 Preview 中粘贴能打开正确图片。",
        "`just scenario rich-types` 通过。"
      ],
      "commit_message": "S6: rich clipboard types (rtf / image / file)"
    },
    {
      "id": "S7",
      "title": "隐私（敏感遮罩）与置顶",
      "goal": "敏感内容进入 store 但被遮罩显示、不落盘；用户可以把条目置顶。",
      "deliverables": [
        "SensitivityFilter 真实实现：识别 org.nspasteboard.ConcealedType → .markSensitive('concealedType')",
        "BlocklistFilter 真实实现：ClipContext.sourceBundleID 命中 → .markSensitive('blockedSource:<bundleID>')",
        "ClipStore 的敏感写盘排除逻辑接入（S2 已预埋，此处验证行为）",
        "UI 遮罩：sensitive 行显示『●●●●（来自 <App>）』+ 锁图标；预览面板遮罩；提供『临时显示敏感内容』按钮（仅当次会话有效）",
        "行右键菜单的置顶开关 + 专属的『已置顶』标签页",
        "隐私设置 UI（skipSensitive 开关、blocklistEnabled 开关 + blockedBundleIDs 列表编辑——开关关闭时列表只读显示）"
      ],
      "acceptance": [
        "从 1Password 拷贝密码 → HistoryPanel 中出现一行『●●●●（来自 1Password）』，选中后可正常粘贴密码到目标 App。",
        "重启 App → 上面这条敏感记录消失；非敏感记录保留。",
        "检查 history.json → 不含该敏感条目；检查 blobs/ → 不含该敏感条目的任何文件。",
        "清空所有类型的操作不会清掉已置顶项。"
      ],
      "commit_message": "S7: sensitive masking (in-memory only) and pinned items"
    },
    {
      "id": "S8",
      "title": "偏好设置",
      "goal": "暴露面向用户的设置界面。",
      "deliverables": [
        "偏好设置窗口：快捷键、保留策略（cap 滑块）、maxClipSizeBytes、skipSensitive、blocklistEnabled + blockedBundleIDs、开机启动（ServiceManagement）、外观（跟随系统/浅色/深色）、语言覆盖",
        "打开偏好设置前先 dismiss HistoryPanel（遵守 S4 的窗口互斥约束）",
        "带确认的『清空历史』操作",
        "JSON 导出 / 导入：导出把 history.json（已排除 sensitive）+ blobs/ 打包为 .zip；导入按 sha256 合并去重，缺失 blob 的条目跳过并在导入结果里计数",
        "Dark Mode 校验：所有 UI 在深色模式下显示正常",
        "显式的本地化键集合与 zh-Hans/en 两份 Localizable.strings"
      ],
      "acceptance": [
        "所有设置在重启后保留。",
        "⌥⌘V 打开 HistoryPanel 后，从菜单栏选择『偏好设置…』→ HistoryPanel 立即关闭，偏好设置窗口正常显示且可交互。",
        "导出 → reset 到空 → 导入 → 历史（非敏感部分）完整恢复；故意删掉一个 blob 再导入 → 该条目被跳过并在日志里计数。",
        "深色模式下面板与偏好设置窗口显示正常，无对比度问题。"
      ],
      "commit_message": "S8: preferences, launch-at-login, export/import, dark mode"
    },
    {
      "id": "S9",
      "title": "UX 打磨与日常可用",
      "goal": "补齐首启引导、反馈、快捷键、菜单栏入口、异常态通知，让普通用户能零配置日常使用。",
      "deliverables": [
        "首启引导（仅首次启动）：介绍默认快捷键 ⌥⌘V、告知工作在菜单栏、一键打开偏好设置",
        "快捷键冲突检测：HotkeyService 绑定失败时通过 bindingStatus 发 .conflict，UI 弹引导页让用户重绑（解决原 open_question 中 ⌥⌘V 可能冲突的问题）",
        "菜单栏图标右键菜单：显示历史面板、偏好设置…、暂停监听、清空历史…、关于、退出",
        "『暂停监听』开关：开启时 Monitor 暂停轮询（输密码 / 调试场景），菜单栏图标显示暂停态",
        "SizeFilter 拒绝的 UI 反馈：菜单栏图标短暂角标 + 可选的 toast『已跳过大于 10 MiB 的条目』，避免用户怀疑 App 坏掉",
        "选中写剪贴板后的确认反馈：菜单栏图标短暂闪烁 + 可选的轻 toast『已复制，按 Cmd+V 粘贴』",
        "history.json 损坏恢复后的用户可见通知：『历史已恢复到备份，原文件保存在 history.json.bak』",
        "搜索 UI 升级：kind 过滤 chips（全部 / 文本 / 图片 / 文件 / 已置顶）",
        "快捷键补齐：⌘F 聚焦搜索、⌘1–9 快速选中前 N 条、⌘⌫ 删除当前选中、⌘P 置顶当前选中、Tab 切换『全部/已置顶』",
        "Accessibility：VoiceOver label、Dynamic Type 基本支持、键盘完全可达"
      ],
      "acceptance": [
        "全新安装后首次启动 → 看到引导页并能成功完成默认快捷键绑定。",
        "将系统级的 ⌥⌘V 占用（例如绑给别的 App）→ 本 App 启动时提示冲突并引导重绑。",
        "在菜单栏图标右键菜单中能看到并成功触发：暂停监听、清空历史（带确认）、偏好设置、退出。",
        "拷贝一个 > 10 MiB 的文件 → 菜单栏图标出现短暂反馈，用户能知晓条目被跳过。",
        "选中一条历史并回写剪贴板 → 菜单栏图标出现短暂反馈。",
        "手动破坏 history.json 后重启 → 用户看到『历史已恢复到备份』的通知，而不是静默清空。",
        "在面板中按 ⌘F 聚焦搜索、⌘1 选第一条、⌘⌫ 删当前选中、⌘P 置顶当前选中——均按预期工作。",
        "打开 VoiceOver 后，面板的每一行都有可读的描述。"
      ],
      "commit_message": "S9: UX polish (onboarding, menu, hotkey conflict, feedback, accessibility)"
    },
    {
      "id": "S10",
      "title": "日常使用回归修复",
      "goal": "修复实测中发现的 5 个交互/行为问题：(1) 跨 App 相同内容被当作重复入库，(2) 偏好设置里无法修改 Hotkey，(3) 打开面板时选中项不归位到首行，(4) 关闭面板不会把选中项写回剪贴板 + 移动到队首，(5) 点击面板外部不会自动关闭面板。",
      "deliverables": [
        "Hashing.sha256 引入规范化：按优先级（image > rtf > text > html > file-url）选首个主 payload 参与哈希，而非整组 payload——跨 App 复制同一内容不再因 UTI 元数据差异产生重复条目。原 all-payload 路径作为兜底保留，仅当没有任何主类型命中时才启用。",
        "HistoryPanelViewModel 新增 resetSelection()；AppDelegate.togglePanel 在调用 panel.toggle 之前，于 open 分支调用 vm.resetSelection() 确保首行高亮；配合 filteredItems（受 tab 影响）而非 items。",
        "HistoryPanel 新增 willCloseCommit 钩子（由 AppDelegate 注入）+ suppressNextCloseCommit 标志；override 其 close() / orderOut(_:) 在关闭流经过时若未被 suppress 则调用钩子；钩子读取 viewModel.selectedID 对应的 ClipItem 并调用 wiring.activate；显式 ↵ 路径先置 suppress=true 再调 activate+orderOut，避免双写。",
        "HistoryPanel 在 makeKeyAndOrderFront 之后通过 NSEvent.addGlobalMonitorForEvents([.leftMouseDown, .rightMouseDown]) 注册外部点击监听，回调直接调 self.close；orderOut 或 close 时移除监听（NSEvent.removeMonitor）；监听器引用必须弱化避免自循环。",
        "AppDelegate.openPreferences 在 show 之前 NSApp.setActivationPolicy(.regular)；PreferencesWindowController 通过 NotificationCenter 订阅自身 window 的 windowWillClose 通知，delegate 回调在收到时切回 .accessory。同时 LSUIElement=true 在 Info.plist 保持不变——activationPolicy 的运行时切换是合法互补手段。",
        "测试：HashingTests × 2——canonicalization 在 text 同内容 + 不同 UTI 元数据下产生相同 hash；image 优先级高于 text；回归 JSONSnapshotClipStoreTests 中的 dedup 用例仍然通过。"
      ],
      "acceptance": [
        "从 TextEdit 复制『hello』→ 从 Safari 复制『hello』→ history.json 只留一条，createdAt 为最近一次，sourceBundleID 为 TextEdit（first-write-wins）。",
        "菜单栏『Preferences…』打开 → General 的 Recorder 处按下新快捷键（如 ⌃⌥⌘C）→ 录制成功并在关闭偏好后立即生效；再按 ⌥⌘V 无反应、按新快捷键召唤面板。",
        "⌥⌘V 打开面板 → 列表非空时首行立即高亮；可直接按 ↵。",
        "⌥⌘V 打开面板 → 鼠标移到其他 App 的窗口并左/右键点击 → 面板立即关闭。",
        "⌥⌘V 打开面板 → ↑↓ 选第 3 条 → 按 Esc（或点击外部）→ 第 3 条被写回剪贴板且在 history 中成为队首；未按 ↵ 也完成了『选择并粘贴』语义。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S10: dedup canonicalization, hotkey editing, open-selects-first, close-commits, outside-click-closes"
    },
    {
      "id": "S11",
      "title": "交互回归修复（点击 / 菜单 / 滚动）",
      "goal": "修复 S10 之后冒出的 3 个交互问题：(1) 点击历史条目没反应；(2) 菜单栏右键菜单不弹出；(3) 打开 HistoryPanel 时列表没自动滚回首行（若之前滚动过）。",
      "deliverables": [
        "ClipRowView 把 onTapGesture 从 count:2 改为 count:1，单击即激活（写回剪贴板 + 关闭面板）——与 Raycast/Alfred 风格对齐；用户仍可通过 ↑↓ 纯键盘导航。",
        "AppDelegate.showStatusMenu 改为直接调用 NSMenu.popUp(positioning:at:in:)，丢弃原来的 statusItem.menu= + performClick 路径——后者在 action 内部设置 menu 后再发 click 既丢事件又会递归进同一 action。菜单每次构建一次性 popUp，结束后不挂在 statusItem 上。",
        "HistoryPanelView 用 ScrollViewReader 包裹 List；HistoryPanelViewModel 新增 scrollEpoch: Int，resetSelection() 时 +1；View 的 .onChange(of: scrollEpoch) 回调里 proxy.scrollTo(firstID, anchor: .top)。保证每次面板打开都滚动到首行，无论面板之前被滚到哪里。",
        "回归确认：S10 的 close-commits / outside-click / dedup / hotkey / open-selects-first 不受影响。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 鼠标点击列表里任意一行 → 面板关闭，该行内容已写入剪贴板。",
        "菜单栏图标右键 → 菜单正确弹出，包含 Show History / Preferences… / Pause Monitoring / Clear History… / About / Quit；左键仍然 toggle 面板。",
        "⌥⌘V 打开面板 → ↑↓ 滚到中间某行 → Esc 关闭 → 复制新内容 → 再开面板 → 列表滚到顶端，首行高亮。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S11: row single-click activates, menu bar popUp, panel scrolls to top on open"
    },
    {
      "id": "S12",
      "title": "交互回归修复（菜单 / 键盘 / 立刻置顶）",
      "goal": "修复 S11 之后仍存在的 3 个交互问题：(1) 菜单栏右键仍然不弹出菜单；(2) 切换 All/Pinned 标签后 ↑↓ 不再切条目；(3) 被激活的条目没有立即挪到列表首位（依赖 300ms 轮询后才到位）。",
      "deliverables": [
        "AppDelegate.showStatusMenu 改为 NSMenu.popUpContextMenu(menu, with: event, for: button)——把当前右键事件直接交给 AppKit，菜单原地弹出；S11 里手算坐标调 popUp(positioning:at:in:) 在 y-up 坐标里会把菜单顶到屏幕外，这才是『不弹』的真因。",
        "HistoryPanelView 监听 viewModel.currentTab 的 .onChange，切换时调 viewModel.resetSelection()——新 filteredItems 的首行立刻高亮，列表滚到顶，键盘 ↑↓ 立即可用。",
        "ClipStore 协议新增 bumpToTop(id: UUID) async：从数组中找出条目、更新 createdAt、移到队首、发出 .updated 事件、触发防抖写盘。JSONSnapshotClipStore 与 InMemoryClipStore 都实现该方法。",
        "AppWiring.activate 在写剪贴板后调 store.bumpToTop(id:)——即时把条目移到首位，不依赖 Monitor 的 300ms 轮询（Monitor 稍后见到剪贴板改变也会再 bump 一次，但因 sha256 已存在会无副作用命中 dedup bump 逻辑）。",
        "测试：ClipStore bumpToTop 把指定 id 的条目移到数组首位且更新 createdAt；bumpToTop 一个不存在的 id 是 no-op；bumpToTop 不会破坏 pinned 项或 sensitive 持久化语义。"
      ],
      "acceptance": [
        "菜单栏图标右键 → 菜单**立即**弹出在按钮下方，包含全部菜单项；左键仍 toggle 面板。",
        "⌥⌘V 打开面板 → Tab 从 All 切到 Pinned → ↑↓ 能在 Pinned 列表里切条目；再切回 All → ↑↓ 也能正常切。",
        "⌥⌘V 打开面板 → 选中非首行 → ↵（或单击、或 Esc 关闭）→ 被激活的条目**立即**成为列表首行；再次打开面板看到的就是新顺序。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S12: menu popUpContextMenu, tab reset, immediate bumpToTop"
    },
    {
      "id": "S13",
      "title": "菜单栏点击统一化 + 面板内溢出菜单",
      "goal": "Mac 触控板的点按事件几乎总是 .leftMouseDown，S10–S12 的『左键 toggle / 右键弹菜单』分流在真实设备上不可靠；改为：菜单栏单击统一 toggle 面板，Preferences / Pause / Clear / Quit 等放进面板右上角的溢出菜单。",
      "deliverables": [
        "AppDelegate 去掉 button.sendAction(on: [.leftMouseDown, .rightMouseDown]) 和 statusItemClicked(_:) 里的事件类型分流；action 直接是 togglePanel——任何点击（鼠标 / 触控板 / Control+click / 二指）行为一致。",
        "HistoryPanelView 顶部加一个溢出按钮（ellipsis.circle 图标，accessibilityLabel『More actions』）放在搜索框右侧；按钮点击时 onShowMenu 回调里 AppKit NSMenu.popUpContextMenu 弹出与原右键菜单相同的项（不含『Show History』）。",
        "弹菜单前 HistoryPanel.suppressNextCloseCommit = true，防止菜单打开引发的焦点切换让 panel 的 close-commit 误写剪贴板。",
        "AppDelegate 移除 showStatusMenu 旧实现（现在不再用），保留 buildMenu() 以便溢出按钮复用。",
        "回归：S10–S12 的 close-commits、outside-click、bumpToTop、tab reset、single-click activate 不受影响。"
      ],
      "acceptance": [
        "用触控板单指点按菜单栏图标 → HistoryPanel 切换显示 / 隐藏；多次重复行为一致，不出现『打开菜单』的意外路径。",
        "HistoryPanel 打开后，点击右上角 ellipsis.circle 按钮 → 菜单弹出，包含 Preferences… / Pause Monitoring / Clear History… / About / Quit。",
        "从溢出菜单选『Preferences…』后 → 偏好窗口打开；HistoryPanel 依据 S4 互斥规则关闭；**剪贴板内容没有被任何条目覆盖**（即 close-commit 被 suppress 成功）。",
        "⌥⌘V 打开面板 / Esc 关闭 / 单击条目激活 / All↔Pinned 切换的键盘行为全部保持 S10–S12 的语义。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S13: unify menu bar click, move submenu into panel overflow"
    },
    {
      "id": "S14",
      "title": "去菜单栏图标 + 面板内入口精简 + 键盘全导航",
      "goal": "按用户指示重塑交互：不放菜单栏图标；不在面板顶部放『⋯』按钮；面板内 ⌘, 直达 Preferences；面板里任何地方按 ↑↓ 都切条目（不进搜索框光标）。",
      "deliverables": [
        "AppDelegate.applicationDidFinishLaunching 去掉 NSStatusItem 的全部创建逻辑；相关字段 statusItem、pauseMenuItem 也一并删除；HistoryPanel.toggle 的 anchoredTo: statusItem 参数改为可选，为 nil 时面板居中到光标所在屏幕的顶部中央。",
        "新增 applicationShouldHandleReopen(_:hasVisibleWindows:) 回退路径：用户在 Finder/Dock/Spotlight 再次启动时打开 Preferences——确保没有菜单栏图标的情况下仍有到达 Preferences 的回退。",
        "HistoryPanelView 去掉 OverflowMenuButton 的引用；onShowOverflowMenu 回调移除；OverflowMenuButton.swift 可以删除（或保留作为以后可能用到的原语）。",
        "HistoryPanelView 的隐藏快捷键 ZStack 新增一个 .keyboardShortcut(',', modifiers: .command) 按钮，调 onShowPreferences 回调；AppDelegate 把回调接到 openPreferences（其内部已按 S4 互斥把 panel 关掉）。",
        "HistoryPanelViewModel 新增 selectNext() / selectPrevious()——在 filteredItems 里顺/逆序移动 selectedID，空列表或已在首/尾的情况采用卷绕（wrap-around）语义：selectNext 的末尾下一步 = 末尾；selectPrevious 的首位上一步 = 首位——不卷绕，避免视觉跳动。",
        "HistoryPanel 在 makeKeyAndOrderFront 后、startOutsideClickMonitor 同一时机调 startKeyMonitor；通过 NSEvent.addLocalMonitorForEvents(.keyDown) 拦截 keyCode 125 / 126 调 onArrowDown / onArrowUp 回调并返回 nil（吞掉事件，不再走响应链到搜索框）；其他 keyCode 直接返回原事件。关闭时 NSEvent.removeMonitor 拆除。",
        "AppDelegate 把 onArrowDown / onArrowUp 接到 viewModel.selectNext / selectPrevious。",
        "Preferences 依旧保留 Pause Monitoring / Clear History 的入口；不再需要菜单栏菜单。buildMenu / showOverflowMenu / pauseMenuItem 相关死代码全部清理。",
        "测试：HistoryPanelViewModel.selectNext / selectPrevious 在 (空 / 1 条 / 多条) 列表、(已选中首 / 中 / 尾)、tab 切换后的选中漂移等场景下行为符合。"
      ],
      "acceptance": [
        "⌘Q 关闭 App 后再次从 Finder 双击 Clipboard.app → Preferences 窗口直接打开。",
        "系统菜单栏不再出现 doc.on.clipboard 图标。",
        "⌥⌘V 打开面板 → 光标焦点在搜索框 → 不点任何地方直接按 ↓ 键 → 列表第二条变为选中；继续 ↑ → 回到首行；全过程搜索框文本无光标移动干扰。",
        "⌥⌘V 打开面板 → 按 ⌘, → 面板关闭、Preferences 窗口打开；剪贴板内容没有被任何条目覆盖（close-commit 被 suppress）。",
        "HistoryPanel 顶部不再出现 ⋯ 按钮。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S14: no menu bar icon, remove panel overflow, Cmd+, for prefs, arrow keys own selection"
    },
    {
      "id": "S15",
      "title": "外部点击判定修复（标题栏不应关窗）",
      "goal": "修复『点击 Clipboard 面板标题栏，窗口自己消失』：`.nonactivatingPanel` 下本 App 从未 activate，AppKit 给 global monitor 派发事件时把自家 chrome 区的点击也算作『发给其他 App』，导致 outsideClickMonitor 把标题栏点击误判为外部点击。",
      "deliverables": [
        "HistoryPanel.startOutsideClickMonitor 的 .leftMouseDown/.rightMouseDown 回调里新增矩形判定：用 NSEvent.mouseLocation 取屏幕坐标，仅当点击落在 self.frame 之外时才调 close()；在 frame 内一律放行。这样标题栏 / 关闭按钮 / resize 角 / 面板正文点击都不会触发关闭。",
        "同一回调也兼顾点击了其他 App（frame 外 → close）和点击了本 App 的其他窗口（Preferences 等，frame 外 → close），覆盖所有『外部』语义，无需再加 local monitor。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 点击面板标题栏 → 面板保持打开。",
        "⌥⌘V 打开面板 → 点击其他 App 的任意位置 → 面板关闭。",
        "⌥⌘V 打开面板 → 点击桌面空白 → 面板关闭。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S15: frame-based outside-click detection (title bar stays clickable)"
    },
    {
      "id": "S16",
      "title": "Pin 原子化 + 预览窗口用全分辨率图片",
      "goal": "修复 3 个实测问题：(1) ⌘P 要按至少两次才出现钉子图标；(2) 预览窗口里的图片不是原图；(3) 预览窗口里的图片模糊。",
      "deliverables": [
        "ClipStore 协议新增 togglePin(id:) async——在 actor 内部原子翻转 pinned 状态，避免调用方用过期的 item.pinned 判断。JSONSnapshotClipStore 与 InMemoryClipStore 都实现。",
        "HistoryPanelViewModel.togglePin(_:) 改为调 store.togglePin(id:) ——不再依赖传入的 item.pinned；第一次 ⌘P 立即生效。",
        "ClipPreviewView.imageView 不再调 ThumbnailLoader——缩略图是给行视图（36×36）预渲染的 64×64 LRU 缓存，把它塞进预览窗口会被拉伸、像素化且可能是错的 payload。改为通过 PayloadResolver 从 inlineData / blobPath 加载**原始图片数据**构造 NSImage，再用 SwiftUI Image.resizable().interpolation(.high).aspectRatio(.fit) 渲染。异步在 DispatchQueue.global() 读取 → 主线程回写 @State。",
        "imageView 的选型与行视图的缩略图解耦：行继续用 ThumbnailLoader（小、快、缓存友好），预览窗口用全分辨率（因为用户期望『在预览里看得清楚』）。",
        "测试：ClipStore togglePin 翻转单条状态；不存在的 id 是 no-op；sensitive 条目的 pinned 翻转不触发磁盘写（保持 S2 的不落盘不变量）。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 选中一条非 pinned 条目 → 按 ⌘P → 立即出现钉子图标（1 次按压）；再按 ⌘P → 钉子消失。",
        "⌥⌘V 打开面板 → 选中一张 PNG/TIFF 条目 → 预览窗口显示**与该条目实际对应**的图片，不是别的条目的缩略图。",
        "同上，预览窗口里的图片清晰（按原分辨率渲染再按 aspectRatio .fit 缩放），而非 64×64 模糊拉伸。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S16: atomic togglePin, preview pane loads full image"
    },
    {
      "id": "S17",
      "title": "单实例强制",
      "goal": "同一时刻只允许一个 Clipboard 进程——避免两份 Monitor 并发读 pasteboard、两份 store 竞争 history.json / blobs 目录的数据损坏风险。",
      "deliverables": [
        "main.swift 在 NSApplication.shared 被访问之前：用 NSRunningApplication.runningApplications(withBundleIdentifier:) 过滤 ProcessInfo.processInfo.processIdentifier 自身，若仍有实例，则调用其 activate(options: [.activateAllWindows])、经 DistributedNotificationCenter 广播 `com.clipboard.app.reopen`，然后 exit(0)。",
        "环境变量 CLIPBOARD_ALLOW_MULTIPLE=1 下跳过检查——开发时 Xcode Run 不会被既有实例阻挡。",
        "AppDelegate.applicationDidFinishLaunching 订阅 DistributedNotificationCenter 的 `com.clipboard.app.reopen` 通知，回调里打开 Preferences（与 applicationShouldHandleReopen 的行为一致）。",
        "AppDelegate deinit 或 applicationWillTerminate 时调 DistributedNotificationCenter.default.removeObserver 清理。",
        "Bundle Identifier 从 Info.plist 读取，避免硬编码漂移；缺省回退到 'com.clipboard.app'。"
      ],
      "acceptance": [
        "App 已在运行 → 在 Finder 中双击 Clipboard.app → 不会启动新进程；既有实例的 Preferences 窗口打开。",
        "App 已在运行 → 命令行 `open /path/to/Clipboard.app` → 同上，不会多出一个进程（用 `pgrep -x Clipboard` 验证只有一个 pid）。",
        "命令行 `open -n /path/to/Clipboard.app`（强制新实例）→ 新启动的进程检测到既有实例后自动退出；原实例的 Preferences 打开。",
        "设置 `CLIPBOARD_ALLOW_MULTIPLE=1 open /path/to/Clipboard.app` → 能启动第二个实例（开发调试用）。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S17: enforce single-instance via NSRunningApplication + distributed notification"
    },
    {
      "id": "S18",
      "title": "乐观 pin 更新 + 预览图后台预解码",
      "goal": "修两个感知延迟问题：(1) ⌘P 的图标反馈经由 store 往返，几十毫秒才可见，用户感到『按了没反应』；(2) 预览窗口显示图片前肉眼可感的 loading 态，哪怕源是小截图也会出现——因为 NSImage(data:) 是 lazy，真正的解码发生在主线程首次渲染时。",
      "deliverables": [
        "HistoryPanelViewModel.togglePin 先做乐观本地翻转：items[idx].pinned.toggle() 在主线程立即生效 → SwiftUI 立即重绘钉子图标；随后才 await store.togglePin(id:)——store 事件回来时会把真值再次写入 items[idx]，但因内容一致不会产生可见闪动。",
        "ClipPreviewView.loadImage 用 CGImageSourceCreateThumbnailAtIndex（配合 kCGImageSourceShouldCacheImmediately=true、kCGImageSourceThumbnailMaxPixelSize=1024、kCGImageSourceCreateThumbnailFromImageAlways=true）在后台线程把原数据直接解码到 CGImage，再包成 NSImage(cgImage:size:)。这样返回到主线程时 SwiftUI 拿到的是预渲染的 bitmap，没有 lazy-decode 延迟。",
        "maxPixelSize=1024 足以覆盖 260pt 预览栏 × 2x retina；更大的截图会被 ImageIO 按比例降采样，进一步缩短解码时间且不损害显示效果。",
        "保留已有的 loadedImageForID 守护：若用户在后台解码期间切换选中，过期结果直接丢弃。",
        "测试：新增 CGImage 缩略图辅助函数在 Core 层可选提供的对应测试；pin 的乐观更新语义由既有 TogglePinTests 回归兜底。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 选中一条非 pinned 条目 → 按 ⌘P → **钉子图标在下一帧即可见**（肉眼无延迟），无需等待磁盘写盘。",
        "连续快速按 ⌘P 若干次 → 最终状态与按压次数的奇偶一致，无中间闪动残留。",
        "复制一张 macOS 截图（Cmd+Shift+4 框选）→ 打开面板选中它 → 预览窗口几乎瞬间显示清晰的原图，不见明显 ProgressView。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S18: optimistic togglePin, preview pane pre-decodes via ImageIO"
    },
    {
      "id": "S19",
      "title": "开机启动（Launch at Login）",
      "goal": "把 S8 延后的『开机启动』补上——剪贴板 App 是 24/7 驻留工具，用户每次重启都要记得手动起动是最高频的卡点。",
      "deliverables": [
        "Preferences 结构新增 launchAtLogin: Bool（默认 false）；PreferencesStore.current / save 同步读写 UserDefaults 的 launchAtLogin 键。",
        "新文件 LoginItemController（App 层）：封装 SMAppService.mainApp 的 register() / unregister() / status 查询；status 映射为 enabled / disabled / notRegistered / requiresApproval 等状态。",
        "PreferencesView.general 追加 『Start at Login』 Toggle，绑定到 prefs.launchAtLogin；onSave 回调里除了持久化到 UserDefaults，还调 LoginItemController.apply(_:) 把状态推给系统。",
        "失败路径：SMAppService 在未签名 / 未放入 /Applications / 用户拒绝时会抛错或返回 notFound。apply 失败时用 NSAlert 提示用户（『需要把 Clipboard 移到 /Applications 后再启用』），并把 Toggle 状态回滚到持久化前的值，避免 UI 与系统实际状态偏离。",
        "初次打开 Preferences 时 LoginItemController 重新查询系统状态并把真值反馈到 View，避免用户在外部（系统设置 → 登录项）禁用后 UI 还显示为开启。",
        "`guardrails.must_not` 无需新增——SMAppService 是公开 API 且不依赖外部二进制。`non_goals` 的『不上 MAS』仍保持。"
      ],
      "acceptance": [
        "Preferences 的 General 页出现『Start at Login』开关。",
        "开启开关 → 重启 macOS → 登录后 Clipboard 自动在后台启动（可通过 `pgrep -x Clipboard` 验证）。",
        "在『系统设置 → 通用 → 登录项』里看到 Clipboard 列在『允许在后台启动』条目内。",
        "关闭开关 → 重启 → 登录后 Clipboard 不自动启动，且系统设置里移除。",
        "Debug 构建下（未签名 / 未在 /Applications）开启开关 → 弹出 NSAlert 说明限制，Toggle 回滚到 false，`launchAtLogin` 不被错误持久化为 true。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S19: launch at login via SMAppService"
    },
    {
      "id": "S20",
      "title": "Kind 过滤 chips（All / Text / Image / File）",
      "goal": "补上 S9 延后的『kind 过滤 chips』——扩容后用户希望按类型快速聚焦（比如只看 image / 只看 file），而不是滚长列表。",
      "deliverables": [
        "HistoryPanelViewModel 新增 @Published var kindFilter: ClipKind?（nil = 不过滤）；filteredItems 在 tab 过滤之前先按 kindFilter 过滤。",
        "HistoryPanelView 在 tabBar 下方加一行横向可滚动的 chip 条：All（nil）/ Text / Image / File；当前选中的 chip 填充 accentColor、其他填淡灰；再次点选中的 chip 会取消（回到 nil）。",
        "resetSelection() 同时清 kindFilter ——保持『每次打开面板都是一次干净的会话』语义（与 S10 ui_interactions.panel_open_resets_selection 一致）。",
        "HistoryPanelView .onChange(of: kindFilter) 调 viewModel.resetSelection()，确保选中/滚动跟上新的 filteredItems。",
        "chip 上显示 kind 图标 + 文字；字号 .caption 保持 panel 顶部紧凑。",
        "测试：视图层行为由 live run 覆盖；ViewModel 层的 filteredItems 组合逻辑（kindFilter × currentTab）在新增一条单测里走一遍 (nil × .all) / (.text × .all) / (.image × .pinned) 等组合。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 点击 Image chip → 列表只剩 image kind 的条目；再点 Image chip → 恢复显示全部。",
        "点 Image chip → 切换到 Pinned tab → 列表显示 image kind 且 pinned=true 的条目；切回 All → 列表显示 image kind 的全部条目。",
        "Esc 关闭面板 → ⌥⌘V 再次打开 → kindFilter 已重置到 nil（All chip 选中），首行高亮。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S20: kind filter chips in history panel"
    },
    {
      "id": "S21",
      "title": "SizeFilter 拒绝反馈（面板内提示条）",
      "goal": "补上 S9 延后的『SizeFilter 拒绝反馈』。无菜单栏图标后，用户拷贝 >10 MiB 的文件/图片时不再有菜单栏角标可显示；改为在打开面板时把『最近一次被跳过的条目』显示在面板顶部的可关闭提示条中，告知原因 + 指向 Preferences 调大上限。",
      "deliverables": [
        "ClipboardCore 新增 SkipEvent 值类型：reason / bytes / types / bundleID / timestamp；ClipboardMonitoring 协议新增 `var skips: AsyncStream<SkipEvent> { get }`——与现有 changes 并列；FakeClipboardMonitor 增 `push(skip:)` 测试钩子。",
        "NSPasteboardMonitor 在 `.reject` 分支里除了结构化日志 monitor.skipped 还 yield 一个 SkipEvent 到 skips 流；existing tests 不受影响。",
        "AppWiring 增 skipTask：订阅 monitor.skips 并写到 HistoryPanelViewModel.lastSkip（@Published）。",
        "HistoryPanelViewModel.lastSkip: SkipEvent? 暴露给 View；resetSelection 不清除（跨会话仍要提醒用户，直到用户显式 dismiss 或事件自然过期）；新增 clearLastSkip() 供 View 的 X 按钮调用；新增 shouldShowLastSkip 判定：非 nil 且 timestamp 距现在 < 60s。",
        "HistoryPanelView 在 tabBar 与 list 之间插入 SkipBannerView（新文件）——黄色底、exclamationmark.triangle 图标、短句『已跳过 <N.N MB> 条目（源自 <bundleID>）。可在偏好设置里调大上限。』 + X 按钮。lastSkip nil 或过期则不渲染。",
        "新增单元测试：SkipEvent 值语义、InMemoryClipStore 无关；NSPasteboardMonitor 在 SizeFilter.reject 情况下把 SkipEvent 推到 skips 流。"
      ],
      "acceptance": [
        "在 Finder 右键选一个 >10 MiB 的文件→拷贝→按 ⌥⌘V 打开面板→顶部出现黄色提示条，写明跳过了多大的条目、来源 App 是哪个。",
        "点击提示条的 X → 消失，下次打开面板不再显示该条事件（除非又有新 skip）。",
        "61 秒内不做任何操作后再次打开面板→提示条自动不显示（超过窗口）。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S21: SizeFilter rejection banner in history panel"
    },
    {
      "id": "S22",
      "title": "ZIP 导出/导入（含 blobs）",
      "goal": "把 S8 延后的『JSON 导出 / 导入』补全——现版本只导出 history.json，漏掉 blobs/，导入后所有大图片引用都失效。改为整包 zip。",
      "deliverables": [
        "ClipStore 协议新增 importItems(_ items: [ClipItem], blobsRoot: URL?) async -> ImportResult，返回 (added, skipped, blobsMissing)；JSONSnapshotClipStore 与 InMemoryClipStore 都实现。",
        "BlobStore 新增 importBlob(from sourceURL: URL, sha256: String, ext: String) throws -> String——已存在则复用路径 + refcount+1；否则拷贝文件 + refcount=1。",
        "ImportResult 值类型（added/skipped/blobsMissing）放 Core 层。",
        "AppDelegate.exportHistory 改用 NSFileCoordinator.coordinate(readingItemAt:options:.forUploading,...) 把整个 Support 目录打包成 zip 拷贝到用户选择位置；导出文件扩展改为 .zip，Info.plist 的 savePanel.allowedContentTypes 改成 [.zip]。",
        "AppDelegate 新增 importHistory：NSOpenPanel 选 .zip → unzip 到临时目录（通过 Process /usr/bin/unzip）→ 读 history.json 反序列化为 [ClipItem] → 调 store.importItems，传入临时目录的 blobs/ 作为 blobsRoot → 展示结果 NSAlert（『导入 N 条，跳过 M 条重复，P 条因 blob 缺失跳过』）。",
        "PreferencesView.data 区块加 Import history… 按钮；Preferences 窗口把 onImportHistory 回调加到 PreferencesView 构造；PreferencesWindowController 接线。",
        "测试：BlobStore.importBlob 新文件/已存在 sha256 两种路径；ClipStore.importItems 在目标库已有 / 无该 sha256 时的去重逻辑；sensitive 条目无法通过 import 进入持久化（写盘过滤仍然生效）。"
      ],
      "acceptance": [
        "拷贝若干条（含 >16KB 图片）→ 打开 Preferences → Data tab → Export history… → 选择 .zip 路径 → 保存；解压 zip 能看到 history.json + blobs/ 完整结构。",
        "`just reset` 清空库 → Preferences → Data → Import history… → 选刚才的 zip → 提示『已导入 N 条』 → 打开面板能看到所有条目，图片缩略图可见（blob 已落地到当前库的 blobs/）。",
        "再次导入同一个 zip → 提示全部跳过（sha256 已存在）。",
        "故意从 zip 里删掉某个 blob 文件后导入 → 该条目的 blobsMissing+1，其它条目正常导入。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S22: full-fidelity zip export + import"
    },
    {
      "id": "S23",
      "title": "快捷键未绑定时的救援路径",
      "goal": "用户若在 Preferences 的 Recorder 里把快捷键清空（或初始安装时系统把默认 ⌥⌘V 被别家占用），再次启动时 ⌥⌘V 不工作、又没菜单栏图标，用户就卡住了。S14 的 applicationShouldHandleReopen → Preferences 已经是兜底；此处加一条启动即检测并主动引导。",
      "deliverables": [
        "AppWiring.startHotkey 后查 KeyboardShortcuts.getShortcut(for: .toggleHistoryPanel)——若 nil，不调 bind；通过新的 onHotkeyUnbound 回调告知 AppDelegate。",
        "AppDelegate 收到 onHotkeyUnbound 时：弹出 Preferences 窗口并聚焦到 General 页（Recorder 就在那里），并在窗口上方加一个 warning 提示『快捷键已清空；请重新录制或应用将无法通过键盘召唤面板』。",
        "PreferencesView.general 新增条件渲染的 warning banner：当 KeyboardShortcuts.getShortcut(for: .toggleHistoryPanel) == nil 时显示；一旦 Recorder 收到新绑定，banner 自动消失（SwiftUI 重绘时重新判定）。",
        "首启引导（Onboarding）在快捷键未绑定时也会额外显示一条『⌥⌘V 可能被其他 App 占用；请到 Preferences 改绑』。"
      ],
      "acceptance": [
        "在 Preferences 的 Recorder 里清掉快捷键 → 重启 App → Preferences 自动打开，顶部红黄色 warning 提示『无快捷键可用』；Recorder 绑定新快捷键后 warning 消失。",
        "保持已绑快捷键的情况下启动 App → Preferences 不自动弹出，按 ⌥⌘V 正常工作。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S23: detect and recover from cleared hotkey"
    },
    {
      "id": "S24",
      "title": "Scenario 脚本占位",
      "goal": "补上 Justfile 的 `scenario: swift run --package-path Core scenario-runner`——目前这条命令执行会报找不到可执行目标；补一个最小可执行 scenario-runner，用 InMemoryClipStore 跑几个预置情景并输出 JSON 报告。",
      "deliverables": [
        "Core/Package.swift 新增 `executableTarget(name: \"scenario-runner\", dependencies: [\"ClipboardCore\"])`。",
        "Core/Sources/scenario-runner/main.swift：从 CLI 参数读场景名（默认 rich-types），执行：插入 text/rtf/image/file/sensitive 各一条 → 校验 dedup、搜索、sensitive 不入持久快照、pin/bump 顺序；把结果以 JSON 输出到 stdout（{scenario, passed, details}）。",
        "场景 rich-types 至少覆盖：text+NSString 同内容进货只得 1 条；image payload 进库后按 image kind；sensitive 条目搜索可见但不在 history.json 的 in-memory 版本里不 applicable（InMemoryClipStore 就是纯内存）。",
        "Justfile 的 `scenario` 命令已存在，不改动。"
      ],
      "acceptance": [
        "`just scenario` 能成功执行并输出合规 JSON；退出码 0 表示全部通过、非 0 表示有失败。",
        "`just build` / `just test` / `just lint` 全绿（不新增测试，本身就是可运行的测试工具）。"
      ],
      "commit_message": "S24: add minimal scenario-runner executable"
    },
    {
      "id": "S25",
      "title": "注入 NSApp.mainMenu 以恢复标准编辑快捷键",
      "goal": "搜索框等 NSTextField 不响应 Cmd+A / Cmd+C / Cmd+V / Cmd+X / Cmd+Z / Cmd+Q——原因：LSUIElement 应用从未设置 NSApp.mainMenu，这些快捷键在 macOS 上依赖主菜单的 Edit 菜单项的 key equivalent 把 selectAll: / copy: / paste: 等 selector 沿响应链派发给 NSTextField；没有菜单就没有派发，TextField 再焦点也收不到。",
      "deliverables": [
        "AppDelegate.applicationDidFinishLaunching 在最前构建并安装 NSApp.mainMenu。菜单不可见（LSUIElement 本就不显示菜单栏），但 key equivalent 仍会被 AppKit 捕获并派发。",
        "App 菜单至少含 Hide (⌘H) / Hide Others (⌥⌘H) / Show All / Quit (⌘Q) —— 标准 Cocoa 约定。",
        "Edit 菜单至少含 Undo (⌘Z) / Redo (⇧⌘Z) / Cut (⌘X) / Copy (⌘C) / Paste (⌘V) / Select All (⌘A) ——这些 selector 会经响应链抵达当前 first responder（即搜索框对应的 NSTextView）。",
        "已有的 ⌥⌘V 全局快捷键（KeyboardShortcuts 库注册）与标准 ⌘V（Paste）不冲突——前者有 ⌥ 修饰、后者没有；事实上 Paste 的 ⌘V 在主菜单里存在也没关系，因为全局快捷键优先于窗口内菜单。",
        "已有的 HistoryPanelView 隐藏 keyboardShortcut 按钮（⌘,、⌘P、⌘1–9、⌘⌫ 等）与主菜单新增项不冲突——这些字符都未被主菜单占用。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板、搜索框获焦、输入『foo bar』→ ⌘A 全选、⌘C 复制、⌘V 再粘贴、⌘Z 撤销、⌘X 剪切均正常。",
        "在任意输入框按 ⌘Q → Clipboard 退出（通过 `pgrep -x Clipboard` 验证进程消失）。",
        "⌘H 隐藏应用（Preferences 窗口若开着会隐藏）。",
        "原有交互不退步：⌥⌘V 仍然召唤面板、⌘, 仍然打开 Preferences、⌘P 仍然置顶选中、面板外点击仍然关闭。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S25: install NSApp.mainMenu so standard Edit shortcuts route to NSTextField"
    },
    {
      "id": "S26",
      "title": "窗口居中 + Preferences 基于历史面板位置",
      "goal": "(1) HistoryPanel 和 Preferences 都未居中——前者是『顶部中央』（S14 改成了 visibleFrame.maxY - height - 40），后者用 NSWindow.center() 只居中在 NSScreen.main。(2) Preferences 应出现在和历史面板同一个屏幕上、居中，而不是漂在主屏上。",
      "deliverables": [
        "HistoryPanel.positionAtCursorScreen 改为真正的居中：origin.y = visibleFrame.midY - panelSize.height / 2（之前是靠上）。光标所在屏依旧是锚点屏。",
        "AppDelegate 新增 `lastPanelScreen: NSScreen?` 私有字段；HistoryPanel 关闭（close / orderOut）前通过回调把 `self.screen` 捕获回 AppDelegate——用作 Preferences 开启时的锚点屏回退。",
        "HistoryPanel 新增 `onBeforeClose: ((NSScreen?) -> Void)?` 回调，close()/orderOut(_:) 最开始调用（在 commitBeforeCloseIfNeeded 之前即可，只要能拿到 self.screen）。",
        "PreferencesWindowController.show 接受可选 `on: NSScreen?` 参数——传入则在该屏居中，否则维持原行为（center()）。",
        "AppDelegate.openPreferences 决定锚点屏：panel?.screen ?? lastPanelScreen ?? 光标所在屏 ?? main；传给 preferencesController.show。",
        "辅助函数 `screenContainingCursor() -> NSScreen?` 复用 positionAtCursorScreen 的逻辑——用 NSEvent.mouseLocation 在 NSScreen.screens 里找包含该点的屏。"
      ],
      "acceptance": [
        "⌥⌘V 在副屏召唤面板 → 面板居中在副屏（不是顶部、也不是主屏）；切回主屏再按 ⌥⌘V → 面板居中在主屏。",
        "面板在副屏打开 → 按 ⌘, 开 Preferences → Preferences 窗口居中在副屏（与面板同屏）。",
        "面板从未打开过、直接通过 `applicationShouldHandleReopen`（Finder 双击重启已运行 App）打开 Preferences → Preferences 居中在光标所在屏。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S26: center history panel + Preferences follows panel's screen"
    },
    {
      "id": "S27",
      "title": "Preferences 锚定到 History 面板的实际位置（而非仅屏）",
      "goal": "S26 只把 Preferences 定位到面板所在屏的屏中央，没参考面板的实际 frame——若用户把面板拖到角落，Prefs 仍然跑回屏中央，视觉上是『跳跃的』，不符合『Preferences 基于 History 位置』的期望。改为 Prefs 的中心与面板上次关闭前的 frame 中心重合（再 clamp 到该点所在屏的 visibleFrame 内防止越界）。",
      "deliverables": [
        "HistoryPanel.onBeforeClose 回调签名从 ((NSScreen?) -> Void) 改为 ((NSRect) -> Void)——传 self.frame 过去（frame 在 screen 置 nil 之前一直有效）。",
        "AppDelegate 的 lastPanelScreen: NSScreen? 字段替换为 lastPanelFrame: NSRect?；openPreferences 里锚点链变为 panel?.frame ?? lastPanelFrame ?? fallbackCenterRect()——fallback 用光标所在屏的屏中央构造一个伪 rect（让 Prefs 居中在光标屏）。",
        "PreferencesWindowController.show 的参数改为 `anchorRect: NSRect?`；内部用新助手 `positionWindow(_:centeredOn:)` 把 Prefs 的 frame 中心对齐 anchorRect 的中心，然后在该中心所在屏的 visibleFrame 上 clamp（边距 8pt），防止 Prefs 部分跑到屏外。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 面板居中（S26 行为）→ ⌘, 开 Prefs → Prefs 居中、与面板重合。",
        "⌥⌘V 打开面板 → 手动把面板拖到副屏右下角 → 关闭面板 → ⌘, 开 Prefs → Prefs 窗口中心与面板关闭前的中心一致，但被 clamp 在副屏 visibleFrame 内（不出屏）。",
        "面板从未打开过、用 Finder 双击 Clipboard.app 触发 applicationShouldHandleReopen → Prefs 居中在光标所在屏（fallback 分支）。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S27: Preferences centers on last History panel frame, not just its screen"
    },
    {
      "id": "S28",
      "title": "预览图载入的竞态修复 + Delete 键删除条目",
      "goal": "(1) 加载图片期间切换到另一条图片条目时，预览窗口有机会永远卡在 Loading——旧载入的完成回调拿过期 targetID 覆盖掉新载入的结果，且回调里的 stale-check 逻辑判反。(2) 面板里按 Delete（非 Cmd+Delete）不删条目——目前只绑了 ⌘⌫，用户自然按 ⌫ 无反应。",
      "deliverables": [
        "ClipPreviewView 新增 @State loadEpoch: Int；loadImage 每次调用都 loadEpoch += 1，后台闭包捕获当时的 epoch；主线程 DispatchQueue.main.async 回调里 `guard epoch == loadEpoch` 才应用结果，否则丢弃——这样无论旧载入多慢都不会覆盖新载入的正确结果，也不会让新载入被旧 guard 屏蔽。",
        "删除原来基于 loadedImageForID 的 stale-check（含义正好判反了，保留反而增加复杂度）；loadedImageForID 仍用于『这张图是否已加载过、不需要重启 ProgressView』的 UI 判定，语义不变。",
        "HistoryPanel 的 NSEvent.addLocalMonitorForEvents(.keyDown) 扩展为拦截 keyCode 51（⌫ / Delete）并调 onDelete 回调——但仅当当前 firstResponder 不是 NSText 时才吞事件；否则让事件走 AppKit 原本的路径（用户在搜索框里按 Delete 还是得能删字符）。",
        "HistoryPanel 新增 var onDelete: (() -> Void)?；AppDelegate 接线到 viewModel.delete(selectedItem)，与现有 ⌘⌫ 的 keyboardShortcut 按钮共用同一 delete 路径。",
        "HistoryPanelView 的隐藏 ⌘⌫ 按钮保留——功能等价、作为键盘用户的另一种触发方式。"
      ],
      "acceptance": [
        "选中一个大截图条目 → 预览窗口显示 Loading → 在 Loading 期间按 ↓ 切到另一张图 → 最终稳定显示第二张图（不是卡住的 Loading、也不是第一张图）。",
        "面板里选中一条（焦点不在搜索框）→ 按 Delete → 该条目从列表消失；搜索框依然有焦点时按 Delete → 正常删除搜索文本字符，不影响条目。",
        "回归：⌘⌫ 依然删除选中条目；⌘A / ⌘C / ⌘V / ⌘X / ⌘Z 仍然在搜索框内正常工作；方向键仍然控制选中；⌘, 仍然打开 Preferences。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S28: epoch-guarded preview loads + bare Delete key deletes the selected row"
    },
    {
      "id": "S29",
      "title": "撤销『⌫ 键删行』的功能",
      "goal": "S28 给 ⌫ 绑了『当前行是否被选中』驱动的删行动作。但 Alfred 风格的焦点模型里搜索框**永远**是 firstResponder——哪怕用户用 ↑↓ 在行间跳转，光标仍在搜索框闪烁。让同一个 ⌫ 键既管搜索字符又管行，要么吞错要么判错，体验就是坏的。干脆回到：⌫ 永远给搜索框用；删行走 ⌘⌫（hidden keyboardShortcut Button）或右键菜单。",
      "deliverables": [
        "HistoryPanel 去掉 S28 引入的 `onDelete: (() -> Void)?` 字段以及 keyMonitor 里的 keyCode 51（⌫）分支；上一轮 patch 里临时加的 `shouldInterceptDelete` 字段也一并删除——整条路径不再存在。",
        "AppDelegate 在 installPanelIfReady 里对 panel.onDelete 的赋值也一并移除。",
        "⌘⌫（hidden keyboardShortcut Button in HistoryPanelView）保留不动；右键菜单 Delete 保留不动。",
        "S28 的另一半修复——ClipPreviewView 的 epoch 守卫——**不回滚**，那是独立的正确修复。"
      ],
      "acceptance": [
        "搜索框有焦点（光标闪烁）→ 按 ⌫ → 删除搜索框里的一个字符（不删行）。",
        "任何时候按 ⌘⌫ → 删除当前选中行；右键某一行选 Delete → 同样删行。",
        "切换图片条目时的 Loading 不再卡住（S28 epoch 修复仍生效）。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S29: drop bare-Delete row-delete (search owns ⌫ exclusively)"
    },
    {
      "id": "S30",
      "title": "App 图标 + UI 视觉一轮打磨",
      "goal": "(1) 没有 App 图标，Finder/Dock/Alert 里显示的是 macOS 默认占位图；(2) HistoryPanel / ClipRowView / ClipPreviewView 整体视觉过于朴素——缺少视觉层次、选中高亮是系统默认、没有预览头部。",
      "deliverables": [
        "tools/generate-icon.swift：独立 Swift 脚本，用 CGContext 直接渲染 10 个 AppIcon PNG（16/32/32/64/128/256/256/512/512/1024 px），蓝紫渐变圆角方形底 + 白色剪贴板卡片 + 小夹子 tab + 3 条『文本行』。脚本可通过 `just icon` 重新生成。",
        "App/Assets.xcassets/AppIcon.appiconset/Contents.json 列出 10 张图；App/Assets.xcassets/Contents.json 顶层文件；project.yml 把 Assets.xcassets 加进 App target 的 sources，并把 ASSETCATALOG_COMPILER_APPICON_NAME 设为 AppIcon。",
        "Justfile 加 `icon: swift tools/generate-icon.swift App/Assets.xcassets/AppIcon.appiconset` recipe。",
        "HistoryPanelView.list 从 SwiftUI `List` 改成 ScrollViewReader + ScrollView + LazyVStack——拿到对选中高亮的完整视觉控制（List 在 macOS 上系统选中环不可定制）；选中态由 ClipRowView 自己画：AccentColor 20% 半透 + 6pt 圆角 + 4pt 水平内边距，Alfred/Raycast 风格。",
        "ClipRowView 加 `isSelected: Bool` 参数；内部用 isSelected 切换背景；图片缩略图 36×36 → 40×40 + 4pt 圆角 + 细浅灰描边；行间距 2pt；改用 .body/.caption/.caption2 语义字号。",
        "ClipPreviewView 加一条头部条（preview 面板顶部）：kind 图标（胶囊背景）+ 源 App bundleID + 相对时间 + pin/sensitive 小图标；提高预览页的可读性。",
        "保留既有交互：方向键导航、↵ 激活、⌘⌫ 删行、右键菜单、outside-click close、tab 切换 resetSelection；ScrollViewReader 仍用 scrollEpoch / selectedID 作为滚动锚。"
      ],
      "acceptance": [
        "`just icon` 能重新生成 AppIcon.appiconset 下全部 10 个 PNG，内容无差异（若无改动则幂等）。",
        "`just build` 产物 Clipboard.app 的 Contents/Resources/AppIcon.icns 存在；在 Finder 中右键 Clipboard.app → 『获取简介』看到新图标，不是默认白板。",
        "⌥⌘V 打开面板 → 选中的行高亮为带圆角的半透 accent 色条；↑↓ 移动时选中视觉跟上；图片行显示圆角缩略图。",
        "预览窗口顶部显示一条头部：胶囊 kind 图标 + 源 App + 相对时间 + 可能的 pin/sensitive 标记。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S30: generated app icon + custom row selection + preview header"
    },
    {
      "id": "S31",
      "title": "面板底部快捷键提示条（keyboard hint bar）",
      "goal": "用户不知道面板里有哪些键盘操作——方向键选择、↵ 激活、⌘P 置顶、⌘⌫ 删除等都没有视觉线索。加一条 Alfred/Raycast 风格的底部提示条把这些暴露出来。",
      "deliverables": [
        "App/ShortcutHint.swift：可复用的 key+label 胶囊（keys 用 11pt monospaced + 4pt 圆角灰底，label 用 10pt secondary 文字）；顶层 HistoryPanelView 再用它们组成一个 FooterBar。",
        "HistoryPanelView 的 body 从单 HStack 改成 VStack：上层是原有的 [list | divider | preview] 横向三栏，下层是 Divider + footerBar；固定高度 520 维持不变，footerBar 占底部 ~28pt。",
        "footerBar 展示：↑↓ Select · ↵ Copy · ⌘P Pin · ⌘⌫ Delete · ⌘, Preferences · Esc Close。\"Pin\" label 是静态文字（不随选中项 pinned 状态切换『Unpin』——只是提示有此键可用，保持稳定不闪烁）。",
        "总体色调保持中性灰，不抢 accent 选中色的视觉重点；`.background(.regularMaterial)` 既有面板材质继承过来。"
      ],
      "acceptance": [
        "⌥⌘V 打开面板 → 底部可见一条浅灰提示条，列出所有主要键盘操作。",
        "新的 footerBar 不影响既有行为：方向键仍导航、↵ 仍激活、⌘P 仍切换 pin、⌘⌫ 仍删行、⌘, 仍开 Prefs、Esc 仍关窗。",
        "列表滚动时 footerBar 固定不动（不随内容滚）。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S31: keyboard hint bar at the bottom of the history panel"
    },
    {
      "id": "S32",
      "title": "7 语言本地化（en + zh-Hans + zh-Hant + ja + ko + de + es）",
      "goal": "S8 的 Localizable.strings 双语拆分一直延后；把延后的补上，顺便扩到 6 个目标语言：简体中文、繁体中文、日文、韩文、德文、西班牙文（加英文基线共 7 种）。每种翻译由独立 subagent 审阅后才合入。",
      "deliverables": [
        "App/<locale>.lproj/Localizable.strings × 7（en / zh-Hans / zh-Hant / ja / ko / de / es），key 用英文短语、值按目标语言翻译。",
        "project.yml 的 info.properties 加 CFBundleLocalizations 列表 + CFBundleDevelopmentRegion=en；App target sources 里按需补 lproj 路径（XcodeGen 自动识别 .lproj 目录）。",
        "AppKit 层的 NSAlert / NSMenuItem / NSWindow.title 等非 SwiftUI 文本全部改走 String(localized:)；SwiftUI 的 Text() / Button() / Toggle() 等字符串字面量依赖内置的 LocalizedStringKey 查找，保持字面量不变。",
        "启动 6 个 subagent 并行审核 zh-Hans / zh-Hant / ja / ko / de / es 的翻译——每个 agent 读基线 en 文件和该语言文件，标出不自然 / 术语不一致 / 占位符语法不对等问题；按 agent 回执 apply。",
        "字符串与 harness.language_locale 约定一致：代码、注释、commit 信息仍全英文；UI 文案按系统语言；未命中本地化的字符串 SwiftUI 会回落到 key 自身（英文）。"
      ],
      "acceptance": [
        "系统语言设为简中 → 打开面板 → 搜索框占位『搜索』、底部提示条『选择 / 复制 / 置顶 / 删除 / 偏好设置 / 关闭』等全部本地化；Preferences 的 tab、标签、按钮全部本地化；NSAlert 标题和正文本地化；菜单栏（⌘A / ⌘C / ⌘V / ⌘X / ⌘Z / ⌘Q）等 key equivalent 继续工作（不受本地化影响）。",
        "依次在系统语言里切 zh-Hant / ja / ko / de / es → 所有界面看到对应语言的翻译。",
        "切到未提供的语言（例如法语）→ App 回落到 CFBundleDevelopmentRegion=en 的英文基线。",
        "6 个审核 agent 给出的可操作修订建议全部落地到对应文件。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S32: localize to en/zh-Hans/zh-Hant/ja/ko/de/es with subagent-reviewed translations"
    },
    {
      "id": "S33",
      "title": "本地化缺漏 + 尺寸上限改数字输入 + Preferences 加语言选择器",
      "goal": "(1) S32 落地的翻译在面板底部快捷键条、KindChip、空状态、context menu、OnboardingView bullets 等处未生效——SwiftUI Text(_ content: String) 不会走 LocalizedStringKey 的查表路径，只有字面量才会。(2) 尺寸上限 Slider 硬编码 100 MiB 最大，用户想存更大就没办法。(3) Preferences 没有语言选择器，非系统默认语言的用户没法独立切换 App 语言。",
      "deliverables": [
        "LocalizedStringKey 修复 —— 把所有传 String 变量到 Text/Button/Label 的地方改为 LocalizedStringKey：ShortcutHint.label / KindChip.label / ClipPreviewView.kindLabel 返回类型 / HistoryPanelView.emptyStateText 返回类型 / Button(item.pinned ? \"Unpin\" : \"Pin\") 外包 LocalizedStringKey / OnboardingView.bullet 第二参类型改为 LocalizedStringKey。",
        "PreferencesView 的尺寸上限从 Slider 改为 TextField（按 MiB 数值输入）——最大值由用户决定；0 = 无上限；下方提示文字『0 to disable...』保持；配合 .number format 自动做字符校验。",
        "Preferences 结构新增 `languageOverride: String?`（nil = 跟随系统；其余为 7 种支持语言代码）；PreferencesStore 同步 UserDefaults 读写；测试覆盖 round-trip。",
        "PreferencesView.General 区块新增 Language 选择器——System / English / 简体中文 / 繁體中文 / 日本語 / 한국어 / Deutsch / Español；变更时写入 UserDefaults.standard 的 `AppleLanguages` 键（system 选项清除该键），并弹出一条 NSAlert 提示重启才会生效。",
        "应用启动时无额外工作——macOS 在 Bundle 解析时自动读 AppleLanguages 和 CFBundleLocalizations 的交集。"
      ],
      "acceptance": [
        "系统语言设为英语，App Preferences 的 Language 选为『简体中文』→ 弹『重启生效』提示 → 重启 App → 面板底部条显示『选中 / 复制 / 置顶 / 删除』，KindChip 显示『全部 / 文本 / 图片 / 文件 / 富文本』，No history yet → 暂无历史，Unpin/Pin/Delete context menu 也都是中文。",
        "Preferences → General → 『Skip items larger than』输入框能填任意数字（如 512 MiB、1024 MiB、0）；打开一张 600 MiB 的图片就不再被拒。",
        "Language 选回『System』→ 清除 AppleLanguages → 下次启动跟随系统语言。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S33: fix SwiftUI LocalizedStringKey usage, unbounded size input, language picker"
    },
    {
      "id": "S34",
      "title": "语言切换不冻窗、不影响快捷键",
      "goal": "S33 的语言 picker 在用户选择某项或取消时会冻住 Preferences 窗口，且会让自定义全局快捷键随后不响应。",
      "deliverables": [
        "PreferencesWindowController.applyLanguageOverride 去掉 NSAlert.runModal() —— 模态在 Picker 的 dismiss 动画期间弹出，会中断 SwiftUI 的响应链接续；用户只会在重启后看到新语言，所以模态并无必要，inline caption 『Language changes apply after the next launch.』 已经陈述过。",
        "PreferencesView 的 Picker binding setter 里 onSave(prefs) 和 onApplyLanguage(newValue) 都 DispatchQueue.main.async 延后 —— 副作用在 Picker 完全关闭后才跑，避免在 Picker 消散过程中触发 @State 变更与 Monitor 重建 + UserDefaults 写入的级联，防止事件分发被暂时阻塞（后者是『快捷键随后无响应』的最可能成因）。",
        "保留 applyLanguageOverride 对 UserDefaults.standard[\"AppleLanguages\"] 的写入 / 清除 —— 这是语言切换的唯一必要副作用。",
        "现有的『语言变更将在下次启动时生效』inline 提示保持，用户知情不受影响。"
      ],
      "acceptance": [
        "打开 Preferences → 点击 Language 下拉 → 选『日本語』→ 下拉自然关闭；Preferences 窗口依然可交互，能继续点其他控件、切 tab。",
        "同一流程后立即按全局快捷键（默认 ⌥⌘V 或用户自设的）→ HistoryPanel 正常出现。",
        "重启 App → UI 切到新语言。",
        "`just test` / `just lint` / `just build` 全绿。"
      ],
      "commit_message": "S34: language switch no longer freezes Preferences or blocks hotkey"
    },
    {
      "id": "S35",
      "title": "白天模式视觉分层",
      "goal": "light mode 下 .regularMaterial 铺满导致的白底白字、弱对比问题——给列表列 / 搜索框 / 底部条 / 选中态补足视觉分层。不动 dark mode 语义。",
      "deliverables": [
        "HistoryPanelView 的列表列叠加 `Color.primary.opacity(0.04)` 背景 tint（light mode 下形成浅灰侧栏；dark mode 下几乎不可见，因为 primary 在深色模式是接近白色，叠在深色 material 上仍然微弱）。preview 列保持纯 `.regularMaterial`，形成左浅右透的双层感。",
        "HistoryPanelView.searchField 包一个胶囊容器——圆角 8，`Color.primary.opacity(0.06)` fill + `Color.primary.opacity(0.10)` 0.5pt stroke；放大镜图标和 TextField 内嵌在容器里，上下 6pt / 左右 10pt 内边距，容器外围再套 10pt 外边距维持原来的排版节奏。",
        "footerBar 应用相同 `Color.primary.opacity(0.04)` 背景 tint（与列表列同色），保留现有的顶部 Divider；使底部条在白底上也能辨出一条状态栏。",
        "ClipRowView 选中态从 `accentColor.opacity(0.20)` fill 升级为 `accentColor.opacity(0.18)` fill + `accentColor.opacity(0.45)` 0.5pt stroke；stroke 让选中框在白底上有清晰轮廓，fill 保持不过饱。",
        "ShortcutHint 键帽背景从 `Color.secondary.opacity(0.18)` 改为 `Color.primary.opacity(0.08)` fill + `Color.primary.opacity(0.14)` 0.5pt stroke；在 light mode 下形成可辨认的按键轮廓。",
        "KindChip 未选中态从 `Color.secondary.opacity(0.18)` 改为 `Color.primary.opacity(0.06)` fill + `Color.primary.opacity(0.10)` 0.5pt stroke；选中态仍是实心 accent + 白字，保证两态对比依然强。"
      ],
      "acceptance": [
        "macOS System Settings → Appearance 切到 Light → ⌥⌘V 打开 HistoryPanel：左侧列表列明显比右侧预览列深一档；搜索框是可辨识的胶囊；底部条是一条浅灰状态带。",
        "同样环境下，点击任一行：选中框是半透明 accent 填充 + accent 描边；在白底上一眼就能看到选中位置。",
        "macOS 切回 Dark mode：整体观感与 S34 时基本一致——新增的 `primary.opacity(0.04)` 之类在深色材质上几乎不可见，不会让 dark 变糊。",
        "ShortcutHint 和 KindChip 在两种模式下均可辨认、与背景有足够对比。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S35: light-mode visual layering for panel, search, rows and chips"
    },
    {
      "id": "S36",
      "title": "设置页面卡片化",
      "goal": "Preferences 在 light mode 下裸 Form + Section 太朴素——切到 macOS 13+ 的 grouped form style，各 Section 变成带浅色圆角背景的卡片。",
      "deliverables": [
        "PreferencesView 的 general / privacy / data 三个 Form 都加 `.formStyle(.grouped)` —— SwiftUI 自动把每个 Section 渲染成圆角卡片，背景色跟随系统 secondary surface，light mode 下形成柔和的分组感。",
        "TabView 的外层 `.padding()` 去掉——grouped form 自己已经有外边距，再加 padding 会显得挤；保留 `.frame(width: 520, height: 380)`。",
        "Privacy tab 的 TextEditor（blocked bundle IDs）在 grouped form 里仍可见、仍可编辑；如果默认背景被 grouped form 接管，确认 monospaced 字体依然生效。",
        "Data tab 的三个按钮（Export/Import/Clear）在 grouped form 里呈现为分组内的条目，保留 destructive role 的红色 Clear。"
      ],
      "acceptance": [
        "打开 Preferences（light mode）：General / Privacy / Data 三个 tab 的每个 Section 都是浅灰圆角卡片，视觉上与 panel 的白天分层一致。",
        "dark mode 下 grouped form 使用系统预设的深色卡片背景，不会过白或过黑。",
        "Privacy → Blocked bundle IDs 输入框仍能编辑，粘贴多行 bundle ID 正常；Data → Clear history 按钮仍是红色。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S36: grouped form style for Preferences tabs"
    },
    {
      "id": "S37",
      "title": "HistoryPanel Alfred 化：去标题栏 + 全宽搜索 + 2:1 分栏 + 玻璃",
      "goal": "把 HistoryPanel 从带标题栏的 .regularMaterial 面板改造为 Alfred 风格：borderless + 圆角 + ultraThinMaterial 玻璃；搜索栏占满 row1；row2 是列表 : 预览 = 2 : 1 的左右分栏；保留底部 ShortcutHint 栏。",
      "deliverables": [
        "HistoryPanel 的 styleMask 改为 `[.nonactivatingPanel]`（borderless），去掉 .titled / .closable / .resizable；窗口 `isOpaque = false`、`backgroundColor = .clear`、`hasShadow = true`；删除 `title =` 赋值——没有标题栏可显示。panel 宽度 720、高 520，`positionAtCursorScreen()` 逻辑保留。",
        "HistoryPanelView 的 body 结构改为：row1 `searchField`（全宽 720）→ Divider → row2 `HStack { listColumn(480) | Divider | ClipPreviewView(239) }`（共 720，近似 2:1）→ Divider → row3 `footerBar`。总 `.frame(width: 720, height: 520)`。",
        "HistoryPanelView 根部背景从 `.regularMaterial` 改为 `.ultraThinMaterial`；再 `.clipShape(RoundedRectangle(cornerRadius: 12))` 让四角与窗口透明背景吻合；外加 `RoundedRectangle(cornerRadius: 12).strokeBorder(Color.primary.opacity(0.12), lineWidth: 0.5)` overlay 做一道发丝级边框。",
        "listColumn 去掉 S35 添加的 `Color.primary.opacity(0.04)` 背景——Alfred 风格下整面玻璃是统一色调，不再做列分色；footerBar 的 `primary.opacity(0.04)` 保留，作为底部状态栏的轻微区分。",
        "searchField 仍是 S35 的胶囊容器（圆角 8 / primary 0.06 fill / 0.10 stroke），但从 listColumn 提到顶层 row1，水平 padding 适配 720 全宽。",
        "其余逻辑不变——@FocusState 自动聚焦搜索框、keyboardShortcutButtons、onChange(tab/kindFilter)、outside-click-close、⬆⬇ 箭头本地监听、Esc cancel、ScrollViewReader 滚动一致性等行为全部保留。"
      ],
      "acceptance": [
        "按全局快捷键打开 HistoryPanel → 没有 macOS 标题栏；四角是 12pt 圆角；整面是 ultraThinMaterial 玻璃，能看到底下桌面 / 其他窗口的模糊画面。",
        "panel 顶部一整条横向搜索框；搜索框下是一条 hairline divider，紧接一个 HStack：左 480 是列表（Tab 切换 / KindChip / 列表）；右 239 是预览；比例明显接近 2:1。",
        "panel 底部仍是 S31 的 ShortcutHint 栏，带 S35 的浅灰状态条底色。",
        "点击 panel 外部会关闭；Esc 关闭；⬆⬇ 切行；Return 复制；⌘P / ⌘⌫ / ⌘, 分别是 pin / delete / 偏好设置——全部功能回归不破。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S37: Alfred-style borderless glass panel with full-width search and 2:1 split"
    },
    {
      "id": "S38",
      "title": "HistoryPanel 三处视觉细节",
      "goal": "S37 玻璃改造完成后三个遗留问题：ultraThinMaterial 太透偏暗、tabBar 紧贴顶部分割线没 margin、左右两列 header 下方的横向分割线高度不一致。",
      "deliverables": [
        "HistoryPanelView 根部背景从 `.ultraThinMaterial` 升到 `.regularMaterial`——在 light mode 下更白、更『明亮』，但仍保留玻璃模糊；dark mode 下也更稳不发灰。",
        "tabBar 从 `.padding(.bottom, 6)` 改为 `.padding(.top, 8).padding(.bottom, 4)`——给 All/Pinned segmented picker 在顶部横向 Divider 下留一口气。",
        "listColumn 的 header 区（tabBar + KindChipBar）和 ClipPreviewView 的 header 区都显式 `.frame(height: 64)`——两列的 in-column 横向 Divider 从而落在同一 y；SkipBanner 从 header 区迁到 Divider 之下，避免压缩 header 高度或破坏对齐。",
        "ClipPreviewView.header 内容保持单行 HStack，但因 `.frame(height: 64)` 后会在 64pt 框内垂直居中，视觉上更像 preview pane 的 toolbar。"
      ],
      "acceptance": [
        "打开 panel：背景明显比 S37 更亮、更接近 Raycast / Alfred 的 light-mode 玻璃；不再像贴在桌面上的透明膜。",
        "All / Pinned segmented 控件上方与顶部横向 Divider 之间有 8pt 空隙。",
        "row2 里 list 列的横 Divider（位于 tabBar + KindChipBar 之下）与 preview 列的横 Divider（位于 kind capsule header 之下）同 y 对齐，肉眼看就是一条连续线。",
        "SkipBanner 触发时位于 Divider 下方（list 行之上），不会把 header 顶歪。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S38: brighter material, tabBar top margin, aligned row-2 dividers"
    },
    {
      "id": "S39",
      "title": "Preferences 玻璃化（保留标题栏）",
      "goal": "把 S37-S38 在 HistoryPanel 上确立的 Alfred 式玻璃观感延伸到 Preferences；但 Preferences 保留 macOS 标题栏，供拖动/关闭/最小化使用。",
      "deliverables": [
        "PreferencesWindowController 的 window 设 `isOpaque = false`、`backgroundColor = .clear`、`hasShadow = true`；styleMask 不动，保留 `.titled / .closable / .miniaturizable` —— 标题栏仍由系统渲染，content 区变透明。",
        "PreferencesView 根部 TabView 加 `.background(.regularMaterial)`，与 HistoryPanel 统一材质；Preferences 本身圆角由系统标题栏自带（titled window 自动圆角），不另加 clipShape。",
        "three 个 Form（general / privacy / data）各追加 `.scrollContentBackground(.hidden)`，把 Form 自带的 ScrollView 不透明底色藏起来，让 grouped Section 卡片悬浮在 material 玻璃上。",
        "标题栏由系统默认 titlebar 材质绘制，与下方 regularMaterial 不强求『合一』——只要两块都是系统玻璃，视觉连续性足够。"
      ],
      "acceptance": [
        "从菜单栏或 ⌘, 打开 Preferences：窗口整体有 HistoryPanel 相同质感的玻璃（桌面背景轻度模糊透出），grouped Section 卡片浮在玻璃上；标题栏仍在、traffic light 仍可用、窗口可拖动。",
        "切到三个 tab，任意一个 Form 都不再渲染不透明白底——背景透出玻璃；卡片本身保持 SwiftUI 默认 grouped 样式。",
        "light / dark 两种系统外观下均自然——material 是 adaptive 的。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S39: apply Alfred-style glass to Preferences while keeping title bar"
    },
    {
      "id": "S40",
      "title": "Preferences 玻璃化四处收尾",
      "goal": "S39 把 Preferences 切到玻璃 + grouped form 之后剩下的四处视觉 / 交互瑕疵：首开 Picker 拿到初始焦点、标题栏与 body 断层、Retention TextField 的方形 bezel 像一条黑线、Privacy TextEditor 默认白底与玻璃不搭。",
      "deliverables": [
        "PreferencesWindowController show() 里 `window?.makeKeyAndOrderFront(nil)` 之后补一句 `window?.makeFirstResponder(nil)` —— 让窗口自己做第一响应者，Language Picker 首开不再显示蓝色焦点环。",
        "PreferencesWindowController 的 styleMask 追加 `.fullSizeContentView`、`window.titlebarAppearsTransparent = true` —— content 延展到 title bar 下方，SwiftUI 根部的 regularMaterial 铺满整面，标题栏与 body 合一；traffic light 由系统绘制，依然在 content 之上。",
        "PreferencesView Retention 下的『Skip items larger than』TextField 加 `.textFieldStyle(.roundedBorder)` —— 避免 default squareBezel 在透明 Form 底上画出一条类似黑线的外框。",
        "PreferencesView Privacy 的 TextEditor（blocked bundle IDs）加 `.scrollContentBackground(.hidden)` 后，自己套一层 `RoundedRectangle(cornerRadius: 8)` 背景 + stroke（和 HistoryPanel 搜索框风格一致：primary 0.06 fill / 0.10 stroke），让它融入 grouped card / 玻璃观感。"
      ],
      "acceptance": [
        "⌘, 首次打开 Preferences：Language Picker 没有蓝色焦点环；点击后能正常弹出。",
        "标题栏与 body 之间没有材质断层，整面是一块玻璃；窗口仍能拖动、最小化、关闭。",
        "Retention 分组下的 MiB 输入框是系统 rounded bezel，不再像一条贴底黑线；能输入数字、能聚焦。",
        "Privacy 分组下的 bundle ID TextEditor 底色与 Section card 一致，圆角 + hairline stroke；文字 monospaced。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S40: preferences glass polish — drop initial focus, unify title bar, bezel text field, themed text editor"
    },
    {
      "id": "S41",
      "title": "just run 自动关闭旧进程",
      "goal": "改版后 `just run` 只 `open` .app，而 Clipboard 是菜单栏常驻单例，macOS 的 open 对已运行 App 只做激活——导致新构建看不到更新。每次手工 pkill 太啰嗦。",
      "deliverables": [
        "Justfile 的 run 目标追加 `-pkill -x Clipboard` 与 `sleep 0.3` 两行，置于 `open` 之前；前缀 `-` 允许 pkill 退出码非零（没有老进程也 OK），0.3s 睡眠留给应用优雅退出 + 释放菜单栏条。"
      ],
      "acceptance": [
        "有老进程在跑时 `just run`：老进程先退出、新构建启动，菜单栏图标是新二进制。",
        "没有老进程时 `just run`：pkill 报 no matches、Just 因前缀 `-` 继续执行、新构建启动。",
        "`just build` / `just lint` / `just test` 未受影响。"
      ],
      "commit_message": "S41: just run auto-kills existing Clipboard before launching"
    },
    {
      "id": "S42",
      "title": "Hotkey Recorder 与 MiB 输入与玻璃风格统一",
      "goal": "S39–S40 做完玻璃 + grouped form 之后，General tab 剩两处输入还在『系统默认 bezel』：Hotkey 的 `KeyboardShortcuts.Recorder` 和 Retention 的 MiB TextField——在 `scrollContentBackground(.hidden)` 的 grouped Section card 上显得突兀。",
      "deliverables": [
        "Hotkey Section 改用显式布局：`HStack { Text(\"Toggle panel\"); Spacer(); KeyboardShortcuts.Recorder(\"\", name: .toggleHistoryPanel).labelsHidden() }`——库自带的 label 由 labelsHidden 收掉，版式由我们掌控。Recorder 本身的 NSSearchField-alike 外观由第三方库决定，我们只能保证其周围节奏与其它控件一致。",
        "Retention 的 MiB TextField 丢掉 S40 的 `.textFieldStyle(.roundedBorder)`，改用 `.textFieldStyle(.plain)` + 自定义圆角容器（与 HistoryPanel 搜索框、Privacy TextEditor 同一风格：圆角 6、`Color.primary.opacity(0.06)` fill、`Color.primary.opacity(0.10)` 0.5pt stroke），视觉并入玻璃一套系统。"
      ],
      "acceptance": [
        "打开 Preferences → General：Hotkey 行的『Toggle panel』文字在左、Recorder 在右；Section card 上下只有 Recorder 这一个控件；Recorder 焦点环仅在录制时出现。",
        "Retention 行的 MiB 输入框是柔和圆角 + hairline stroke，与 Privacy 的 TextEditor、HistoryPanel 的搜索框是同一视觉家族。",
        "输入数字 / 录制快捷键功能全部正常。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S42: hotkey recorder + MiB field adopt the glass container style"
    },
    {
      "id": "S43",
      "title": "自建 HotkeyRecorder + 标题栏分隔线",
      "goal": "S42 的 KeyboardShortcuts 库自带 Recorder 外观 AppKit 第三方控死，在玻璃 Preferences 里视觉仍然突兀；另 `.fullSizeContentView` 让 Form 内容可以滑到标题栏之下，但没有视觉分隔线。",
      "deliverables": [
        "新增 App/HotkeyRecorder.swift —— SwiftUI 自建 shortcut recorder：仅用库的 `KeyboardShortcuts.getShortcut(for:)` / `setShortcut(_:for:)` 存取快捷键，不再引用库的 Recorder UI。状态机：idle（显示 Shortcut.description 或『None』）→ recording（点击进入，边框高亮 accent + 文案『Recording…』）→ 捕获到带修饰键的 keyDown 后写入并回到 idle；Esc 中止；idle 下右侧有 `xmark.circle.fill` 按钮清除快捷键。",
        "HotkeyRecorder 的容器与 HistoryPanel 搜索框 / Privacy TextEditor / MiB 输入框同风格：圆角 6、primary 0.06 fill、primary 0.10 stroke；录制状态 fill 升到 0.10、stroke 切为 accent。",
        "使用 `NSEvent.addLocalMonitorForEvents(matching: .keyDown)` 在 start 时订阅、在 stop / onDisappear 时释放；未带任何修饰键的 keyDown 会 `NSSound.beep()` 并忽略，防止误录裸字母。",
        "PreferencesView General Hotkey Section 改为 `HStack { Text(\"Toggle panel\"); Spacer(); HotkeyRecorder(name: .toggleHistoryPanel) }`——彻底不再使用 `KeyboardShortcuts.Recorder`。",
        "PreferencesWindowController init 里 `window.titlebarSeparatorStyle = .line`——让内容滑到 title bar 下方时有一条系统绘制的分隔线。`titlebarAppearsTransparent = true` 默认下该 style 会是 `.none`，需要显式指定。"
      ],
      "acceptance": [
        "打开 Preferences → General → Hotkey：Toggle panel 行左侧文字、右侧是圆角胶囊 recorder，视觉与 MiB 输入框、Privacy TextEditor、HistoryPanel 搜索框同家族。",
        "点击 recorder → 边框变 accent、文字显示『Recording…』；按 ⌥⌘V → 自动保存、展示 ⌥⌘V；按 Esc → 取消保持原值；已有快捷键时右侧 × 可清除。",
        "记录完成后立即按该快捷键 → HistoryPanel 弹出。",
        "Preferences 内容能向上滑到标题栏下方时，标题栏与 body 之间有一条系统分隔线（.line 风格）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S43: custom HotkeyRecorder + titlebar separator line"
    },
    {
      "id": "S44",
      "title": "Retention 内分隔线去除 + 标题栏显式下缘线",
      "goal": "两处未完成的视觉修整：(a) grouped Form 会在 Retention 分组的 Cap 行与 Skip 行之间画一条系统内分隔线——『Cap slider 下面的黑线』就是它；(b) S43 的 `titlebarSeparatorStyle = .line` 在 `titlebarAppearsTransparent=true` + 无 toolbar 的组合下并未实际绘出下缘分隔。",
      "deliverables": [
        "PreferencesView.Retention Section 内容从多个平铺行改为单个 VStack(alignment: .leading, spacing: 10)——grouped Form 把 VStack 当成一条 row，不再在 Cap 行与 MiB 行之间绘制内分隔线；caption 依然随 VStack 自然靠左。",
        "MiB 行内加 Spacer() 把 TextField + MiB 标签推到右端，保持原来的视觉右对齐。",
        "PreferencesView body 追加 `.safeAreaInset(edge: .top, spacing: 0) { Divider() }` —— SwiftUI 在安全区顶部（= 标题栏下缘）显式绘一条 1pt 分隔线，内容滑到标题栏下方时该线始终可见；`titlebarSeparatorStyle = .line` 在当前样式组合下不可靠，但保留不冲突。"
      ],
      "acceptance": [
        "Preferences → General → Retention：Cap 滑块下方没有黑色横线；Cap 行与 MiB 行之间保持 10pt 空隙；MiB 输入框仍靠右、右侧紧跟 `MiB` 标签。",
        "Preferences body 上下滑动时，标题栏下缘始终有一条清晰的 1pt 分隔线。",
        "其余功能不受影响：Cap 滑块实时保存、MiB 输入保存、语言切换、快捷键录制等。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S44: collapse Retention rows into one + explicit divider under title bar"
    },
    {
      "id": "S45",
      "title": "回退 fullSizeContentView + 替换 Cap Slider",
      "goal": "S40 引入的 `.fullSizeContentView + titlebarAppearsTransparent` 组合带来两个顽固问题：(a) Cap 行里 SwiftUI Slider 在 20–2000 范围下几乎全是暗灰 unfilled track，肉眼像『一条黑线』；(b) SwiftUI content 物理上延伸到 title bar 范围内，滚动时 body 会穿到标题栏下。最干净的解法：回归标准 titled window，让 title bar 天然落在 body 之上，同时把 Slider 换成 Stepper，彻底不产生暗色轨。",
      "deliverables": [
        "PreferencesWindowController 移除 `.fullSizeContentView` 从 styleMask、移除 `titlebarAppearsTransparent = true`、移除 `titlebarSeparatorStyle = .line`；保留 `isOpaque = false`、`backgroundColor = .clear`、`hasShadow = true`。结果：title bar 是 macOS 默认 translucent chrome、body 在其下方独立铺 regularMaterial；两层都是系统玻璃，观感继续统一，但 body 不再延伸到 title bar。",
        "PreferencesView 移除 S44 添加的 `.safeAreaInset(edge: .top) { Divider() }` —— title bar 作为独立层本身就是 body 的天然上边界，不需要再塞 SwiftUI 分隔。",
        "PreferencesView Retention 区 Cap 行从 `HStack { Text; Slider; Text }` 改为 `HStack { Text(\"Cap\"); Spacer(); Stepper(value:, in: capRange, step: 10) { Text(\"\\(prefs.cap)\").monospacedDigit() } }` —— Stepper 是 ± 按钮 + 数值，无轨、无暗色线；同时保留随时 onSave。",
        "capRange 类型保持 ClosedRange<Int>（Stepper in: 用 Int 更自然）；若原来是 Double range，同步改成 `20...2000`（Int）。"
      ],
      "acceptance": [
        "Preferences → General → Retention：Cap 行只有 Stepper（± 按钮 + 当前数值），不再有横向暗灰 Slider 轨。",
        "Preferences 上下滚动时，body 内容不再穿到 title bar 下方；title bar 与 body 之间视觉分层清晰。",
        "title bar 仍是系统 translucent chrome，与 body 的 regularMaterial 同为系统玻璃，观感继续融洽。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S45: drop fullSizeContentView, replace cap slider with stepper"
    },
    {
      "id": "S46",
      "title": "Cap 走 TextField + 玻璃统一但 body 有界滚动",
      "goal": "基于用户两点反馈：(a) Stepper 仍然不够干净，希望 Cap 用纯数字输入框（与 MiB 一致）；(b) S40 的『玻璃统一标题栏』观感更好，只是 S45 的退让让标题栏与 body 再次分层——但 fullSizeContentView 之前 body 内容会滚进标题栏下方。解法：回到 fullSizeContentView + titlebarAppearsTransparent，但用 SwiftUI 的 safe area 机制把 body 框在标题栏下，仅让 material 背景穿过 safe area。",
      "deliverables": [
        "PreferencesWindowController: contentRect 从 520x380 → 520x408（title bar 约 28pt，这样去掉 title bar 后可视 body 还是 380pt）；styleMask 重新加 `.fullSizeContentView`；重新打开 `titlebarAppearsTransparent = true`；保留 `isOpaque = false / backgroundColor = .clear / hasShadow = true`。",
        "PreferencesView body: 把 `.frame(width: 520, height: 380)` 换成 `.frame(maxWidth: .infinity, maxHeight: .infinity)`，让 TabView 自然吃 safe area；背景改为 `.background(.regularMaterial, ignoresSafeAreaEdges: .all)` —— material 穿透 safe area 铺满整个窗口（含标题栏下方），但 TabView 内容被 safe area 约束；补回 `.safeAreaInset(edge: .top, spacing: 0) { Divider() }` 提供标题栏下缘的显式分隔线。",
        "PreferencesView Retention 里 Cap 从 `Stepper` 改成纯 TextField（与 MiB 同款 pill 容器：圆角 6、primary 0.06 fill、primary 0.10 stroke、`.textFieldStyle(.plain)`、宽 80、右对齐）；用 `format: .number` 约束只接受数字，在 set 闭包里 clamp 到 20...2000 并立即 onSave。",
        "capRange 保持 `ClosedRange<Int> = 20...2000`，在 TextField set 闭包里用做 clamp 区间。"
      ],
      "acceptance": [
        "Preferences Retention：Cap 行只有 Text('Cap') + Spacer + 胶囊数字输入框，没有 Stepper 按钮、没有 Slider 轨。输入非数字被拒，<20 或 >2000 被 clamp。",
        "打开 Preferences：标题栏透明、content 的 regularMaterial 向上穿到标题栏下方，整窗是一块玻璃；scroll 时 body 内容在 safe area 内滚动，不会越过标题栏下缘 Divider；该 Divider 全程可见。",
        "traffic light / 拖动 / 关 / 最小化正常；切换 tab 正常。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S46: cap as number field; glass unified but body bounded by safe area"
    },
    {
      "id": "S47",
      "title": "修复 S46 导致的 Preferences 无法打开",
      "goal": "S46 把 PreferencesView 的 `.frame(width:520, height:380)` 换成 `.frame(maxWidth: .infinity, maxHeight: .infinity)`，在 macOS 14+ 上 NSHostingController 的默认 `sizingOptions = .standardBounds` 会读取 SwiftUI intrinsic size 回写到 window 的 preferredContentSize，导致窗口被缩到 0，Preferences 看起来『打不开』。",
      "deliverables": [
        "PreferencesWindowController.rebuildContent() 创建 NSHostingController 后显式 `hosting.sizingOptions = []`——关掉 SwiftUI intrinsic size → 窗口尺寸 的反向联动，窗口尺寸始终由 contentRect 控制。",
        "PreferencesView body 的 frame 从 `.frame(maxWidth: .infinity, maxHeight: .infinity)` 改回显式 `.frame(width: 520, height: 408)`——和 contentRect 一致，TabView 按此尺寸布局，内部 SwiftUI 组件继承 window 的 safe area（标题栏 28pt），safeAreaInset Divider 仍在 safe area 顶端。",
        "保留 S46 的 fullSizeContentView / titlebarAppearsTransparent / `.background(.regularMaterial, ignoresSafeAreaEdges: .all)` / `.safeAreaInset(edge: .top) { Divider() }` / Cap TextField ——本 slice 只解决窗口无法打开这个 regression。"
      ],
      "acceptance": [
        "`⌘,` 或菜单栏 『偏好设置』能正常打开 Preferences 窗口，大小 520x408，标题栏透明、body 是玻璃、顶端有 Divider。",
        "TabView / Form 内容在 safe area 内滚动，不穿越 Divider。",
        "Cap TextField、其它 Preferences 控件照常工作。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S47: fix preferences failing to open after S46 (NSHostingController sizing)"
    },
    {
      "id": "S48",
      "title": "修复 Preferences 打不开（sizingOptions = [] 反而截断了 preferredContentSize）",
      "goal": "S47 把 `hosting.sizingOptions = []` 当『安全措施』，结果弄巧成拙：SwiftUI 本应把 `.frame(width: 520, height: 408)` 的 intrinsic 推到 NSViewController.preferredContentSize，然后 window 按此刷 frame；清空 sizingOptions 导致 preferredContentSize 停在 NSZeroSize，AppKit 把窗口折成 0，Preferences 依旧打不开。",
      "deliverables": [
        "PreferencesWindowController.rebuildContent() 移除 `hosting.sizingOptions = []` —— 恢复默认 `.standardBounds`，让 SwiftUI 的显式 frame 驱动 preferredContentSize 驱动 window frame。",
        "PreferencesView 保持 `.frame(width: 520, height: 408)` —— SwiftUI intrinsic 明确 520x408，默认 sizingOptions 会安全地把它写到 preferredContentSize。",
        "其它 S46 / S47 状态（fullSizeContentView / titlebarAppearsTransparent / 玻璃 ignoresSafeAreaEdges / safeAreaInset Divider / Cap TextField）不动。"
      ],
      "acceptance": [
        "Preferences 能通过 `⌘,` 正常打开，尺寸 520x408，玻璃统一、body 有界。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S48: restore NSHostingController default sizingOptions so preferences window shows"
    },
    {
      "id": "S49",
      "title": "Preferences body 真正留在 safe area 内",
      "goal": "S48 后窗口能打开了，但 body 依然穿到标题栏下方——因为 `.frame(width: 520, height: 408)` 把 TabView 钉成固定尺寸盒子，SwiftUI 不再给它应用 safe area inset，TabView 从 y=0 开始铺，tab 条和 『快捷键』 section 标题刚好落在透明标题栏下。",
      "deliverables": [
        "PreferencesView body 的 `.frame(width: 520, height: 408)` 改为 `.frame(maxWidth: .infinity, maxHeight: .infinity)` —— TabView 走 flex 布局，继承 parent 的 safe area，内容自动落在标题栏下方。",
        "PreferencesWindowController.rebuildContent() 设 `hosting.sizingOptions = []` 并显式 `hosting.preferredContentSize = NSSize(width: 520, height: 408)` —— 阻止 SwiftUI flex frame 的零 intrinsic 反向把窗口缩成 0，同时手动把窗口尺寸钉在 520x408。",
        "background 继续 `.regularMaterial, ignoresSafeAreaEdges: .all)` —— material 跨过 safe area 铺到窗口满幅（含标题栏下方），玻璃仍统一。",
        "`.safeAreaInset(edge: .top, spacing: 0) { Divider() }` 继续提供标题栏下缘的显式分隔线。"
      ],
      "acceptance": [
        "⌘, 打开 Preferences：窗口显示 520x408。",
        "tab 选择器（通用/隐私/数据）位于标题栏 *下方*，不再压在 traffic light 旁边。",
        "`快捷键` 这样的 section 标题不再被标题栏遮挡；滚动时内容也被 safe area + Divider 限制在下方。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S49: flex frame + manual preferredContentSize so preferences body stays below title bar"
    },
    {
      "id": "S50",
      "title": "放弃 fullSizeContentView，稳定 Preferences",
      "goal": "S40 / S46 / S49 多次尝试『fullSizeContentView + 透明标题栏』让玻璃贯通，但每次都有 regression——要么窗口塌陷（S46），要么 body 穿到标题栏下方（S49）。SwiftUI 在 fullSizeContentView + 显式 frame 下不会把 safe area inset 传给固定盒子，flex frame 又让 intrinsic 归零破坏窗口尺寸。放弃这条路，回到标准 titled window：标题栏由系统 macOS chrome 渲染（它本来就是系统玻璃）、body 在其下方独立铺 regularMaterial，两层都是系统玻璃，差异极小；body 从来不会穿到标题栏下方。",
      "deliverables": [
        "PreferencesWindowController: styleMask 去掉 `.fullSizeContentView`，保留 `[.titled, .closable, .miniaturizable]`；去掉 `titlebarAppearsTransparent = true`；保留 `isOpaque = false / backgroundColor = .clear / hasShadow = true`（让 SwiftUI 的 regularMaterial 仍能填充 content 区）；contentRect 从 520x408 回到 520x380（title bar 作为独立层不占 contentRect）。",
        "PreferencesWindowController.rebuildContent: 去掉 S49 加的 `sizingOptions = []` 和 `preferredContentSize` 手动设置，恢复默认——explicit SwiftUI frame 自然驱动 preferredContentSize。",
        "PreferencesView body: 从 `.frame(maxWidth: .infinity, maxHeight: .infinity) + .background(.regularMaterial, ignoresSafeAreaEdges: .all) + .safeAreaInset(edge: .top) { Divider() }` 简化回 `.frame(width: 520, height: 380) + .background(.regularMaterial)`——content 区就是 520x380，没有 safe area 困扰。",
        "保留 S46 的 Cap TextField（pill 容器 / plain style / 仅接受数字 / clamp 到 20…2000）。"
      ],
      "acceptance": [
        "⌘, 打开 Preferences 正常显示：标题栏是 macOS 默认 translucent chrome（上方 28pt），body 是 regularMaterial 玻璃（520x380），两层都是系统玻璃，观感一致。",
        "Body 不再穿到标题栏下方——title bar 作为独立层本身就是天然边界。",
        "Cap / MiB 数字输入框、Hotkey Recorder、Language Picker、其它控件全部正常工作。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S50: stabilize preferences by reverting fullSizeContentView attempts"
    },
    {
      "id": "S51",
      "title": "HistoryPanel 标题还原 + 各 tab/chip 展示条数",
      "goal": "(a) S37 borderless 化时把 `NSPanel.title = \"Clipboard\"` 拿掉了——虽然无 title bar 展示，但 NSWindow.title 对 macOS Accessibility 与 `⌘\\`` 切换器可见，补回；(b) HistoryPanel 顶部 All/Pinned 双 tab 与 KindChipBar（全部/文本/图片/文件/富文本）在切换时不直观，用户需要提前知道各分类有多少条——在标签和 chip 上追加条数。",
      "deliverables": [
        "HistoryPanel.swift init 里恢复 `title = String(localized: \"Clipboard\")`——不影响视觉（panel 无 title bar），补回 accessibility/窗口管理的可识别名。",
        "HistoryPanelViewModel 新增三个计算入口：`allTabCount`（当前 search + kindFilter 下的总数）、`pinnedTabCount`（当前 search + kindFilter 下的 pinned 数）、`kindChipCount(for: ClipKind?)`（当前 search + currentTab + 该 kind 下的条数）；nil kind = 不限类型。实现上以 `items`（store.search 结果）为输入做 in-memory 过滤；counts 在 items/search/currentTab/kindFilter 任一变化时自动随 @Published 触发 UI 刷新。",
        "HistoryPanelView.tabBar 的两个 tag 文字分别从 `Text(\"All\")` / `Text(\"Pinned\")` 改为 `Text(\"All\") + Text(verbatim: \" (N)\")` 的 concat 形式——『All』『Pinned』继续走 LocalizedStringKey 本地化，括号+数字由 verbatim 拼接不参与翻译。",
        "KindChip 加 `count: Int` 入参，HStack 末尾多一个 `Text(\"\\(count)\")`，`.monospacedDigit()`，未选中时 secondary 色、选中时白字 0.7 opacity；KindChipBar 从 `@Binding selection` 扩展到 `@Binding selection + count: (ClipKind?) -> Int` 闭包，HistoryPanelView 调用时传 `{ viewModel.kindChipCount(for: $0) }`。",
        "条数语义：tab 的 count 反映『切到该 tab 后会看到的条数』（保留当前 kindFilter）；kind chip 的 count 反映『切到该 kind 后会看到的条数』（保留当前 tab）——切换交互可预测。"
      ],
      "acceptance": [
        "打开 HistoryPanel：顶部 All 标签显示『全部 (N)』、Pinned 显示『已固定 (M)』；KindChipBar 每个 chip 展示各自条数，nil→All chip 显示当前 tab 全量。",
        "输入搜索关键字或切换 kindFilter：tab 数字同步变化；切换 tab：kind chip 数字同步变化。",
        "Accessibility Inspector 或 `⌘\\`` 切换器可以识别 HistoryPanel 的 window title 为『剪贴板』（或系统语言对应本地化）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S51: restore history panel window title + show counts on tabs and kind chips"
    },
    {
      "id": "S52",
      "title": "Preferences 标题栏真正变玻璃",
      "goal": "S50 回到标准 titled window 后，标题栏是 macOS 默认 chrome（也是系统玻璃，但和 regularMaterial body 观感有细微差别）。本 slice 用 `fullSizeContentView + titlebarAppearsTransparent` 让 body 的 regularMaterial 真正流过标题栏；避开 S49 的 safe area 陷阱——改用 VStack 手动为标题栏腾 28pt 空间，不再依赖 SwiftUI safe area inset。",
      "deliverables": [
        "PreferencesWindowController: styleMask 加回 `.fullSizeContentView`；重新打开 `titlebarAppearsTransparent = true`；contentRect 从 520x380 bump 回 520x408；保留 `isOpaque = false / backgroundColor = .clear / hasShadow = true`。",
        "PreferencesView body 重构为 VStack(spacing: 0) { Color.clear.frame(height: 28); Divider(); TabView { ... } }，总 `.frame(width: 520, height: 408) + .background(.regularMaterial)` —— 标题栏 28pt 靠显式 Color.clear 占位（标题栏作为透明 overlay 穿透 regularMaterial），Divider 在 y=28 标记分界，TabView 填剩余 520x379。",
        "不使用 safe area / ignoresSafeAreaEdges / safeAreaInset —— 布局完全由 VStack + 明确常量控制，避免 S49 的 SwiftUI 视 `.frame(width:height:)` 为固定盒子后忽略 safe area 的问题。",
        "NSHostingController 保持默认 sizingOptions —— 显式 .frame(520, 408) 的 intrinsic 自然推送 preferredContentSize=520x408，窗口不塌陷。"
      ],
      "acceptance": [
        "⌘, 打开 Preferences：整窗背景是连续的 regularMaterial 玻璃，标题栏与 body 观感一致；traffic light / 窗口标题文字都在 title bar 正确位置。",
        "TabView tab 栏位于 y=28 分隔线下方，『快捷键』等 section 标题不再被标题栏遮挡。",
        "Cap TextField、Hotkey Recorder、Language Picker 等控件均正常工作。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S52: unified glass title bar via fullSizeContentView + manual 28pt VStack spacer"
    },
    {
      "id": "S53",
      "title": "KindChipBar 补上 Mixed chip 消除 All 数量差",
      "goal": "S51 开始显示各类型条数后，用户发现 All=51 但 Text+Image+File+Rich=38，差 13。原因是 ClipKind 里还有 `.mixed` 类型（含多种 pasteboard type 的条目，如 RTF+image 同时出现），但 KindChipBar 没对应 chip，13 条 `.mixed` 被漏掉。",
      "deliverables": [
        "KindChipBar 新增第 6 个 chip：『Mixed』，icon `square.stack`，value `.mixed`——保持与 ClipRowView / ClipPreviewView 的 kindIcon / kindLabel 一致。",
        "『Mixed』的本地化字符串 (`\"Mixed\"` key) 已经存在于所有 7 个语言包（S32 引入），无需新增；直接 `Text(LocalizedStringKey(\"Mixed\"))`。",
        "验证 `allTabCount` 与各 chip count 之和一致：Text + Image + File + Rich + Mixed = All。"
      ],
      "acceptance": [
        "打开 HistoryPanel：All chip 数字 = Text + Image + File + Rich + Mixed 之和；全部分类皆可见。",
        "点击 Mixed chip：仅显示 `.mixed` 类型条目。",
        "其它语言（zh-Hans 显示『混合』、日文『複合』等）label 正确。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S53: add Mixed chip so kind counts sum to All"
    },
    {
      "id": "S54",
      "title": "修复语言 Picker 点击不展开",
      "goal": "S52 引入 `VStack { Color.clear.frame(height: 28); Divider(); TabView }` 后，语言 Picker 点击无反应——大概率是 Color.clear 或 VStack 结构干扰了 NSPopUpButton 的 popover hit-test/定位。换成 `.padding(.top, 28)` 直接给 TabView 留出标题栏空间，不再引入中间子视图；Divider 用 `.overlay(alignment: .top)` 单独绘制，不进入 VStack 链。",
      "deliverables": [
        "PreferencesView body 改为：`TabView { ... }.padding(.top, 28).frame(width: 520, height: 408).background(.regularMaterial).overlay(alignment: .top) { Divider().padding(.top, 28) }` —— 用 padding 代替 VStack 的 Color.clear spacer，TabView 是 body 唯一的交互容器；Divider 作为 overlay 绘在 y=28 不影响 hit-test。",
        "不动 PreferencesWindowController 的 styleMask / titlebarAppearsTransparent / contentRect（S52 已就位）。"
      ],
      "acceptance": [
        "打开 Preferences → General → Language：点击 Picker 能正常弹出下拉菜单，选项可选。",
        "窗口观感仍是连续玻璃；Divider 可见于 y=28（标题栏下沿）。",
        "Hotkey Recorder、Cap TextField 等其它交互控件正常。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S54: use padding + overlay divider so Language picker opens"
    },
    {
      "id": "S55",
      "title": "顶部 All/Pinned 条数不随 kindFilter 变化",
      "goal": "S51 的 allTabCount / pinnedTabCount 会同时应用 kindFilter，导致切到 Image chip 时顶部 All 从 51 掉到 6，直觉不对——顶部 tab 应该是『库里一共多少条』的总览，只受搜索约束，不受当前类型选择约束。",
      "deliverables": [
        "HistoryPanelViewModel.allTabCount 简化为 `items.count`——直接反映当前 search 结果的总条数，与 kindFilter 无关；切换 kind chip 时数字保持。",
        "HistoryPanelViewModel.pinnedTabCount 简化为 `items.filter { $0.pinned }.count`——pinned 条目的总数，忽略 kindFilter。",
        "kindChipCount(for:) 保持 S51 行为（仍看 currentTab）——切 tab 时 chip 数字更新是合理的。",
        "kindMatches 辅助方法若不再被其它 count 使用则删除；tabMatches 保留（kindChipCount 仍在用）。"
      ],
      "acceptance": [
        "任意 search 下选中 kind chip：顶部『All (N)』『Pinned (M)』数字不变。",
        "清空搜索：All 回到库总条数；Pinned 回到库内 pinned 条目数。",
        "切换 tab：kind chip 数字仍然会随之变化（Pinned tab 下，Text chip 显示 pinned 的 text 条数）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S55: top All/Pinned counts no longer follow kind chip selection"
    },
    {
      "id": "S56",
      "title": "ClipKind 按优先级分类，不再生成 mixed",
      "goal": "当前 `ClipKind.infer` 只要粘贴板上出现 2 种 primary（把 html 当 text）就判 `.mixed`：浏览器图片复制（PNG + HTML wrapper）被归为 Mixed，但用户认为应该是 Image；富文本（RTF + text）被归为 Mixed，视觉上和 Text 区分不开。用优先级策略替代：哪个最富内容就归哪类。",
      "deliverables": [
        "Core/Sources/ClipboardCore/ClipKind.swift 中 `infer(from:)` 改为按优先级选：依次检查 file / image / rtf / text（html 仍算 text），先命中的胜出。不再输出 `.mixed`。",
        "`.mixed` enum case 保留，但仅为数据兼容——现存 JSON 里已有的 `.mixed` 条目仍可解码；新条目不会再被分到 mixed。",
        "App/KindChip.swift 的 Mixed chip 保留（旧数据仍可通过它过滤）；随着历史轮转，Mixed 数字会自然归零。",
        "Core/Tests/ClipboardCoreTests/ClipItemCodableTests.swift 的 `testKindInference` 更新：原先的 `.rtf + .png → .mixed` 断言改为 `→ .image`（image 优先级高于 rtf）；补几个优先级断言：file > image > rtf > text（html 等同 text）。"
      ],
      "acceptance": [
        "从浏览器复制一张图片：HistoryPanel 中显示 kind = Image，不再是 Mixed。",
        "从富文本编辑器复制一段带格式文字（RTF + text）：kind = Rich Text (rtf)，不再 Mixed。",
        "从 Finder 复制一个文件（file-url）：kind = File。",
        "现存 .mixed 条目仍可在 Mixed chip 筛出；UI 保持工作。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S56: priority-based kind inference (file > image > rtf > text), stop producing .mixed"
    },
    {
      "id": "S57",
      "title": "ClipKind 优先级调整：image > file",
      "goal": "S56 的优先级 `file > image` 让 Telegram / Messages / 某些截图工具的图片粘贴（同时有 `public.png` 和指向临时文件的 `public.file-url`）被归为 File——用户直觉是 Image。把优先级调成 `image > file > rtf > text`：只要 payload 里有真正的图像数据就归 Image；仅 file-url（无 image 数据，典型是 Finder 文件复制）才归 File。",
      "deliverables": [
        "Core/Sources/ClipboardCore/ClipKind.swift `infer(from:)` 的优先级分支顺序从 `file → image → rtf → text` 改为 `image → file → rtf → text`；更新函数内的文档注释说明原因（Telegram/Messages 这类 temp-file-url + image 的粘贴）。",
        "Core/Tests/ClipboardCoreTests/ClipItemCodableTests.swift `testKindInferencePriority` 中原 `['public.file-url', 'public.png'] → .file` 的断言改为 `→ .image`，并加一条 `['public.file-url'] → .file` 明确 Finder 纯文件复制的场景。"
      ],
      "acceptance": [
        "从 Telegram 复制一张图片 → HistoryPanel 显示 Image。",
        "从 Messages 复制图片 → Image。",
        "从 Finder Copy 一个图片/非图片文件（仅 file-url，无 image 数据）→ File。",
        "从浏览器复制图片（png + html）→ Image（同 S56）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S57: image beats file in kind priority (Telegram/Messages image pastes)"
    },
    {
      "id": "S58",
      "title": "PreviewBuilder 不再把图片 clip 的 HTML wrapper 当 preview",
      "goal": "浏览器复制图片时 `public.html` 是个 `<meta charset=...><img src=...>` 的包装，plain text / rtf 都是空的。当前 PreviewBuilder.extractText 在 plain/rtf 都缺时 fallback 到 HTML 原文，于是 preview 字段塞满了 HTML 源码——行视图显示 `<meta charset=...>` 而不是图像。修 build() 的优先级：plain/rtf → image sentinel → html；纯 HTML 粘贴的情况下再用 `NSAttributedString` 把 HTML 解析成纯文本。",
      "deliverables": [
        "把 `PreviewBuilder.extractText(from:)` 拆成两个辅助：`extractPlainTextOrRtf` 只看 utf8/plain/string/rtf；`extractHtmlPlainText` 通过 NSAttributedString(data:options:documentType:.html,.characterEncoding:utf8) 把 HTML 转成 plain text，失败时返回原文作兜底。",
        "`PreviewBuilder.build(for:limit:)` 顺序调整为：plain/rtf → `<image>`（若有 image payload）→ HTML 纯文本 → file name → 空串。这样有图像数据的 clip 永远返回 `<image>` sentinel，绕过 HTML 包装；纯 HTML 粘贴才会走 HTML→plain text 路径。",
        "行为不影响纯图像 clip（仍然 `<image>`）/ 纯文本 / RTF / 文件 / 空 clip；现有 PreviewBuilder 行为文档在注释里简要说明。"
      ],
      "acceptance": [
        "从浏览器复制一张图片 → HistoryPanel 列表行的预览文字为 `<image>`，不再是 HTML 源码。预览区（ClipPreviewView.imageView）正常渲染图像。",
        "从纯 HTML 源（无 plain/rtf/image）的场景 → preview 为 HTML 解析后的纯文本，不再是带标签的原文。",
        "纯文本 / RTF / 文件 / 图片截图等场景 preview 行为不变。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S58: preview uses <image> sentinel for image clips, parses html for text-only clips"
    },
    {
      "id": "S59",
      "title": "Image payload 无条件优先：图片 clip 的 preview 不再受 plain text 牵连",
      "goal": "S58 把 image sentinel 放在 plain/rtf 之后，以为 plain text 是『有用的描述』；但浏览器实测在 `public.utf8-plain-text` 里也塞了 `<meta charset=...><img src=...>` 的 HTML 源码（为了兼容只读 plain text 的应用），于是图片 clip 的 preview 仍然显示 HTML 源码。只要 pasteboard 有 image payload，preview 就应该是 `<image>` sentinel，与任何文字包装无关。",
      "deliverables": [
        "`PreviewBuilder.build(for:limit:)` 把 `hasImageType` 检查移到最顶——有 image payload 就立刻返回 `<image>`，不再读 plain/rtf/html。现有优先级剩余链条变成：plain/rtf → html 纯文本 → file name → 空串。",
        "注释更新说明原因：复制 image 时用户的意图是图片本身，剪贴板上其它附带的文字只是源 app 为兼容性塞的元数据。"
      ],
      "acceptance": [
        "从任意网页复制图片 → HistoryPanel 行预览文字为 `<image>`，不再是 `<meta...` HTML。",
        "从 Telegram/Messages 复制图片 → 同上。",
        "纯文本 / RTF / 纯 HTML / 文件 clip 的预览行为不变（没有 image payload 时逻辑照旧）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S59: image payload preempts any text preview"
    },
    {
      "id": "S60",
      "title": "替换 App 图标（真正的光栅艺术）",
      "goal": "S30 起用的 `tools/generate-icon.swift` 是 Core Graphics 程序化绘制的占位图标（蓝紫方块 + 白色剪贴板卡）。现在有正式设计稿——紫色 3D 剪贴板 + 内容条目（文本/图片/代码/链接）+ 右下粘贴版徽章——把它作为官方图标。",
      "deliverables": [
        "新增 `tools/icon-source.png` 作为 1024x1024 光栅源（单一真源）。",
        "删除 `tools/generate-icon.swift`——程序化图标不再是真源。",
        "Justfile `icon` target 改为 bash shebang 脚本：从 `tools/icon-source.png` 用 `sips -Z` 生成 10 个尺寸写入 `App/Assets.xcassets/AppIcon.appiconset/`。",
        "`AppIcon.appiconset` 里的 10 个 PNG 更新为新图。`Contents.json` 不动（文件名/尺寸约定不变）。"
      ],
      "acceptance": [
        "`just build && open build/Build/Products/Debug/Clipboard.app` → Dock/菜单栏/Preferences 窗口标题栏等处显示新紫色剪贴板图标。",
        "`just icon` 可以从 `tools/icon-source.png` 再生 10 个 PNG；替换源图后重跑一次即可刷新全尺寸。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S60: replace programmatic icon with designed raster artwork"
    },
    {
      "id": "S61",
      "title": "小尺寸图标清晰化：Core Image Lanczos + UnsharpMask",
      "goal": "S60 用 `sips -Z` 把 1024x1024 的复杂 3D 图标降到 16/32/64 这些小尺寸时失去锐度，在 Activity Monitor、Finder 列表视图里糊成一团紫色。改用 Core Image 的 CILanczosScaleTransform 做高质量重采样，并在 ≤128px 档追加 CIUnsharpMask 恢复边缘对比。",
      "deliverables": [
        "新增 `tools/resize-icon.swift`：读 `tools/icon-source.png`，按 10 档 AppIcon 尺寸逐张走 CILanczosScaleTransform → (小尺寸) CIUnsharpMask → 写入目标 PNG。intensity 分档：≤32px 用 0.85、≤128px 用 0.5、其它不锐化；radius 固定 0.6。",
        "Justfile `icon` target 改为调用该 swift 脚本：`swift tools/resize-icon.swift tools/icon-source.png App/Assets.xcassets/AppIcon.appiconset`。",
        "`AppIcon.appiconset` 的 10 个 PNG 全部重新生成。"
      ],
      "acceptance": [
        "Activity Monitor 中 Clipboard 进程图标比之前清晰可辨，剪贴板的大致形状 / 紫色主色可见。",
        "Finder 列表视图 / Get Info 窗口中的各尺寸图标都更锐。",
        "大尺寸（256/512/1024）观感不变——大尺寸不进 UnsharpMask 分支。",
        "`just icon` 幂等：多次跑输出一致。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S61: high-quality icon resampling via CILanczos + UnsharpMask at small sizes"
    },
    {
      "id": "S62",
      "title": "Icon 按内容 alpha bbox tight-crop 再缩放",
      "goal": "`tools/icon-source.png` 画布是 1024x1024，但紫色剪贴板主体的实际内容区（alpha > 0）只占 663x718 ≈ 65-70%。这导致每个导出尺寸上图标本身更小——Activity Monitor / Finder 看着糊。修 tools/resize-icon.swift 先按 alpha 检测 bbox、扩成方形、裁剪到内容区，再走 Lanczos；图标本身在每个尺寸都填满画布，小尺寸下收益显著。",
      "deliverables": [
        "`tools/resize-icon.swift` 在 Lanczos 之前新增：读源图 bitmap 数据 → 按 `alpha > 8` 阈值扫描 4-sample pixel 求 minX/minY/maxX/maxY → 以 bbox 中心构造方形 crop 区（side = max(bw, bh)） → `CIImage.cropped(to:)` + 平移到原点；fallback 为整幅画布（如果检测不到 alpha 信息）。",
        "后续 Lanczos scale 基于 `targetPx / cropSide` 计算；UnsharpMask 行为不变。",
        "脚本源载入从 `CIImage(contentsOf:)` + `NSBitmapImageRep(contentsOfFile:)` 改为 `Data(contentsOf:)` + `CIImage(data:)` + `NSBitmapImageRep(data:)`（前者 API 不存在，最新 SDK 下编译报错）。",
        "打印行增加：`source 1024x1024, content bbox (180,150)-(842,867) = 663x718, square crop 718x718` 方便以后换源图时 sanity check。",
        "10 个 AppIcon PNG 全部重新生成；Justfile `icon` target 不变。"
      ],
      "acceptance": [
        "跑 `just icon` 输出包含 bbox / crop 调试行，10 个 PNG 都写入。",
        "Activity Monitor / Finder list view / Get Info：图标本身占据整个显示槽，不再被空白边压小。",
        "大尺寸（256/512/1024）仍视觉完整——cropSide 只是去掉 alpha=0 的边，没切掉任何可见像素。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S62: tight-crop icon source by alpha bbox so it fills the canvas at every size"
    },
    {
      "id": "S63",
      "title": "AI summary 基础设施（数据模型 + 能力检测 + 偏好开关 + UI 槽位）",
      "goal": "系统性引入 AI summary：未来按设备能力分别用 Vision（macOS 13+ 全可）/ Writing Tools（macOS 15.1+）/ Foundation Models（macOS 26+ Apple Silicon）为图片/文本/文件生成摘要。本 slice 只建地基：数据模型能承载 summary、偏好能打开/关闭、ClipPreviewView 有展示槽位、能力检测能运行。不接任何实际的生成器——那是 S64/S65/S66 的工作。不保证与旧 history.json 向前兼容（用户接受 clear history）。",
      "deliverables": [
        "Core: 新 enum `SummarySource: String, Codable { case vision, writingTools, foundationModels }`（放在 ClipItem.swift 或新 file）。",
        "Core: ClipItem 新增 `summary: String?` 与 `summarySource: SummarySource?` 字段，均默认 nil；`init(...)` 跟上，Codable 自动合成。Payload/其它字段不动。",
        "Core: Preferences 新增 `summariesEnabled: Bool = true`（master）、`allowImageSummaries: Bool = true`、`allowTextSummaries: Bool = true`、`allowFileSummaries: Bool = true` 四个字段；PreferencesStore 的 Keys / current / save / reset 同步扩充，走 UserDefaults。",
        "App: 新文件 `App/AICapability.swift`——静态 `isVisionAvailable`（true）/ `isWritingToolsAvailable`（`#available(macOS 15.1, *)` 判断）/ `isFoundationModelsAvailable`（macOS 26.0 + `arch(arm64)` 编译 guard + 运行时）。S63 不引入 FoundationModels framework 依赖，FM 判断返回 `false` 占位，等 S66 接真框架。",
        "App: PreferencesView General tab 追加 Section『AI Summaries』：一个 master Toggle `summariesEnabled`，下面三个子 Toggle（图片/文本/文件）。子开关在 master 关闭或对应能力不可用时 disabled。Section 下方有一条 secondary caption 说明本机检测到的能力（例如『Vision on-device only』/ 『Writing Tools available』/ 『Foundation Models requires macOS 26+ Apple Silicon』）。",
        "App: ClipPreviewView 在 header 与 content 之间插入 `summarySection(item:)`——当 `item.summary` 非空时显示：小 `sparkles` icon + `summary` 文本 + 右上角 `summarySource` 小徽章（vision / writing tools / FM），所有文字 secondary 色、caption 字号；为空则不绘制该 section。",
        "Core 测试：ClipItemCodableTests 加一条 round trip 验证新 summary + summarySource 字段；PreferencesStoreTests 加一条验证四个新 pref 的默认值与 round trip。",
        "en.lproj/Localizable.strings 补新 key：AI Summaries / Enable AI summaries / Summarize images / Summarize text / Summarize files / Vision on-device only / Writing Tools available / Requires macOS 26+ Apple Silicon。其它 6 种语言的翻译走 S64+ 的落地 slice。"
      ],
      "acceptance": [
        "`just test` 新增 2 条测试绿；存量 80+ 测试不受影响。",
        "Preferences 打开 → General → 能看到『AI Summaries』分区 + master + 3 子 toggle，依赖的能力未满足时子开关变灰。",
        "ClipItem.summary 手动填值后，ClipPreviewView 里能看到新 summary section（icon + 文案 + source 徽章）；summary 为 nil 则该 section 隐藏。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S63: AI summary infrastructure (data model, capability detection, prefs, UI slot)"
    },
    {
      "id": "S64",
      "title": "Vision 图片摘要：OCR + Classify 自动生成 summary",
      "goal": "S63 的地基上接第一条生成器路径。新图片 clip 入库后异步跑 `VNClassifyImageRequest`（top-N 类别）+ `VNRecognizeTextRequest`（嵌入文字 OCR），拼一句简短摘要回写到 `ClipItem.summary`，source 标 `.vision`。Vision 在 macOS 13+ 全设备可用，所以这是 baseline，后续 Writing Tools / Foundation Models 接进来时会在同一个 coordinator 里按能力择优。",
      "deliverables": [
        "Core: `ClipStore` 协议新增 `updateSummary(id: UUID, summary: String, source: SummarySource) async`；`InMemoryClipStore` 与 `JSONSnapshotClipStore` 同步实现——mutate 对应 item、yield `.updated(item)` 事件；非 sensitive 条目 scheduleWrite 持久化。协议语义与 togglePin 对齐。",
        "App: 新文件 `App/VisionImageSummarizer.swift` —— 从 Data 读 CGImage、用一个 `VNImageRequestHandler` 同时 perform `[VNClassifyImageRequest, VNRecognizeTextRequest(.accurate)]`。过滤 classifier 置信度 ≥ 0.4 取前 3，OCR 结果拼成字符串截断到 160 字符。返回格式 `\"dog, mammal. Contains text: <ocr>\"` 或仅其中一部分；都为空时返回 nil。在 `DispatchQueue.global(qos: .utility)` 异步调度后通过 `withCheckedContinuation` 回到 async 调用方。",
        "App: 新文件 `App/SummaryCoordinator.swift` —— @MainActor，监听 `store.events`，只在 `.inserted(item)` 分支触发；过滤 `prefs.summariesEnabled && !item.sensitive && item.summary == nil`；按 `item.kind` 分派：`.image` 再校验 `prefs.allowImageSummaries && AICapability.isVisionAvailable`，加载 image payload 数据（通过 `PayloadResolver.data(for:)`），喂给 VisionImageSummarizer，拿到 summary 后回调 `store.updateSummary(...)`。`.text/.rtf/.mixed/.file` 当前 no-op，留给 S65/S66。",
        "App: `AppWiring` 新增 `summaryCoordinator: SummaryCoordinator?` 实例变量；`start()` 里构造并 `.start()`；`stop()` 里 `.stop()` 并置 nil。生命周期随 App 启停。",
        "Core 测试：新增 `Core/Tests/ClipboardCoreTests/UpdateSummaryTests.swift` 验证 `updateSummary` 的三条性质——写入成功 / 替换成功 / 未知 id no-op；Vision 本身的端到端测试不在 CI 可稳定跑，留作手动验证。"
      ],
      "acceptance": [
        "启动 App 后复制任何一张包含文字的截图（比如代码块截图）→ 数秒内 HistoryPanel 的预览区出现 sparkles icon + 一句『top-labels. Contains text: …』+ 右上 `Vision` 徽章。",
        "复制一张不含可识别文字的图片（风景、人像）→ 预览区出现 sparkles + 分类标签（例如『outdoor, nature』）；若两者都空则不显示 summary section（不污染 UI）。",
        "关闭 Preferences 里的 master 开关或『Summarize images』子开关后，新增 image clip 不再生成 summary（但已有 summary 保留）。",
        "sensitive clip / 非 image kind clip 永远不跑 Vision（SummaryCoordinator 守卫逻辑）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿，测试数从 82 升至 85。"
      ],
      "commit_message": "S64: vision image summarizer wired through ClipStore.updateSummary + SummaryCoordinator"
    },
    {
      "id": "S65",
      "title": "NaturalLanguage 文本摘要（替代原计划中的 Writing Tools 层）",
      "goal": "原计划里 S65 用 Writing Tools 做文本摘要，但 macOS 15.1 的 `WritingToolsCoordinator` 只有 UI 集成路径（绑 NSTextView + 用户触发），没有 headless 文本 API，不适合后台自动摘要。改用 `NaturalLanguage` 框架——语言检测 + 命名实体抽取——作为文本侧 baseline；跟 Vision 同处 macOS 13+ 永远可用的 tier；S66 Foundation Models 上线后自动取代这两条 baseline。",
      "deliverables": [
        "Core: `SummarySource.writingTools` 重命名为 `.naturalLanguage`，反映实际使用的 framework。",
        "App: `AICapability.isWritingToolsAvailable` 重命名为 `isNaturalLanguageAvailable`，都永为 true（macOS 13+ 一直可用）；顶部 doc 注释更新说明 Writing Tools 的 pivot 原因。",
        "App: 新文件 `NaturalLanguageTextSummarizer.swift` —— `NLLanguageRecognizer` 检测主语言 + `NLTagger(tagSchemes: [.nameType])` 抽 personalName / placeName / organizationName；返回 `\"Language · E1, E2, E3\"` 或仅 `Language`；文本 < 100 字符或无任何产出返回 nil。本地化语言名用 `Locale.current.localizedString(forLanguageCode:)`，中文用户看到『英语 · Apple、Tim Cook』而不是『English · …』。",
        "App: `SummaryCoordinator` 新增 `summarizeText(_:)` 路径——`.text/.rtf/.mixed` 分派到文本引擎；`extractPlainText(from:)` 先扫 utf8/plain/string/NSStringPboardType 四种 payload，找不到再解析 public.rtf（via `NSAttributedString(..., documentType: .rtf)`）。非 sensitive 才跑；加 `import AppKit` 以使用 NSAttributedString。",
        "App: `PreferencesView` 的 `aiCapabilityCaption` 从 3 分支简化为 2 分支：Foundation Models 可用写 『FM available』；否则写 『Vision + NaturalLanguage on-device; richer summaries need macOS 26+ Apple Silicon』。『Summarize text』toggle 的 disabled 条件改用 `isNaturalLanguageAvailable`。`ClipPreviewView.sourceBadge` 为 `.naturalLanguage` 返回 `NL`。",
        "App/en.lproj: 撤掉过时的 Writing Tools caption，加新 Vision+NL caption。"
      ],
      "acceptance": [
        "复制一段含人名 / 地名 / 组织名的文本（>= 100 字符）→ 数秒内 HistoryPanel 预览出现 sparkles + `语言 · 实体,实体` + 右上 `NL` 徽章。",
        "复制一段短文本（< 100 字符）→ 不生成 summary，preview 区正常显示正文。",
        "复制富文本（RTF）→ 同样能摘要，因为 extractPlainText 会 fallback 到 RTF 的 attributed string 平化。",
        "关闭『Summarize text』→ 新文本 clip 不再生成；图片仍然走 Vision。",
        "sensitive text clip 永远不跑 NL。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests 保持）。"
      ],
      "commit_message": "S65: NaturalLanguage text summarizer (Writing Tools isn't viable for background summarization)"
    },
    {
      "id": "S66",
      "title": "Foundation Models 摘要（LLM，macOS 26+ Apple Silicon）",
      "goal": "上层 tier：`FoundationModels` 框架的 on-device ~3B LLM。对文本 clip 产出一句真正的自然语言概括（取代 NL 的实体列表），对文件 clip（PDF / 文本）也能吃进去做摘要——这是 baseline 路径做不了的。图像 clip 暂时仍走 Vision（FM 的多模态 API 这一轮先不碰）。编译期 `#if canImport(FoundationModels)`，运行期 `@available(macOS 26.0, *)` + `SystemLanguageModel.default.availability` 双门，在老系统上 FM 相关符号永远不加载。",
      "deliverables": [
        "新文件 `App/FoundationModelsSummarizer.swift` —— `@available(macOS 26.0, *)` 的 struct。`summarize(text:)` 构造 `LanguageModelSession(instructions:)`（含『单句 / 无前言 / <160 字符』指令），调用 `session.respond(to:)`，失败或空返回 nil。`summarizeFile(url:)` 对 `.pdf` 走 PDFKit 抽文字、其它尝试 UTF-8 加载，再交给 `summarize(text:)`。最长截断 200 字符防御。",
        "`AICapability.isFoundationModelsAvailable` 用真实可用性判断取代 S63 占位：走 `FoundationModelsAvailability.isActive`（`@available(macOS 26.0, *)` 私有 enum 里 switch `SystemLanguageModel.default.availability`）。`#if arch(arm64)` + `#available` 双层守卫。",
        "`SummaryCoordinator.handle(_:)` 的 dispatch 调整：`.text/.rtf/.mixed` 不再直接走 NL，`summarizeText(_:)` 内部先试 FM（可用时）、失败或不可用 fallback 到 NL。`.file` 分支启用：`prefs.allowFileSummaries && isFoundationModelsAvailable` 时跑 `summarizeFile(_:)`。图像分支不变。",
        "新 `extractFileURL(from:)` —— 找第一个 `public.file-url` payload、用 PayloadResolver 读 bytes、UTF-8 解成 URL。",
        "`#if canImport(FoundationModels)` 保护 import（缺 SDK 的环境也能编译），FM 相关 struct / enum 整体被 `@available(macOS 26.0, *)` 隔离；macOS 13–25 上从 AICapability 的 `#available` 分支直接返回 false，永不触达 FM 符号。"
      ],
      "acceptance": [
        "macOS 26+ Apple Silicon 机器 + Apple Intelligence 已启用：复制一段长文本（> 100 字符）→ 预览出现 sparkles + 一句真正的英文/中文摘要 + 右上 `FM` 徽章。",
        "同一机器上复制一个 .pdf 文件（Finder 拖入）→ 预览出现 FM 生成的 PDF 内容摘要。",
        "非 macOS 26 或非 Apple Silicon：FM 代码永不执行，文本仍走 NL（`NL` 徽章）、图像 Vision、文件无 summary。",
        "关闭『Summarize files』子开关 → 文件 clip 不跑 FM。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 保持）。"
      ],
      "commit_message": "S66: Foundation Models summarizer for text + files (macOS 26+ Apple Silicon)"
    },
    {
      "id": "S67",
      "title": "VisionImageSummarizer 保证有输出",
      "goal": "用户反馈：新截图里有的显示 OCR 文字摘要、有的什么都没显示。根因是 VisionImageSummarizer 的分类器置信度阈值 0.4 对 UI / 代码类截图太严——VNClassifyImageRequest 对合成内容信心普遍偏低，常常一个 label 都不过阈；若 OCR 也没识别到任何文字，方法就返回 nil，UI 里整个 summary section 被隐藏。修法：降阈 + 最终兜底维度占位。",
      "deliverables": [
        "VisionImageSummarizer.minClassifierConfidence 从 0.4 降到 0.1——screenshots 类别更容易命中 top-3；仍只取前 3。",
        "build() 的末尾分支：labels + OCR 都空时，不再返回 nil，改返回 `Image (width×height)` 占位，读取自 CGImage 尺寸——用户至少看到一条带 `Vision` 徽章的 summary，证明 pipeline 运行过；维度本身也是弱信息（1920×1080 暗示是标准屏截图）。",
        "Vision.perform() 直接抛错的分支也补同样的 dimensions-only 占位，进一步保证 image clip 必有输出。"
      ],
      "acceptance": [
        "复制任意新截图（UI / 代码 / 风景 / 纯色图案）→ HistoryPanel 预览一定有 sparkles + summary + Vision 徽章；弱信息场景至少看到 `Image (w×h)`。",
        "有 OCR 文字或高信心分类时，行为不变（仍然返回原来的 labels + text）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S67: vision summarizer always returns something (lower threshold + dimensions fallback)"
    },
    {
      "id": "S68",
      "title": "事件流 fanout + NL fallback + pipeline 日志：修 summary 完全看不到",
      "goal": "用户反馈：剪贴板有新条目但 AI summary 完全不显示。加日志后发现三个问题叠加：(1) `ClipStore.events` 是单一 AsyncStream，HistoryPanelViewModel / AppWiring.storeEventObserver / SummaryCoordinator 三个 consumer 抢同一个 buffer，每个事件只被一个收到——SummaryCoordinator 基本抢不到；(2) NaturalLanguage summarizer 对无命名实体的英文短文本只返回语言代码 `en`（2 字符），体验上等同没有 summary；(3) pipeline 任何环节静默失败时无法排查。",
      "deliverables": [
        "Core: 新增 `StoreEventBroadcaster`——内部管理 `[UUID: AsyncStream.Continuation]`，`subscribe()` 发新流、`yield(_:)` 对所有活跃 continuation 广播、`onTermination` 清理。`JSONSnapshotClipStore` / `InMemoryClipStore` 删掉单一 `eventsContinuation`，换成 `broadcaster = StoreEventBroadcaster()` + `events: AsyncStream<StoreEvent> { broadcaster.subscribe() }` 计算属性；所有 `.yield(...)` 改走 broadcaster；init 不再 makeStream。每个 consumer 现在拿到独立 stream、事件被完整广播。",
        "App: `NaturalLanguageTextSummarizer.summarize(text:)` 语义调整——不再把『仅语言名』作为有效输出。新行为：①language + entities → `English · Apple, Tim Cook`；②entities 空 → 用 `NLTokenizer(unit: .sentence)` 抽首句、截到 120 字符加省略号、前缀加语言；③language 也无 → 只返回首句；④实在没东西 → nil。即永不返回 2 字符的语言代码占位。",
        "App: `SummaryCoordinator` 加 `Log.ui.info` 诊断——`summary.coordinator.start` 启动信号；`summary.event.inserted` 每次收到 `.inserted`；`summary.skip` 记录 gate 原因（主开关 / sensitive / 已有 summary / 子开关 / 能力）；`summarizeImage` / `summarizeText` 记录 start / empty / done + 长度或字节数；`summary.image.noPayload` 列出可见 pasteboard types 便于诊断分类问题。",
        "harness + commit 记录 pre-existing 多 consumer race 本存在（vm / storeEventObserver 并存），但直到 S64 的 SummaryCoordinator 才暴露——因为前两者的事件类型（.updated 全量刷新 / .corrupted 偶发）对丢失不敏感，而 .inserted 对 summarization 是关键。"
      ],
      "acceptance": [
        "`just logs` 或 `log show --last 2m --predicate 'subsystem == \"com.clipboard.app\"'`：启动后能看到 `summary.coordinator.start`；每个新 clip 都能看到 `summary.event.inserted` + 后续 `summary.*.start` + `summary.*.nl/fm/done` 或 `summary.*.empty`。",
        "复制一段含人名地名的英文长文本 → UI 看到 `English · Apple, John, NYC` 风格。",
        "复制一段无实体的文本（例如纯叙述） → UI 看到 `English · First sentence here…` 而不是 `en`。",
        "复制短文本（< 100 字符） → 不显示 summary row。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests 保持）。"
      ],
      "commit_message": "S68: broadcast store events + NL sentence fallback + pipeline logs (summaries now actually render)"
    },
    {
      "id": "S69",
      "title": "打开即选中首条 + 预览图 hover peek 弹窗",
      "goal": "两个易用性改进：(1) 面板开启时应默认选中最新（第一条）clip，用户不需要再按 ↓；resetSelection() 之前只在 tab 切换 / kindFilter 变更时触发，面板首开时未调用。(2) 预览区图片只 240pt 宽，看不清细节；鼠标悬浮时需要弹一个更大的 peek 预览。",
      "deliverables": [
        "HistoryPanel 新增 `var onDidOpen: (() -> Void)?` 回调；`toggle()` 在 `makeKeyAndOrderFront` / 监视器启动之后调用 `onDidOpen?()`。AppDelegate 创建面板时 `panel.onDidOpen = { [weak vm] in vm?.resetSelection() }`——每次开面板都回到首条 + 清空 kindFilter + 重置 scrollEpoch。",
        "ClipPreviewView.imageView 的 `Image(nsImage:)` 加 `.onHover { ... }` + `.popover(isPresented: $showPeek, arrowEdge: .leading)`；popover 内是一个更大的 `Image(nsImage:)`，`frame(maxWidth: min(naturalWidth, 600), maxHeight: min(naturalHeight, 600))`——不超出原图原尺寸也不超 600pt。",
        "`updatePeek(hovering:)`：hover-in 立即显示；hover-out 启动 250ms 延时再关，期间若 `isHoveringImage` 再次为 true 就保持打开，避免 popover 弹出瞬间因 pointer 路径轻微偏移被取消造成闪烁。",
        "ClipPreviewView 在 `item.id` 变更时额外 reset peek 状态（`showPeek=false`、取消 hideTask）——换条目时老 peek 立刻关。",
        "@State 新增三个字段：`isHoveringImage` / `showPeek` / `peekHideTask: Task<Void, Never>?`。"
      ],
      "acceptance": [
        "按全局快捷键打开剪贴板历史 → 列表第一条默认高亮，↓ 键从第二条开始移动。",
        "鼠标悬停在预览区的缩略图上约 100ms → 右/左侧弹出一个箭头指向图片的 popover，内嵌原尺寸图（上限 600x600）。",
        "鼠标离开缩略图 250ms 内不再悬停 → popover 关闭；若在延时内重新悬停回来，popover 保持开启。",
        "切换列表选中条目 → peek 立即消失；新条目 hover 重新触发。",
        "非图像 kind（text / file）不受影响。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests 保持）。"
      ],
      "commit_message": "S69: auto-select first row on panel open + hover peek popover for preview image"
    },
    {
      "id": "S70",
      "title": "去噪 Vision 输出 + 复制按钮 + 行内 summary fallback",
      "goal": "用户三处不满：(1) Vision summary 里 `document, screenshot. Contains text: ...` 冗余（『Contains text:』是 English-only prefix、label 跟 OCR 同时出现形成噪音）；(2) summary 无法复制出来单独使用；(3) 图片 clip 在历史列表里显示的是 `<image>` 占位符、毫无信息。",
      "deliverables": [
        "VisionImageSummarizer 输出优先级调整：OCR 文本非空 → 直接返回 trimmed OCR（去掉『Contains text:』前缀）；OCR 空而 labels 非空 → 返回 `label1, label2, label3`；都空 → `Image (width×height)`。OCR 和 labels 不再同一 summary 里并列，避免重复/噪音。",
        "ClipPreviewView.summarySection 新增复制按钮：`doc.on.doc` icon + 直接 `NSPasteboard.general.setString(summary, forType: .string)`；按钮 tooltip / accessibility label 均为 `Copy summary`；summary 文本加 `.textSelection(.enabled)` 允许拖选。复制后新字符串会被 app 自己的 monitor 捕获为新 clip——符合 clipboard manager 语义。",
        "ClipRowView.displayPreview：当 `item.kind == .image && item.summary != nil && !summary.isEmpty` 时返回 summary，代替原 preview 里的 `<image>` 占位；这样图片行在列表里展示实际识别到的 OCR/label 内容。非图像 kind 行为不变（preview 已是有效文本）。",
        "PreviewBuilder 不动——`<image>` 仍作为 preview 字段默认值，只是 UI 层在显示时优先展示 summary；保持 preview 作为 search 索引的基础兼容性。"
      ],
      "acceptance": [
        "复制一张带文字的截图 → 预览区 summary 只显示 OCR 文本（例如 `hello world 123`），不再有 `document, screenshot. Contains text:` 前缀。",
        "复制一张纯图片（无 OCR）→ summary 显示逗号分隔的 label（如 `nature, outdoor, mountain`）。",
        "summary 行右侧有复制图标；点击后 summary 文本进到剪贴板（会作为新 text clip 出现在历史顶部）。",
        "历史列表中图片 clip 行：已有 summary 时显示 summary 文本，无 summary 时仍显示 `<image>`。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests）。"
      ],
      "commit_message": "S70: vision summary drops noise prefix; summary gains copy button; image rows show summary instead of <image>"
    },
    {
      "id": "S71",
      "title": "Preferences 固定居中光标所在屏幕，不再跟随 panel",
      "goal": "用户反馈 Preferences 每次弹出都偏在右上方、很奇怪。S26/S27 引入的『以 panel 的 frame 为中心放置 Prefs』算法在某些 y-up 坐标组合下会叠出偏移，而用户对『跟随』并没有期待——固定居中光标所在屏幕的 visibleFrame 更简单也更符合预期。",
      "deliverables": [
        "`PreferencesWindowController.show(anchorRect:)` 改签为 `show()`；新私有方法 `centerOnCursorScreen(_ window:)` 基于 `NSEvent.mouseLocation` 找到当前光标所在 screen，在其 `visibleFrame.midX/midY` 上用 `setFrameOrigin` 放置 window —— 与 `HistoryPanel.positionAtCursorScreen` 一致。",
        "删除旧的 `positionWindow(_:centeredOn:)` 方法（无人再调用）。",
        "`AppDelegate.openPreferences` 去掉 `anchor = panel?.frame ?? ...` 三级 fallback；直接 `panel?.close()` + `installPreferencesIfReady()` + `preferencesController?.show()`。",
        "顺带删除 `AppDelegate.lastPanelFrame` 属性、`panel.onBeforeClose` 绑定、`fallbackAnchorRect()` 辅助方法——全部随 anchor 机制一并清空。",
        "`HistoryPanel.onBeforeClose` 回调、`notifyBeforeCloseIfVisible` 方法、close/orderOut 里对它的调用一并删除（只剩 `commitBeforeCloseIfNeeded`）。"
      ],
      "acceptance": [
        "在任意位置打开 Preferences（⌘, 从 panel / 菜单 / 重新打开 app）→ 窗口居中于光标所在 screen 的可视区。不再右上方偏移。",
        "多显示器场景：光标在副屏 → Prefs 出现在副屏正中；切到主屏 → 下次打开在主屏正中。",
        "HistoryPanel 仍然 centers 在光标 screen 的 visibleFrame（S2 以来行为不变）。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests）。"
      ],
      "commit_message": "S71: center preferences on the cursor screen's visibleFrame (drop panel-anchor tracking)"
    },
    {
      "id": "S72",
      "title": "强制 Preferences 居中（禁 state restoration + orderFront 后再定位）",
      "goal": "S71 改为光标所在屏幕居中后，用户反馈仍然偏右上。大概率 AppKit 的 state restoration 或 contentViewController 赋值触发的 resize 在我的 setFrameOrigin 之后再次挪动了 window，使我们的定位被覆盖。",
      "deliverables": [
        "`PreferencesWindowController.init`: `window.isRestorable = false`——关闭 AppKit 的 window frame 状态恢复，避免它在 orderFront 时把窗口拉回上次位置。",
        "`show()`: centerOnCursorScreen 从 orderFront 之前挪到之后——保证我们的 `setFrameOrigin` 是这一次 show 的最后一步、不会被 AppKit cascading/state restore 覆盖。顺序改为 reconcile → rebuildContent → setActivationPolicy → makeKeyAndOrderFront → activate → centerOnCursorScreen → makeFirstResponder(nil)。",
        "`centerOnCursorScreen` 末尾加 `Log.ui.info(\"prefs.center ...\")` 记录 screen.visibleFrame / mouse / size / 计算得到的 origin——`just logs` 能看到实际坐标便于继续排查。"
      ],
      "acceptance": [
        "⌘, 打开 Preferences → 窗口居中于光标所在屏幕可视区；反复开关多次都稳定居中。",
        "`just logs` 过滤 `prefs.center`：输出的 origin 大致等于 `(visible.midX - 260, visible.midY - 204)`。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests）。"
      ],
      "commit_message": "S72: disable Preferences state restoration + center after orderFront + log origin"
    },
    {
      "id": "S73",
      "title": "删除当前条目后选中相邻行而不是跳回首条",
      "goal": "用户反馈：删除中间的某条条目后，selection 跳回了首条。根因是 `HistoryPanelViewModel.refresh()` 发现旧 selectedID 不在新 items 里时 fallback 到 `filteredItems.first?.id`——对删除场景来说不是好行为，常见期待是光标停在紧邻的下一行（或上一行，如果删的是末行）。",
      "deliverables": [
        "`HistoryPanelViewModel.delete(_ item:)` 在调用 `store.delete` 之前：若 `item.id == selectedID`，就在当前 `filteredItems` 里找到它的 idx，把 `selectedID` 切到 `idx+1`（下一行），若 `idx` 是最后一行则切到 `idx-1`（上一行），只剩这一行时置 nil。pre-compute 后再 await store.delete——refresh 回来时看到 selectedID 已经在新列表里，不会再 fallback。",
        "非当前选中条目被删（例如 context menu 对其他行点 Delete）时 selectedID 保持不动——refresh 自然不会覆盖已有的有效 selection。"
      ],
      "acceptance": [
        "选中第 5 条，按 ⌘⌫ 删除 → 第 5 行消失，原第 6 条（现在是第 5 条）被选中。",
        "选中末条删除 → 倒数第二条被选中；删到只剩一条时再删 → 列表空，selectedID = nil。",
        "右键非选中条目删除 → 当前选中行保持不变。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿（85 tests）。"
      ],
      "commit_message": "S73: delete keeps selection adjacent to the deleted row (no more jumping to top)"
    },
    {
      "id": "S74",
      "title": "Vision OCR 识别中文（zh-Hans / zh-Hant）",
      "goal": "中文 UI 截图的 OCR 为空并降级到 classifier 的泛化 label。根因是 `VNRecognizeTextRequest` 默认只认英文；显式声明 `recognitionLanguages = [\"zh-Hans\", \"zh-Hant\", \"en-US\"]`，脚本歧义时偏向 CJK，英文仍然识别。",
      "deliverables": [
        "`App/VisionImageSummarizer.swift` 的 `VNRecognizeTextRequest` 设置 `recognitionLanguages = [\"zh-Hans\", \"zh-Hant\", \"en-US\"]`；中文排在前以便脚本歧义时偏向 CJK。",
        "保留现有 `.accurate` / `usesLanguageCorrection = false` 等既有参数，不改 classifier 分支。"
      ],
      "acceptance": [
        "截图一段全中文 UI 并拷入剪贴板 → 条目的 summary 出现识别到的中文正文，而非分类器的泛化 label。",
        "英文截图的 summary 仍能正常识别。",
        "`just build` / `just test` 全绿。"
      ],
      "commit_message": "vision OCR recognizes Chinese (add zh-Hans/zh-Hant to recognitionLanguages)"
    },
    {
      "id": "S75",
      "title": "搜索匹配 summary 字段（OCR 文本可检索）",
      "goal": "搜索过滤器只比对 `item.preview`，Vision OCR 从图片中提取的 `summary` 文本对搜索框不可见。把 `summary` 折入匹配：preview 或 summary 任一命中即算命中。",
      "deliverables": [
        "`Core/Sources/ClipboardCore/InMemoryClipStore.swift` 的 `search(query:filters:)` 在 `query` 非空时同时对 `item.preview` 与 `item.summary` 做 case-insensitive 子串匹配，任一命中即保留；其它 filter 语义不变。",
        "`Core/Sources/ClipboardCore/JSONSnapshotClipStore.swift` 的 `search(query:filters:)` 应用同样的改动，两处行为保持一致。"
      ],
      "acceptance": [
        "拷入一张带中文文字的截图、等 Vision OCR 产出 summary → 在搜索框输入该中文关键词，该条目进入结果；同关键词不在任何 preview/summary 中的条目不出现。",
        "`just test` 全绿；既有 preview-only 查询行为不回归。"
      ],
      "commit_message": "search matches on summary field too (OCR'd image text is findable)"
    },
    {
      "id": "S76",
      "title": "AI Summaries 区块 + Copy summary 的 7 语言本地化",
      "goal": "S63 引入的 AI Summaries 偏好文案只入了 `en.lproj`，其余 6 语言显示生英文；S70 的 Copy summary 按钮 tooltip / a11y label 从未进任何 `.strings`。补齐 8 个 key 在 en / zh-Hans / zh-Hant / ja / ko / de / es 的翻译。",
      "deliverables": [
        "在 zh-Hans / zh-Hant / ja / ko / de / es 的 `Localizable.strings` 中补齐 8 个 key：`AI Summaries`、`Enable AI summaries`、`Summarize images`、`Summarize text`、`Summarize files`、`Foundation Models available on this device.`、`Vision + NaturalLanguage on-device; richer summaries need macOS 26+ Apple Silicon.`、`Copy summary`。",
        "在 `en.lproj/Localizable.strings` 补上缺失的 `Copy summary`。",
        "不改动现有译文排序/其它 key。"
      ],
      "acceptance": [
        "切换到 7 种语言后打开 Preferences → AI Summaries 区块所有文案按语言正确显示，无生英文残留。",
        "hover 预览条目的 Copy summary 按钮 → tooltip 本地化。",
        "`just build` 全绿。"
      ],
      "commit_message": "localize AI Summaries section + Copy summary across 7 locales"
    },
    {
      "id": "S77",
      "title": "滚动卡顿修复：Row 用 SwiftUI 原生相对时间 + refresh 幂等短路",
      "goal": "用户反馈在高性能机上 HistoryPanel 滚动仍偶发卡顿。代码审阅发现两处主要元凶：(1) `ClipRowView` 的 body 里每帧调一次 `RelativeDateTimeFormatter.localizedString(for: item.createdAt, relativeTo: Date())`，每次 ViewModel publish 都让所有可见行重新生成本地化字符串；(2) `HistoryPanelViewModel.refresh()` 无差别 `self.items = results`，即便内容一致也会触发 `@Published` —— 滚动期间 SummaryCoordinator 回写 summary / togglePin 的 `.updated` 事件都会撞上手势。",
      "deliverables": [
        "`App/ClipRowView.swift`：删除 `Self.relativeFormatter` static，时间行改用 `Text(item.createdAt, style: .relative)` —— 由 SwiftUI 自己管理刷新，body 里不再每帧生成字符串。",
        "`App/HistoryPanelViewModel.swift` 的 `refresh()`：在赋值前比较 `results == self.items`，相等则直接 return，避免 `@Published` 无差别触发；不变的 selectedID 回退逻辑也不必跑。依赖 `ClipItem: Equatable`（已合成，不用额外工作）。"
      ],
      "acceptance": [
        "肉眼：面板里滚动 ~100 条历史时不再感到间歇性卡顿；尤其在后台 Vision OCR 完成、或 pin/unpin 非可见行时。",
        "行视图的 createdAt 文本仍会随时间自动刷新（SwiftUI 负责），语言切换后相对时间格式正确本地化。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿，测试数不回归。"
      ],
      "commit_message": "S77: fix list scroll stutter (SwiftUI relative-date text + idempotent refresh short-circuit)"
    },
    {
      "id": "S78",
      "title": "Preferences 新增 Shortcuts Tab：集中展示快捷键",
      "goal": "用户反馈在 Preferences 里看不到完整的快捷键清单。新增独立的 Shortcuts Tab：把原 General 里的『Hotkey』段（含 HotkeyRecorder 与未绑定 banner）整体迁移过去；下方追加只读的『In panel』段，列出 HistoryPanel 内全部 12 个面板内快捷键（↵ Copy / ↑↓ Select / Esc Close / ⌘P Pin / ⌘⌫ Delete / ⌘, Preferences / ⌘⇧[ Previous tab / ⌘⇧] Next tab / ⌘1–6 Filter）。Cocoa 系统级菜单项（⌘Q / ⌘W / ⌘M / ⌘H）按 macOS 惯例不展示也不允许改。面板内快捷键当前是 SwiftUI .keyboardShortcut 字面量，做成可改需要单独的 ShortcutBinding 抽象 + 冲突检测，超出本 slice 范围。",
      "deliverables": [
        "App/PreferencesView.swift：TabView 增加 .shortcuts case + selection binding；新增 `shortcuts` computed view，结构为『hotkeyMissing 警告 banner（条件渲染）→ Section(\"Global\") 内 HotkeyRecorder → Section(\"In panel\") 内 12 行只读 chip → footer caption』；从 `general` 移除原 Hotkey Section 与同名 banner；新增私有 `ReadOnlyShortcutChip` View（与 HotkeyRecorder idle 态同视觉：圆角 6、primary 0.06 fill、primary 0.10 stroke、12pt monospaced、minWidth 110）；面板内快捷键以 `[(keys: String, label: LocalizedStringKey)]` 字面量数组维护，源真相来自 HistoryPanelView.keyboardShortcutButtons，注释提醒同步更新。",
        "App/PreferencesView.swift：body 加 .onAppear 在 hotkeyMissing 时把 selectedTab 切到 .shortcuts —— 用户被 S23 escape 路径或手动开偏好时直接落到 Shortcuts tab，看到 banner 与 recorder 不需要二次点击。",
        "7 份 Localizable.strings：新增 6 个 key —— Shortcuts / Global / In panel / Previous tab / Next tab / In-panel shortcuts only work while the history panel is focused. 译文按各 locale 已审定的术语风格补齐（en/zh-Hans/zh-Hant/ja/ko/de/es）。原 \"Hotkey\" key 暂留各 locale 文件作为 orphan，无副作用。"
      ],
      "acceptance": [
        "打开 Preferences → 顶部 TabView 出现 4 个 tab：General / Shortcuts / Privacy / Data；点 Shortcuts → 上方为 Global 段含 HotkeyRecorder（与 S43 视觉一致），下方 In panel 段以只读 chip 列出 12 条快捷键。",
        "在 Shortcuts tab 用 HotkeyRecorder 录入新快捷键（如 ⌃⌥⌘C）→ 关闭 Preferences → ⌥⌘V 无反应、新组合可召唤面板（与 S10 行为一致，未回归）。",
        "在 Shortcuts tab 清空快捷键 → 立即出现『No shortcut is set』banner；切换到 General tab → 不再有重复的 Hotkey 段；再次打开 Preferences → 自动落在 Shortcuts tab（hotkeyMissing 时）。",
        "切换 7 种语言后 Shortcuts tab 标题、Global / In panel section 标题、12 行 label、底部 caption 均按对应语言显示，无英文残留。",
        "Cocoa 菜单项（⌘Q / ⌘W / ⌘M / ⌘H）不出现在 Shortcuts tab。",
        "`just fmt` / `just build` / `just lint` / `just test` 全绿，测试数不回归。"
      ],
      "commit_message": "S78: preferences shortcuts tab (move global hotkey + read-only in-panel listing)"
    },
    {
      "id": "S79",
      "title": "Check for Updates 本地化 + Sparkle 多语言 release notes",
      "goal": "S54 的 Sparkle 更新通道引入了『Updates』/『Check for Updates…』/『Automatic updates are not configured in this build.』三行英文文案，但未进入 7 份 `.strings`，非英文用户直接看到英文；同时 `docs/appcast.xml` 的两个 `<item>` 都没有 release notes 字段，有新版本时 Sparkle 弹窗显示空白。本 slice 一并修掉：补齐三行文案的 7 种翻译，并为每个历史版本写 7 份极简 HTML release notes，通过 `<sparkle:releaseNotesLink xml:lang=\"…\">` 接入 appcast，由 Sparkle 根据用户系统语言自动挑对。",
      "deliverables": [
        "7 份 `App/<locale>.lproj/Localizable.strings` 各新增 3 个 key —— `Updates` / `Check for Updates…` / `Automatic updates are not configured in this build.` —— 译文按各 locale 已审定的术语风格补齐（en / zh-Hans / zh-Hant / ja / ko / de / es）。",
        "`docs/release-notes/<version>/<lang>.html` × 14：为 1.0.0 与 1.0.1 各写 7 份轻量 HTML（section 复用 CHANGELOG.md 的结构：新增 / 优化 / 分发；内联 CSS 支持浅/深色，字体跟随系统）。这些文件通过 GitHub Pages 暴露为 `https://carter-ya.github.io/clipboard/release-notes/<version>/<lang>.html`。",
        "`docs/appcast.xml` 为两个 `<item>` 各追加 7 条 `<sparkle:releaseNotesLink xml:lang=\"…\">`，指向上述 HTML。Sparkle 会按系统 `AppleLanguages` 自动挑对，无匹配时回退到首个 link（en）。",
        "`Justfile` 新增 `release-notes-check VERSION` recipe：遍历 7 种 locale 确认 HTML 都在；缺失则打印列表并以非零退出。开发者在 `git tag` 前可先本地跑一把预演。",
        "`.github/workflows/release.yml` 新增『Verify localized release notes』step（位于 tag↔project.yml 版本核对之后、tests 之前），直接调用上面的 just recipe——任何一种语言缺 HTML 就让 CI 停在 package 之前，避免推出缺 notes 的 appcast。",
        "同一个 workflow 的『Emit appcast snippet』step 改写模板，生成的 `<item>` 里自动带上 7 条 `<sparkle:releaseNotesLink xml:lang=\"…\">`，URL 指向 `https://carter-ya.github.io/clipboard/release-notes/${VERSION}/<lang>.html`，无需再手工改 appcast。",
        "`.github/RELEASE_TEMPLATE.md` 末尾增加发版前 checklist（HTML 注释形式，不进 GitHub Release 正文）：提醒 project.yml 版本、CHANGELOG、7 份 release notes、tag 四步。"
      ],
      "acceptance": [
        "切换到 7 种语言后打开 Preferences → Updates 区块标题、按钮、未配置提示均按语言显示，无英文残留。",
        "从 1.0.0 构建手动触发 Check for Updates → Sparkle 弹窗展示 1.0.1 的 release notes（中文系统显示 zh-Hans 版本，日文系统显示 ja 版本，以此类推）。",
        "`just release-notes-check 1.0.1` 打印 `7/7 present` 并退出 0；`just release-notes-check 99.0.0` 列出 7 份缺失文件并以非零退出。",
        "假定故意删掉 `docs/release-notes/1.0.2/ja.html` 后打 tag `v1.0.2` → CI 在 `Verify localized release notes` step 红掉，error 消息指明缺哪份文件。",
        "`just build` 全绿；`docs/appcast.xml` 经 `xmllint --noout` 校验通过。"
      ],
      "commit_message": "S79: localize Updates section + multilingual Sparkle release notes (with release-pipeline gate)"
    },
    {
      "id": "S80",
      "title": "移除历史面板底部的快捷键 footer",
      "goal": "S78 把所有快捷键收进 Preferences → Shortcuts 之后，历史面板底部的 6 格 `ShortcutHint` footer 就成了冗余曝光：同一份信息在用户常驻视线内多占一条，还挡掉了 1~2 行列表内容。本 slice 直接删 footer，让面板更紧凑。",
      "deliverables": [
        "`App/HistoryPanelView.swift` 删掉 body 里紧贴 footer 的 `Divider()` 和 `footerBar`，以及 `footerBar` computed property；面板 frame 高度从 520 收到 491（少掉的 29pt = footer 28pt + Divider 1pt）。",
        "`App/ShortcutHint.swift` 整个文件删除：footer 移除后无人引用（grep 确认无剩余调用点）。",
        "`App/<locale>.lproj/Localizable.strings` 中 `Select` / `Copy` / `Pin` / `Delete` / `Preferences` / `Close` 这 6 个 key 一律**保留** —— 右键菜单 / `AppDelegate.swift` / `PreferencesView.swift` 的 Shortcuts tab 仍在用。"
      ],
      "acceptance": [
        "`just run` 召唤历史面板 → 底部 footer 消失，面板整体降低约 29pt，列表区域自然多显示 1~2 条。",
        "Preferences → Shortcuts tab 仍完整列出所有快捷键；右键菜单的 `Copy / Pin / Delete` label 未变。",
        "`grep -R ShortcutHint App/` 无输出；`just build` / `just lint` / `just test` 全绿。"
      ],
      "commit_message": "S80: drop history panel footer hints (Shortcuts tab already covers them)"
    }
  ],
  "slice_rhythm": [
    "1. 先改协议与测试（红）。",
    "2. 实现至测试全绿。",
    "3. `just run` 手动演示本 slice 的验收标准。",
    "4. `just lint` 干净，然后按 slice 指定的 commit_message 提交。",
    "5. 如果本 harness.json 里有任何与现实不符之处，在同一次提交里一并修订。"
  ],
  "definition_of_done_per_slice": [
    "所有 deliverables 均已合入。",
    "所有验收项都已校验（能自动化的用自动化；手动步骤写入 commit 正文）。",
    "Core 测试全绿；没有新增 @Skipped 用例。",
    "`just lint` 干净。",
    "若约定有变，同步更新 CLAUDE.md。",
    "若计划有变，同步更新 harness.json。"
  ],
  "guardrails": {
    "must": [
      "业务逻辑留在 ClipboardCore 内部。",
      "先定义协议，再写具体实现。",
      "每个新模块都要有一份可在测试中使用的 Fake 实现。",
      "每个 slice 以绿色、可运行、可提交的状态结束。",
      "任何偏离 harness.json 的改动必须在同一次提交中更新 harness.json。"
    ],
    "must_not": [
      "在 ClipboardMonitoring（读）/ PasteboardWriter（写）之外触碰 NSPasteboard。",
      "通过 CGEvent / AppleScript / Accessibility 合成键盘事件——本应用绝不自动粘贴。",
      "把 sensitive=true 的条目以任何形式写入磁盘（history.json、blobs/、导出文件、日志中的 payload 正文均不得明文出现）。",
      "在 monitor_queue 上算 sha256 或做磁盘 I/O；在 store_queue 上读写 NSPasteboard。",
      "引入 tech_stack 未列出的依赖，除非先更新本文档。",
      "启用 App Sandbox（会把开机启动和源 App bundle ID 检测搞复杂）。",
      "提交生成的 .xcodeproj。",
      "引入跨平台抽象——本项目有意只做 macOS。",
      "用 print() 记日志；统一用 os.Logger。"
    ],
    "required_permissions": []
  },
  "risks": [
    {
      "risk": "轮询可能漏掉 <80ms 内的连续拷贝；即便命中，同一 changeCount 只看得到最新一次内容（NSPasteboard 语义决定，与间隔无关）。",
      "mitigation": "默认 80ms 轮询 + tick 内 changeCount 短路，使单次 tick 开销≈一次 IPC；人类双手连击 Cmd+C 几乎不可能触发 <80ms 间隔。若仍有用户反馈，再考虑更短间隔或 NSPasteboardObserver 私有 API。",
      "owner": "S1"
    },
    {
      "risk": "RTF / HTML 往返在某些目标 App 中会丢失格式。",
      "mitigation": "PasteboardWriter 始终写入所有原始 pasteboard 类型，由接收方挑选；场景测试覆盖常见组合。",
      "owner": "S6"
    },
    {
      "risk": "用户关闭面板后焦点没有被正确保留，Cmd+V 落到错误的 App。",
      "mitigation": "非激活面板（S4）保证目标 App 始终不失去前台状态——本应用不会在 Dock 中被激活。S4 的验收项会显式校验目标 App 保持前台。",
      "owner": "S4"
    },
    {
      "risk": "大图片即便有条数上限仍可能让磁盘无限增长。",
      "mitigation": "按 sha256 的 blob 引用计数在驱逐时 decrement 并在归零时 unlink；启动 reconcileBlobs() 清理孤儿；可选的第二道总大小上限；列表中只显示缩略图而非完整图片。",
      "owner": "S2"
    },
    {
      "risk": "防抖写入可能在崩溃时丢失最后 N 个非敏感事件。",
      "mitigation": "500ms 防抖可接受；NSApplication willTerminate 时强制 flush；仅当用户反馈丢失才重新评估。",
      "owner": "S2"
    },
    {
      "risk": "敏感条目在 App 重启后丢失，对期望持久保留的用户可能反直觉。",
      "mitigation": "这是『不明文落盘』安全目标的有意权衡，不是缺陷。UI 中对 sensitive 条目明示『关闭 App 后将丢失』；敏感条目不做加密持久化——永久决策，不再评估。",
      "owner": "S7"
    },
    {
      "risk": "Monitor 在大 payload 上做探测时阻塞 poll tick → 漏拷贝。",
      "mitigation": "SizeFilter 的内联读取早停（达到 maxClipSizeBytes 即中断）；sha256 和磁盘 I/O 搬到 store_queue；monitor_queue 只做 probe + bundleID + filter 链。",
      "owner": "S1/S2"
    },
    {
      "risk": "超过几千条且大量 payload 被内联时，JSON 快照性能会下降。",
      "mitigation": "默认 100 条的容量上限足以把文件控制得很小。较大 payload 单独存到 blobs/，history.json 保持只存元数据。用户把 cap 调到 >5000 再讨论换存储格式——v1 不用操心。",
      "owner": "S2"
    },
    {
      "risk": "S9 把太多 UX 打磨挤进一个 slice，工作量失控。",
      "mitigation": "S9 按 deliverables 的顺序实现，允许按清单切 sub-commit；如果 v1.x 要提前发布，可先合入『首启引导 + 冲突检测 + 菜单栏菜单 + 拒绝反馈』四项作为 S9a，剩余作为 S9b。",
      "owner": "S9"
    }
  ],
  "features_roadmap": {
    "mvp_v1_0": [
      "S0",
      "S1",
      "S2",
      "S3",
      "S4",
      "S5"
    ],
    "v1_1": [
      "S6",
      "S7"
    ],
    "v1_2": [
      "S8",
      "S9"
    ],
    "v1_2_patch": [
      "S10",
      "S11",
      "S12",
      "S13",
      "S14",
      "S15",
      "S16",
      "S17",
      "S18",
      "S19",
      "S20",
      "S21",
      "S22",
      "S23",
      "S24",
      "S25",
      "S26",
      "S27",
      "S28",
      "S29",
      "S30",
      "S31",
      "S32",
      "S33",
      "S34",
      "S35",
      "S36",
      "S37",
      "S38",
      "S39",
      "S40",
      "S41",
      "S42",
      "S43",
      "S44",
      "S45",
      "S46",
      "S47",
      "S48",
      "S49",
      "S50",
      "S51",
      "S52",
      "S53",
      "S54",
      "S55",
      "S56",
      "S57",
      "S58",
      "S59",
      "S60",
      "S61",
      "S62",
      "S63",
      "S64",
      "S65",
      "S66",
      "S67",
      "S68",
      "S69",
      "S70",
      "S71",
      "S72",
      "S73",
      "S74",
      "S75",
      "S76",
      "S77",
      "S78"
    ]
  },
  "open_questions": [
    "macOS 最低版本：保持 13.0 底线——已决，不用数据库，没理由再抬高",
    "默认 cap=100 是否仍需调整——v1 定为 100（同类产品多为 200+，此值偏保守），S8 偏好中滑块量程给足，必要时按用户反馈再改默认"
  ]
}
